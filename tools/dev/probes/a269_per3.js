/* #269 - the new build's fights run longer (70% absorb on both sides), so the
   matrix has to chunk smaller or eyes.py's 120s SOCKET timeout kills the eval
   and wedges the page. Three fight-comps a call. */
(function(){window.BAL_PER=3;return 'BAL_PER='+window.BAL_PER;})()
