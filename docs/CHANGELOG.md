# RabbleBound - the record

> **Everything in this file is history, not instructions.** It exists because the reasoning behind
> a past decision is the most expensive thing to reconstruct and the cheapest thing to keep - but
> nothing here is a task, and many of its numbers were true only on the day they were written.
>
> **Changelog rows are a historical record: the numbers inside them describe what a build did at
> the time and are deliberately not updated.** [`01_GAME_CONCEPT.md`](01_GAME_CONCEPT.md) and the
> running build hold what is true now. **When the record and the build disagree, the build wins.**
>
> *Split out of [`00_PLAN_AND_BACKLOG.md`](00_PLAN_AND_BACKLOG.md) on 2026-07-31 at the user's
> request - the working file keeps only what a session can act on; everything shipped or
> superseded lands here. When an active entry is built: move its full text here, leave its row in
> the Done table there.*

**Contents:** [the build log](#the-build-log--one-row-per-sub-build) ·
[built backlog entries in full](archive/CHANGELOG_BUILT_ENTRY_TEXT.md) ·
[the outside review's P0 list](#codex-packet--status) ·
[playtest snapshots](#snapshots-kept-for-the-reasoning) ·
[the original phase plan](#the-original-phase-plan)

## The build log - one row per sub-build

### Phase A - Pre-production (paper only, no code)

| Step | Deliverable | Status |
|------|-------------|--------|
| 1 | **Game Concept** (`01_GAME_CONCEPT.md`) - pillars, loops, tone, scope, run economy, metaprogression | ✅ DONE |
| 2 | **Art Direction + visual prompt pack #1** (`02_ART_DIRECTION.md`) - style bible, palette, tech specs, 8 prompts | ✅ DONE - awaiting generated results |
| 2.5 | **Style Bible** (Claude design project) - Ash & Iron base + 4 atmosphere keys + screens 4a/4b | ✅ DONE |
| 3 | **Core combat design** - stat tables, threat budget, professions, enemy types | ✅ SUPERSEDED - designed *in* the slice and documented in `01_GAME_CONCEPT.md`. (The "Winds economy" line here is dead: **Winds of Magic was cut entirely** - casting costs the caster's own nerve instead.) |
| 4 | **Overworld design** - node map rules, event types, camp choices, resource economy | ✅ SUPERSEDED - same: built and documented. Economy reworked twice since (see the Provisions and Party-size sections of the concept). |
| 5 | **MVP scope lock** - cut list, data formats, Godot 4 port plan | pending - worth doing *after* the settlements MVP, when the systems stop moving |

### Phase B - Vertical Slice (first playable)

| Step | Deliverable | Status |
|------|-------------|--------|
| 6 | **Browser slice v1** - 2 world nodes → hex battle → post-battle dialog | ✅ DONE |
| 7 | **Browser slice v2** (`prototype/grimtoll_slice.html`) - 3-route map with real distances & food, 3-action combat, morale & routing, visible race art, run/camp resources | ✅ DONE - wins in ~8 rounds, inside the band |
| 8 | **Browser slice v3** - you as the Captain, charisma & wages, archer + battle-mage, enemy captains, engagement rule, surrender & mercy, visible trinkets | ✅ DONE - 7 rounds, inside the band |
| 8b | **Browser slice v4** - four core stats, equipment with sized armour, traits, classes/levels/perks, Battle-Brothers morale, readable hit & dodge, inventory screen, richer map | ✅ DONE |
| 8c | **Browser slice v5** - after-battle spoils & mercy, dialogs on the map, daily upkeep, camp incidents, injuries from greed, recruitment, a second (beast) battle, trinket tooltips, funny gear | ✅ DONE |
| 8d | **Browser slice v6–v8** - scars & downed, facing/backstab arcs, spell ladder, The Old Camp (forge + yard + roster, localStorage persistence), healing over time, brigand faction + per-battle terrain, synthesized audio & music | ✅ DONE |
| 8e | **Browser slice v9–v10** (first playtest incorporated) - two-hex lindwurm boss + circling cub, four spell schools (casters roll 2), facing/backstab arcs, AUTO & WITHDRAW (final battle is life-or-death), perk points spendable from inventory, event recruitment (Pell), big-four HUD, day-1 start, node vignettes, withdraw-softlock fixed | ✅ DONE |
| 8f | **Content layer + lore split** - `docs/03_WORLD_LORE.md`, `content/` folder (names & personalities, events, art prompts), 16 floating events shuffled per run, 14 personalities, 12 names per race, battle rebalance (2 actions, tighter spreads) | ✅ DONE |
| - | **`docs/06_DESIGN_REQUEST_FOR_GPT.md`** - self-contained brief for an outside designer: rewrite choice labels away from arithmetic, 10 events, 12 personalities, 8 camp incidents. Paste-and-go, no code access needed. | ✅ SENT · deleted 2026-07-31 - jobs done, facts stale |
| - | **`docs/07_WORLD_MAP_ART_BRIEF.md`** - tech task for the world-map visual pass: what to send an image generator, what I wire up afterwards, and the fixed node coordinates it must not disturb | ⚰ SUPERSEDED · deleted 2026-07-31 - its "fixed" node coordinates predate the map rebuild |
| 9 | **Settlements & Legacy - metaprogression redesign** ([09_SETTLEMENTS_AND_LEGACY.md](09_SETTLEMENTS_AND_LEGACY.md)). Designed, NOT built. Materials get spent inside the story (reforge, repair the village forge, build an infirmary, strengthen a gate) instead of banking abstractly; between acts the settlement takes 1–2 buildings that change scenes rather than numbers; buildings do NOT cross runs - each leaves ONE legacy (a blueprint, a tool, a known smith, a guaranteed node, a person), so **possibility transfers, not power**; gems become rule-breakers (finish a building with no materials, keep one between runs, pass a dead character's weapon on) with the axis "survive now vs outlast the company". The wagon stays as mobile inventory and legacy-carrier. | designed |
| - | **PILLAR promoted to the concept: "You cannot afford to be good to everyone."** Six rules in [01_GAME_CONCEPT.md](01_GAME_CONCEPT.md): the good path exists but never gets everything · no universal optimal route (incompatible kinds of success; every act costs one important thing) · bad deeds actually pay · good deeds don't secretly pay better · consequences personal, never a meter · deeds change future options, not a bar. This is the content-authoring yardstick for every event from here on. | pillar |
| - | **⚠ Logged, not fixed at the time: backlog #32 - the Fen-Mother can reach a state that never ends.** *(Since CLOSED; #32 now carries the rally rule instead.)* Roughly one arena boss fight in forty never resolves (835 rounds before the harness gave up): the last survivor oscillates across the rout line every round (`steadies · breaks · misses`), routed so their hit chance is floored at 5%, while `noRout` stops her leaving and `checkEnd`'s mercy branch excludes `mother` by design (B03). **Pre-existing and unrelated to the pathing fix** - 0/20 stalls with `closeOn()` on and 0/20 with it off. Not fatal (she is withdrawable) but it reads as a broken game. Left out of this batch deliberately: it wants a morale/rout change, which is its own package. | logged |
| ... | **The 195 rows before 8f.200 are in [`archive/CHANGELOG_BUILD_LOG_pre_8f200.md`](archive/CHANGELOG_BUILD_LOG_pre_8f200.md)** - split out 2026-08-21, still greppable, same ascending order. | ✅ |
| ... | **The rows from 8f.200 to 8f.274 are in [`archive/CHANGELOG_BUILD_LOG_pre_8f275.md`](archive/CHANGELOG_BUILD_LOG_pre_8f275.md)**, moved there on 2026-09-01 for the same reason the 195 before them were moved: the live file is the era being worked in. `grep` still reaches them and `tools/record.py` reads them as part of the record. | 
| 8f.305 | **#281 - THE GLASS-CANNON MAGE IS DELETED, AND THE CLASS LAB SAYS WHAT EACH WEAPON UNLOCKS.** Two asks, one root. **AA1 answered, the other way from the recommendation** (user: *“Older MAGE off the wall - we can delete from here and from current build”*): the ROW goes, not the name. #243 had turned the glass cannon off the muster wall with one `const` and kept the row alive because *“traits, `isCaster`, the `elemental` gate and every save written before today still name it”* - so the fix is to teach those readers rather than to keep a dead class for them. **⛑ THE MECHANIC SURVIVES THE CLASS, WHICH IS THE WHOLE DESIGN OF THE DELETION.** `SCHOOLS` is now the definition of a caster rather than a count beside one: `isCaster` is `!!SCHOOLS[cls]` and `unitFrom`'s second casting reads `want>1` instead of `cls==='mage'`. Give any class a `SCHOOLS` row above 1 and it gets two schools and a second working at half, with no other edit - **proved by making it fire**: `SCHOOLS.warmage=2` hands the battle-mage two schools, `=1` hands him one, and the row is put back inside a `finally`. Twenty-three rows went with the class (`CLSLEAN`, `CLASSHELP`, `CLASS_ICON`, `STAT_PRIO`, `START_GEAR`, `KIND_BY_CLS`, `MAGE_GLASS`, the perk lane, the sig-act row, four personality gates, two sprite keys, two linter lists), and `CLASSHELP.mage` was the one the sweep missed - **caught by the probe, which asked each of the eight tables whether it still had the row, not by reading the diff.** **⛔ AND A DELETED CLASS KEY GETS A DOOR, NOT A CRASH.** A save written by any earlier build can carry `cls:'mage'`, and with the row gone `CLASSES[p.cls].n` throws and takes the render with it. `CLS_RETIRED` + `clsAlive()` map it to the survivor at the two places a class key arrives from outside the file - `loadRun` and `simStock` - and an UNKNOWN key lands on `none` rather than on a guess. **Proved by making it fire**: a real save was written with `cls:'mage'` on a founder, loaded through the real `loadRun`, and read back as `warmage`; localStorage and `G` are snapshotted and restored, because the user has a live run in there. **⛑ One live defect fell out of it.** The Bloom-Stem's `bonusD` has read *“+2 INT more on a battle-mage”* since it was written and its `forCls` gated on the GLASS CANNON, who has been off the muster wall since #243 - so the clause had been unpayable for two entries while advertising itself on the card. The promise-versus-payment split, on a seventh surface. Measured after the fix: a warmage in the stem reads INT 4, a spear reads 2. **The class lab (#280) is rebuilt around the second ask** (*“add more visual weapon classes can use and perks and skills, especially class ones”*, *“not class ones details on hover”*, *“for each class add accessible weapon with skill clear (for example archer - crossbow and bow)”*, *“make it more manageable and changeable”*). A **sticky class filter** takes the page from 9,448px to 3,707px on one class; the class's OWN skills are full cards with their mechanics on the page and everybody else's are chips with the text on hover; and **WHAT IT CAN CARRY, AND WHAT EACH ONE UNLOCKS** is a visual grid of every main-hand row, with the real item painting, built as the race that could actually be hired as that class. ⛔ **Nothing in the build holds a “weapons this class may use” table and nothing should** - a weapon does not suit a class, it CHANGES WHAT THE CLASS DOES - so the tool builds the body once per weapon and reads the cards back off `unitFrom`, with the BARE-HANDED body as the control arm so it can say which acts the weapon brought. That is how it prints, without a word of it authored: archer + hunting bow = CRIPPLING SHOT, archer + crossbow = WIND IT and PUNCH THROUGH; spear + boar spear = SPEAR WALL, the same spear + halberd = BRACE AND SHOVE; and an ogre brute's SWEEP scaling to whatever is in his hands, 5-7 with the warclub and 2-2 with a stave. The tool's own typed list of seven class keys was deleted at the same time and derived instead - #281 would have kept drawing a MAGE card off it. **Gates**: `LINT()` 0 · `gates.js` all counters clean, `cards.emdash` false, `noIco` 0 · `smoke.js` 19 steps, no errors · `regress.js` all eight fights ok, no ERR/FATAL/HIT GUARD · `promises257.js` 18 rungs all PAYS · `reach.js` unchanged (the working stave keeps a door: Marrow's own kit) · `tools/artifacts.html` regenerated, and the Bloom-Stem's user row now reads MAGE. |
| 8f.304 | **#280 - THE CLASS LAB: EVERY CLASS IN ONE TABLE, A BODY BUILT BY HAND, AND A FIGHT TO TRY IT IN.** (User: a tool for the classes like the one for the enemies. One tab: the class, what it gives, what perks it opens. Another: pick a race, a class, a level and the art, build a body and see what it looks like; see the artefacts it can reach; and start a simple fight - two bandits and a bar-ish map - to see how it plays. And one column, for the design rather than the player: strength, weakness, archetype.) `tools/classes.html`, in **the gauntlet's shape and not the foedex's**: it live-loads the game in a hidden same-origin iframe and evals a bridge into it, so nothing is generated, nothing is a second copy of `CLASSES`, `PERKS`, `GEAR` or a derived figure, and a lean retuned in the prototype is in the tool at the next RELOAD BUILD. **Tab one** is the seven classes plus the stranger, one card each - signature, lean, start gear, battle-art kind, where its stat points go, who may be hired as one - beside the three races, and the whole tree in three lanes over eight tiers. **Tab two** builds one body through the build's own doors (`finishRecruit` folds the leans, `levelUp` banks the promotion, `unitFrom` deals the cards) and prints the four ladders with their named contributors, the hitpoint and dodge and to-hit and nerve writers, the acts, the perk pool with the ticks in your hand, and the whole armoury filtered by this race's harness. **And it takes the body onto the practice field**: `SIM` is borrowed exactly the way `simStart` borrows it, `DEVFOES` fields two Poachers off the ⚙ bench's own catalogue, and `SIM.ground='tavern'` puts them in the tap-room - `TERRAIN.tavern` is a ground like any other and was simply never on `SIM_GROUNDS`' list. Leaving calls the build's own `simRestore`. **⛔ The one authored thing is the STRENGTH / WEAKNESS / ARCHETYPE column**, marked AUTHORED wherever it is drawn, and it sits beside the derived column each judgement is made against. **Four defects found by driving rather than reading**: the readiness poll asked for `w.CLASSES` and a top-level `const` is a global LEXICAL binding that is never a property of `window`, so the tool spun forever on a game that was ready in eight seconds; `.big div` matched the three divs INSIDE each tile and drew three nested boxes; the clickable perk chip carried class `pick`, which is also the button-row flex CONTAINER, so every chip computed `display:flex` and stretched to 365px of a 368px lane; and `face()` went through `drawArt`, which paints inside `im.onload`, so `toDataURL()` on the next line handed back a blank canvas. Two more were arithmetic: `spec.level|0||1` swallowed the level-0 stranger, and the `START_GEAR` fallback armed him with a cudgel against #227's own rule that he comes empty-handed. **⛑ And the board was diffed, not counted.** `terrainCheck()` calls the tap-room `ok:false` - *a single mandatory corridor at column 3* - which reads like a broken ground until the SHIPPED tap-room brawl is measured on its own: same sentence, 16% walkable against this tool's 24%. It is what the room is. The verdict is printed in the arena bar rather than hidden. `tools/dev/gt.py` learned one thing: an absolute http(s) URL passes through `url_for` instead of being wrapped in `file:///`, because Chrome gives a `file://` frame an opaque origin and the bridge can never take there. **Gates**: `gt.py check` 0 problems on the prototype and on the tool · `emdash.py` prose 0 · 72 race x class x level cases through the builder with zero failures, every one with a face, a figure, a sprite, acts and a price · the fight played out to `won:true` in 11 rounds with the build's own `aiTurn` · `G` restored to You/Vesna/Marrow/Ilka after leaving. |
| 8f.303 | **#279 - THE ARMOURY: EVERY ARTIFACT, WHERE IT COMES FROM, AND WHO IT IS FOR.** *(User: “can you please do artifacts managemt tools (same as for enemies) and put it in separete tool. It should be easily manageble for me”, then “artifacts group by type. Also add a class that can use them fully. And also add option for me to create new artifact”.)* ⛑ **`tools/artifacts.html` is one table of every artifact the game can hand you** (60 rows, 55 painted): the painting, the tier in its own colour, the slot, what it stops and what it does to a body, the price `gearPrice()` charges and the break-down `scrapPay()` pays, every effect the row carries, **where a player can actually get it**, **who gets full value out of it**, and the description. Built the way the foe dex is built - `tools/dev/probes/gear.js` derives it off the running build and `tools/dev/build_gear.py` lays it out - so a `GEAR` row change moves the file by itself and nothing is authored twice. Edits live BESIDE the row in localStorage and ride EXPORT, so a regenerate can never cost anybody an afternoon · ⛔ **WHERE A THING COMES FROM IS SCANNED, NOT LISTED**, because a second list beside the row is the promise-versus-payment shape this repo keeps deleting: a door already carries `gear:'<key>'`, so `EVENTS`, `CAMPS`, `VIGNETTES`, the 15 `LOOT` tables, the roster, `START_GEAR` and `CRAFT` ARE the index. **The walk is DEEP and it reads CODE as well as tables** - three keys are handed over by a function (`cubChoice` builds its card at runtime, `pickChoice` holds a `stashGain('oddcoin')` and walks a body on wearing the Toll-Man's boots) and a decks-only census called all three unreachable, which is the worst lie a reach column can tell. ⚠ **Comments are stripped before that scan and it is not tidiness**: `LINT`'s own essay quotes `gear:'spear'` while explaining a check, and the first cut reported the linter as a place the road hands you a boar spear · ⚡ **AND IT FOUND FOUR ARTIFACTS NOTHING IN THE GAME CAN HAND OVER** - **The Wood-Stick With Two Holes** (legendary), **The Mirrored Sphere**, **The Company Banner** and the **Foundry Ledger-Pin**. All four are `unique`, so `findPool` refuses them, and grep finds **zero** references to any of them outside their own `GEAR` row: painted, priced, break-downable, and structurally unreachable. Filed as 👤 **Z1** · ⚡ **AND FIVE SENTENCES QUOTING A FIGURE THE ROW NO LONGER PAYS.** `HDA_CUT` rewrites `armour` and `dmg` IN PLACE at boot, and the forge at the Old Camp still advertises the authored numbers: the Foundry plate is sold as *96 armour* and stops **27**, the rat-silk shroud as *30* and stops **9**, the lashed plates as *74* and stop **21**, the Weeping Hammer as *27-40* and hits for **8-11**. The test is arithmetic and not a judgment - an integer in the prose which, PUT THROUGH THE CUT, lands on what the row pays is the pre-cut figure still on the page - and **it is run over both halves for exactly the reason rule 5 exists**: `GEAR`'s own `d` lines quote no figures and report zero, the forge's five all report, so the check is proved to fire rather than proved to be silent · ⛑ **WHO USES IT FULLY IS DERIVED OFF THE FIELDS, BECAUSE THERE IS NO CLASS-GEAR TABLE IN THIS BUILD AND WRITING ONE HERE WOULD BE A SECOND ONE.** Nothing stops a mage picking up a warclub; what decides who a piece of kit is FOR is which field it carries and who the code that reads that field pays. `spellRange` and `windsCut` are spent by a working and by nothing else, so they are the caster's; `rangedDmg` is added only when `main.range` is set, so it is the archer's; `meleeHit` lands on the blow, so a bow gets nothing; `forCls` names the class itself. **Every clause carries the line it is read off**, and a row that asks nothing of its carrier honestly answers *anybody* · ⛑ **THREE GROUPINGS, AND SORTING NO LONGER SWITCHES GROUPING OFF.** By slot, by tier, or by **the first way a player can meet it** - day one, a hire, the field, the road, the fire, the forge, in code, a roll and nothing else, or nothing at all. A row can come from four places and a group takes one key, so it is filed under the earliest; repeating it under four headers would make every count on the page a lie. The foe dex's rule (a header saying DOGS over rows ordered by hitpoints is a claim the rows do not support) does not survive an explicit control: TRINKET over rows ordered by armour is still true of every row under it · ⛑ **AND A NEW ARTIFACT CAN BE SKETCHED IN THE TOOL.** A form built off the same tables the table is - slots from `slotOrder`, tiers from `RARITY`, sizes from the sizes the build's rows use - plus one free-text field where **any** `GEAR` field works, including one that does not exist yet. It lands in the table as a real row, grouped by its own slot, editable and deletable, and EXPORT hands back the **paste-ready `GEAR{}` source line** rather than a description of one somebody has to transcribe. ⛔ **AND ITS PRICE IS CHARGED BY THE BUILD'S OWN `gearPrice`, SHIPPED INTO THE FILE AS SOURCE** and run against a one-row shim: a second price model living in the tool is precisely the drift the tool exists to report. ⚠ **A draft is not an edit and RESET must not eat one** - it is the only copy of something that exists nowhere else, so it is asked about separately. **Gates:** `gt.py check` 3 scripts 0 problems · `emdash.py` **0 prose** · `node --check` on the whole template script · **driven, not read**: 60 rows, 5 slot groups, 4 tier groups, 9 source groups summing to 60, 55 images all loaded, the search proved to reach a door's text, a fight's name, an effect word, a raw field key and a code grant, an edit proved to ride localStorage and to clear itself when put back, the redraw flag proved to move the count line, and a draft carried the whole way from the form to a `ratcloak:{...}` source line at the price `gearPrice()` charges for it. **Eye check:** `shots/armoury_items.png`, `shots/armoury_where.png`, `shots/armoury_orphans.png`, `shots/armoury_forge.png`. *(2026-09-01, 8f.303)* |
| 8f.302 | **#278 - THREE VACUOUS CHECKS, THE TABLE THE LINTER NEVER READ, AND TWO DEAD VIGNETTES.** *(No ask: a solo pass over the standing gates, on the user's instruction to take the small fixes that do not need him.)* ⛔ **`smoke.js` HAD BEEN CALLING TWO FUNCTIONS THAT HAVE NEVER EXISTED IN THIS BUILD.** `newRun` and `openShop` are both absent, and both sites were wrapped in `if (typeof X === 'function')`, so the walk printed `newRun ok` and `shop ok no openShop` on every run it has ever had. ⛑ **This is rule 5 exactly**: a check that has only ever returned ok is indistinguishable from a broken one, and this file shipped two of them. The run starts with **`enterWorld`** and the shop is **`openVillage`**; every symbol is called BARE now, so a rename throws, which is the entire job of a smoke walk. ⚡ **AND THE SHOP STEP WAS NOT THE ONLY GAP IT WAS HIDING.** Nothing in the walk had ever opened a surface the player spends money on: the village (**6 doors**), provisions (**6 doors**) and the wagon (**8 doors**, the whole #270 racks-and-kept-crate system, with no smoke coverage at all since the day it landed), plus the camp incident, the vignette and the menu. Fourteen steps became **nineteen** and the walk is 0 throws. ⚠ **A camp card and a vignette are BEAT cards and have no doors when they open**, so counting buttons there reads 0 and means nothing; what is asserted is that `#wDlg` is on with an `.evcard` in it, and what is reported is the cast off `.evname` (checked on the DOM: it is the person on the painted stage, not the card's title). ⚠ **Each surface shuts and stands on the road first**, which was found by seeding the faults: with the village step broken, provisions read **0 doors instead of 6** purely because the card before it was still open in the dialog. ⛔ **AND `VIGNETTES` WAS THE ONE CONTENT TABLE `LINT()` HAD NO BLOCK FOR.** `EVENTS` has block 4 and `CAMPS` has block 10, both of them validating card-level gates at length; the vignettes had nothing. ⛑ **Which is how two dead cards lived in it unnoticed.** `VIGNETTES[8]` and `VIGNETTES[9]` carry `needMut:'gills'` **on the card** rather than on a door, and `MUTATIONS_ON` has been false since #16 parked the system, so **2 of the 10 vignettes have never once been dealt to anybody**. ⚡ **THE OTHER CHECK WRITTEN TO FIND EXACTLY THIS WAS BLIND FOR THE SAME REASON**: `probes/reach.js`'s `deadGates` scanned `(e.choices||[])` and nothing else, so it reported the chapel's one door and missed both cards. It reads the card now, over all three tables, and `CAMPS` and `VIGNETTES` are ARRAYS rather than keyed tables, which is why the scan branches on `Array.isArray` instead of concatenating them. ⛔ **BOTH NEW CHECKS WERE PROVED BY MAKING THEM FIRE.** Four faults seeded into `VIGNETTES` (a body that is not a function, a missing title, `needMut:'wings'`, `fx:{sparkle:3}`) took `LINT()` from **0 to 4** with the right four sentences and back to **0** when they were removed; a card-level `needMut` seeded onto an `EVENTS` and a `CAMPS` card appeared in `reach.js`'s list beside the two real ones; and `enterWorldXX`/`openVillageXX`/`.evcardXX` put four errors into the smoke walk that had been silent. ⚠ **THE LINTER IS NOT ASKED WHETHER A GATE CAN BE PAID, ON PURPOSE.** Three things in the build are gated on gills and the system is parked, so asking there would put three findings into a gate whose whole contract is 0; that question belongs to `reach.js`, which is a list to READ, and it is asked there. ⚡ **AND THE RECORD WAS WRONG IN THREE PLACES, ALL OF THEM COUNTS.** #16's row said *exactly one door in the entire content set* is gated on mutations (it is one door and two whole cards); #224's remainder said the items with no painting are **FOUR** (it is **FIVE**: the disguise cloak arrived with #275 the same way `tmap` and `draught` arrived with #245, and nobody added it); and that same row said `kris` and `stoneshield` are both painted with no `GEAR` row, when **`git log -S` says `stoneshield` has had one since #248, the very entry whose count the row quotes** - stale on the day it was written. ⛑ Neither vignette can simply be un-gated: **both bodies dereference the gilled body**, so they would throw. They stay where they are, for the reason `chapel[1]` stays: they come back with the system. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented `#bField`/`#bLog` on the battle · sheetOverlap `[]` · map 0/0/0 · emdash 0 prose · `regress.js` 8/8 no ERR, no FATAL, no HIT GUARD · `smoke.js` 19 steps 0 throws · `reach.js` re-read. | ⏳ The five unpainted items are still five, and the cloak is now one of them. ⏳ The two vignettes and `chapel[1]` stay dead until #16 is unparked. |
| 8f.275 | **#250 - THE PIT, THE FRONT DOOR SHUTS ONE OF ITS OWN ROWS, AND A MOOD FLOAT STOPS COUNTING.** *(User, 2026-08-26, three asks.)* **⛔ THE PIT IS A PLACE WITH ITS OWN RULE, WHICH IS #61's TEST FOR EARNING A WORD.** (*"I want to add a pit to the surfaces ... if you shove somebody into it, 10 damage ... the enemy needs 1 turn to get out ... you cannot melee an enemy out of or into a pit ... you can shoot at them and cast at them, but they cannot cast back"*.) `pit` is a WALKABLE kind like `ember` and unlike the camp's `fire`, and its five rules are one idea: **THE RIM IS A FLOOR YOU ARE STANDING UNDER.** A body shoved, hooked, dragged or thrown over the edge takes **`PIT_FALL` 10, through any armour**; a body that WALKS in takes nothing, because it climbed down and that is a decision rather than an accident - **the fall is what the shove BUYS, and it is the whole of what makes the hole a tool.** `moveBudget` is **1** while you are in one and `pitClimb` spends whatever the turn had left the moment you step over the rim, which is *"1 turn to get out"* without taking the choice away: you may stay down there and shoot instead. **⛑ AND EVERY REFUSAL IS STATED ONCE, IN `mayAim`**, which is #148's rule and the reason none of this needed a line in either AI brain: `handsOn(a) && acrossRim(u,t)` refuses a swing, a spear, a kick and a hook in both directions, and `a.arcane && inPit(u)` refuses the working. **Shooting is untouched in both directions**, which is his third clause. **⛑ THE RIM IS A DIFFERENCE AND NOT A PLACE, AND THAT IS THE ONE DESIGN CALL IN THE ENTRY.** Two bodies down in the same hole are on one ground and fight normally. His sentence is about the pit against the FIELD and does not consider pit-to-pit; a rule that refused it would need explaining every time two people fell in together, and **pits are allowed to touch (his words), so it is not a corner case** - measured over 40 boards, **6 of the 10 two-pit boards put them adjacent.** **⚠ A PIT DOES NOT HOLD AN OGRE**, which is arithmetic as much as fiction: `sizeOf>=2` is exempt, which matches #46's *"an ogre does not get moved"* (no `push` act in the game can put one in anyway) and means a two-hex body can never be half in and half out - the case `inPit` would otherwise have to answer by reading a tail. **⛔ WHERE THE HOLES COME FROM IS DERIVED TWICE AND AUTHORED ONCE.** `TERRAIN.rocky` carries `pits:2, pitOdds:.65` and **no other ground does**, because the ask named *"a rocky ravine and a lot of stones"*; `pitDue()` is the *"from the middle of the map onward"* half and is **anchored to the SHAPE of the map and never to a node id or a day count** - `armourWhere`'s rule and #160's, which #71 proved by moving the Muster Field and nearly firing the Thing in Armour on top of the Fen-Mother. `stepsToGoal()` counts roads backwards from the bells, so **`hold` is 20 out and the back half is 10 or fewer**, i.e. the Clan Wedding onward. **⛑ The dial and the gate were picked against each other and nothing had to be excluded by hand**: the three fights fought on `rocky` sit at 8, 4 and wherever `armourWhere` lands, and every fight in the front half is fought on `fen`, `forest`, `camp` or `swamp`. `digPits` is `lightEmbers`'s shape with `lightEmbers`'s three guards (never in a deployment band, never on a reserved hex, **the odds roll taken even when it fails** so a ridge with no hole consumes exactly one number) and one deliberate difference: **two pits may touch, where two ember patches may not.** Measured over 40 boards: **16 with none, 14 with one, 10 with two.** **⛑ AND THE ONE THING ONLY DRIVING IT FOUND: A FOE IN A PIT WAS NOT LIT AND ITS HIT ODDS WERE PRINTED OVER ITS HEAD ANYWAY.** `81%` on a swing `clickHex` would have refused. The ring comes off `offerAtk` (which asks `mayAim`, so it was already correct) and the FIGURE comes off a hand-copied branch inside `render` that repeats `mayAim`'s size gate and its oak - **#148's four-copies warning arriving for the fifth time.** The two clauses are repeated there with the note saying so; the honest fix is for that branch to ask `mayAim`, and it is not one line, because `mayAim` refuses a BLOCKED lane outright while that site deliberately draws one as refused-red. **⛑ `1 HEXES`.** Reachable since crippled-in-the-marsh and never once seen, because nothing put a body on a budget of one often enough. A pit does, on every climb. **⚠ AND THE PAINT WAS A PUDDLE UNTIL IT WAS PHOTOGRAPHED.** The hex layer washes teal over the ground canvas, so a blue-black mouth composites to exactly the colour `marsh` and `deep` wear. Warm black, a lit crescent for the far wall rather than a hairline (an outline reads as a pond), and a brighter spoil ring, which is the only part of a hole that is ABOVE the surface. **⛔ THE CAPTAIN SAYS IT BEFORE THE GROUND DOES ANYTHING**, which is what the LEARN tier is for (#64's split) and what he asked for in the same breath: `learn_pit` rides `B.hasPits` in `learnTurn`, beside `learn_bloom`, and the badge is `STATUS.inpit` - **computed like `chilled` and `nearring`, because it is a fact about the ground and not about the body.** **⛔ THE PRACTICE FIELD IS SHUT UNTIL THE ROAD HAS BEEN WALKED.** (*"disable 'practive fight' before person tried 'the road' (icon locked)"*.) The front door offered a menu of thirteen fights beside the button that starts the game, to somebody who has not seen one of them, which is the 2026-08-10 report (*"too many options/systems from the start"*) on the very first screen. **⛑ DERIVED FROM THREE FACTS THE SAVE ALREADY CARRIES AND NEVER A FOURTH FLAG** (`LEGACY.brawlDone`, `LEGACY.runs`, `hasSavedRun()`): a new `roadTaken` would have read FALSE for every player who has already played, i.e. it would have locked the screen for the ten people whose feedback this build is written from. `TEST.on` opens it, because it is also the developer's bench. **LOCKED AND NOT HIDDEN** - a missing row teaches nothing and comes back one day as a surprise. ⚠ **A `disabled` button shows no `title`**, so the reason is ON the row: measured 346px of text in 438 of inner width, 0 overflow, click does nothing, and it comes back the moment any of the three flags does. **⛔ AND A MOOD FLOAT STOPS COUNTING.** (*"Moral boost - one medium triangle. Or one big. All (now there are 1, 2, 3 triangles, and some circle under them. Simplify). One symbol slightly different sizes - and the direction is clear."*) The arc and the repeated glyph were both a NUMBER WEARING A PICTURE: the player had to read three shapes to learn one figure. One triangle says the direction and its size says how much. The three thresholds are the ones `moodFace` always used (22 / 12 / rest); what changed is that they pick a size. **Measured 10 / 13 / 17px, boxes equal to the type (`line-height:1`), all under `FX_ROW`'s 18** - `--fs7` at 20 was the first pick and is arithmetically closed, because a box over 17 eats the next line's row. `--fs1` is the floor and 10 sits on it. ⚠ `fx()`'s fourth argument takes a class name as well as `true` now, and every existing caller is untouched. **Measured, n=20 a side on the Steading-Line** (the fight that gets the most holes), the two arms differing only in `pitDue`: **12.05 rounds against 11.20, won 6/20 against 5/20, 4.55 down against 4.50.** So the hazard costs the AI about four fifths of a round of walking round it and changes nothing about who wins - **which is the right first-cut result, because only the player currently AIMS a shove at one.** **Gates:** `LINT()` **0**, and 8i proved by making all four of its branches fire and returning to silence · floor `[]` on both screens · clip the documented set · `iChar` overlap `[]` · map 0/0/0 · no em dash in `EVENTS` · the three scale greps 0 / `86px` / 0 · smoke 0 throws over 40 road cards, 19 camps and 59 gear hovers · the eight arena fights clean · **and the three rocky fights run again WITH holes in them, n=8 each, both brains at FULL: 0 guards, 0 errors** (`regress.js` cannot reach this - it runs from `hold`, where `pitDue` is false by design) · `gt.py check` 3 scripts 0 problems. | ✅ DONE |
| 8f.276 | **#251 - THE SEVEN SMALL FIXES: THE CONTRACT STOPS RECEIPTING ITS OWN SENTENCE, THE SHEET STOPS BEING THE SMALL VERSION OF THE BATTLE CARD, THE DOLL BECOMES A BODY AGAIN, AND THE UNDO WINDOW STOPS BEING A WAIT.** *(User, 2026-08-26, three numbered items over two screenshots.)* **⛔ THE CONTRACT CARD'S TWO CHIPS ARE THE RECEIPT RULE CATCHING ITSELF.** (*"Delete heads and crowns"*, both struck through in red.) `.claude/skills/event-cards/SKILL.md` opens with *the prose says what happened, the chips say what it cost, and they must never say the same thing* - and on every other card in the game that split is real, because the prose has had its figures cut out of it. **This card is the one place where the two halves are the SAME event, read a second apart**: the paragraph directly above the chips is *"He puts a purse on the wood"* and *"It appears you are a company now"*, and the chips under it said **+88 CROWNS** and **+3 HEADS**. That is the second receipt this file spends its length deleting, arriving from the one direction nobody had checked - not a number typed twice, but a number standing beside the sentence that already is it. ⛑ **Nothing is lost, which is the test**: the purse is `CONTRACT_PURSE` and the door pays it, and the world bar the very next screen opens on carries the crowns and the headcount as live readouts. ⚠ **The three faces stay** - `.tvrwho` is not a receipt, it is three people with names, which is the card's last sentence as a picture. The divider went with the chips because a rule between one thing and nothing is furniture. ⚡ **And it paid the card 56 of its 99 overflowed pixels back**: the receipt strip measured 82.6px and now measures 28.9, so the flow scrolls **43 instead of 99** and the top-edge fade `#219` built for it fires over that much less. **⛑ AND THE ONE DOOR GAINS THE SHOE BESIDE ITS SCALES.** (*"put shoe on the go"*.) The label is two acts in one sentence - you take the purse AND you walk out of the tap-room - and it wore only the first. `['trade','leave']` is two VERBS and is legal by precedent this repo has already paid for: THE WARM SPRING's second door is `['take','leave']`, and it is the door that broke #218's first cut of the rail cascade. ⚠ **The rail does not move and that is derived, not lucky**: `leave` is the one intent with NO rail row (the quietest door has no colour), so the coin-yellow the door had as a `trade` door is what it keeps - measured `rgb(201,162,39)` both ways. **⛔ THE SHEET'S HEADER MARK LINE GOES, AND EVERY FACT ON IT IS ON THE TILE 130px TO ITS LEFT.** (*"Delete that smaller line near human face"*.) #246 put the level on the roster portrait's bottom-right corner and #249 put the class mark on its bottom-left; the race is the `.race` word on the same row AND the face in the picture; and the arc round the bust is `xpFrame`, the same reading `xpRing` was drawing in the header. ⚠ **One fact did not survive the move on its own and was handed over rather than lost**: the EXACT xp figures were on that chip's `title` and the roster's arc had no hover at all, so `xpHover` rides the roster's own level plate now. ⚠ **AND IT COSTS THE COLUMN NOTHING, WHICH IS WORTH KNOWING BEFORE SOMEBODY COMES HERE FOR PIXELS**: `#iHead` is **90px on every body in the game** because `#iField`'s portrait tile is what sets it, and the text column beside it measured 71 to 86.6. The 26px row came out of slack. ⚠ `SHEET_HEAD_MARKS=true` puts it back, which is #240's switch written the same way for the same reason - and the `#iChips`/`.ichip` stylesheet block is left standing for it. **⛔ THE THREE POOLS TAKE THE BATTLE CARD'S OWN TWO NUMBERS, AND THE WORST BODY PAYS NOTHING.** (*"design better this bar with health, more like in the battle"*.) #247 gave the sheet 16/`--fs3` against the card's 20/`--fs5` on the argument that *"the card is read mid-fight at arm's length and the sheet is read at a desk"*. Reasonable, and overruled: two surfaces draw the same three pools off one `.sbar` block, and the one the player STUDIES was the one drawn smallest. ⛑ **It could be granted because `#iStatTop` is a flex ROW and its height is the taller of two columns.** Measured over all 16 body x state cases: the pools stack to **127** against the four tells' **124** on a clean crew member, so +12 costs 12 out of 107px of slack - and to **141** the moment a level is banked, which is the state that owns the worst slack in the game. There the tells are still taller at 141 against 139, so **the worst case pays 0**. Worst slack **38 -> 29**, and every one of the 16 cases still `scroll 0`, `overlap []`. ⚠ **20/`--fs5` is the card's pair and `ui-scales.md` §3c records it as *one pixel over*, which the clip counter ignores by design.** Copying the card means copying that: 21 would be clean and would make two surfaces disagree about a shared block over a pixel nobody can see. **⛔ THE DOLL IS A BODY AGAIN, WITHOUT THE BODY.** (*"Make artifacts shown as on real body (armor in the middle a bit up) - hands on the sides. Two spots for trinket and bag centered under"*.) #249 parked the drawn figure and the six slots became a READING ORDER - *worn, then wielded, then carried* - which is a correct thing for a list to be and is not what a doll is. Now: the chest in the middle and raised, a hand either side of it, and what is merely carried centred underneath. **The arrangement IS the figure.** ⛔ **Two flex rows and not a grid, and the PET is the reason**: the Captain has a sixth slot and nobody else does, so the bottom row is two boxes on fifteen sheets and three on his - a grid would need its placement re-authored per count, where `justify-content:center` over a fixed basis is one rule that is right at both. The basis is the top row's own third, so the two rows line up (measured: trinket 192.7 and bag 380 inside 99..655, dead centre). ⛑ **`align-items:flex-end` is what makes the lift a lift**: with the row aligned to its foot, a bottom margin on the armour raises that box and nothing else, and the row grows by exactly the margin - **109 -> 118**. A negative top margin would have been free and would have hung the chest out of the panel it is drawn inside. ⚠ **The row a slot belongs in is DERIVED**, from a two-field table keyed on the slot, and `SLOTS` is not re-sorted: a seventh slot declares itself in one row and nowhere else. `order` rather than a DOM shuffle, because *armour is authored first and stands second* is a special case as an insertion rule and a number beside the slot's name as a flex order. **⛔ THE STASH READS IN TWO, AND THE ROW WAS NEVER THE WIDTH.** (*"items in the inventory in two raws - there is too much free space"*.) A stash row is a 40px painting beside three short lines, the longest of them a mono receipt like `COMMON · ARMOUR · stops 12`; #246 widened this pane to 606 for the PERK TREE, which genuinely wants it. So the widest thing in the list has been sitting in about half the box ever since. Rows go **586 -> 290.5** and stand two up. ⛔ **`#iStashBody.on` and never `#iStashBody` bare**, which is one of this file's oldest traps in a new coat: `.itabbody{display:none}` is 0-1-0 and an id is 1-0-0, so a rule without the class would have shown the stash on the PERKS tab as well, permanently, with nothing in the stylesheet looking wrong. ⚠ **`align-content:start` is not decoration either** - a grid's auto rows stretch to fill their container, and this container is a 639px scroller, so a stash holding one thing would have drawn that thing 639px tall. The caption, the empty line and the BREAK door span both columns; a door in one of two columns reads as a third item. **⛔ AND THE UNDO WINDOW: THE REPORT AS WRITTEN WAS NOT REPRODUCIBLE, AND DRIVING IT IS WHAT FOUND THE REAL FAULT.** (*"When undo - it is 'undo with timer' - but you can make a move with your next unit already"*.) **You cannot act with another body while the bar runs.** `cur()` is still the mover, `clickHex` refuses anybody else, and the order rail carries no click at all - measured on the brigand field at 0.06s and 0.96s (the mover is up), with a click on Two Teeth leaving it on its own hex with both actions, and at 3.56s the turn has passed and the button is gone. ⛔ **What was actually wrong is that the click was EATEN.** A player who has decided not to take the step back does what anybody does: clicks the next body. That click fell through every branch of `clickHex` and did nothing - so the board answered a deliberate act with silence and then made him watch 3.15 seconds of bar. **A control that swallows input while showing a countdown reads as a hang**, which is the same complaint #249 was answering and it fixed only the half where the player pressed END TURN. ⛑ **So the window is a MAXIMUM and never a pause**: `undoGo` does now what the timer was going to do anyway. ⛔ **The turn is still not rewound** - #247's paragraph holds and this does not go near it: the handover happens exactly once, at a moment the player chose, and `B.undo` is dead BEFORE `beginTurn` runs rather than after it. **The reading that would have kept ⟲ alive across the handover was put to the user and is not what he asked for** (*"stop making me wait"*). ⚠ **It is only ever the emptied body**: a body with actions left carries an undo with NO hold, and a click on the board is that body's own next move - `u.actions<=0` is what tells them apart, and it is the same test `autoEnd` opened the window on. ⚡ **AND IT CAUGHT A SHIPPED DEFECT NOBODY HAD REPORTED: THE DRAIN BAR OUTLIVED ITS OWN WINDOW.** `.draining` was added by `undoDrain` and removed only by `undoMove`, so it survived every handover on the hidden button - and the NEXT offer, which is the untimed kind a body with an action left puts up, opened wearing a countdown that had already run out. **A timer drawn on an offer that has no timer is #102's wrong-unit rule with a clock in it.** Stripped whenever the offer is not up. ⚑ **AND A MEASUREMENT THAT WAS WRONG TWICE, WHICH IS THE PART OF THIS ENTRY WORTH KEEPING.** The first floor reading on the opening screen came back with **20 elements under the 10px floor** and `#prDlg` carrying `scale(0.965)`. There is no such rule in the stylesheet: the card was still 96.5% of the way through **`evCardIn`**, the 0.18s pop-in #218 built, so every `--fs1` label on it was rendering at 9.65. **The obvious correction was to wait, and waiting 800ms produced a SECOND artefact that looked exactly like a real finding**: one element left, `.evname`, the cast plate's name, at 10px inside a `.evcast` reading `scale(0.97)` - i.e. *9.7px on every card in the game*, which was written into three documents before it was re-run. **`.evcast` has no authored scale either. That 0.97 is `evColIn`, the column's own delayed entry, and at 900ms it is 1.000 and the floor is `[]` on both decks.** ⛔ **So #218's rule needed applying twice: *a card is captured by PAUSING the timeline, not by racing it* is true of a PROBE as much as of a screenshot, and a second reading that merely waited LONGER is not the same thing as pausing.** Driven settled on both builds at party 4: floor `[]`, `.evname` **10px x 1.000**, on HEAD and on this one alike. ⛑ **`probes/cardfloor.js` is that reading, kept**, because `gates.js` has never opened a card and therefore has never run either §5 counter over the stage at all - which is a real hole whatever this particular non-finding turned out to be. **Gates:** `LINT()` **0** · floor `[]` on the sheet and the battle · clip `[]` on the sheet and the documented `#bField`/`#bTrait`/`#bLog` in the battle · `iChar` overlap `[]` on all 16 body x state cases · map 0/0/0 · no em dash in `EVENTS`, and `emdash.py` prose 0 · the three scale greps **0 / `86px` / 0** · smoke 0 throws over 40 road cards, 19 camps, 59 gear hovers and every body sheet · the eight arena fights clean · `gt.py check` 3 scripts 0 problems. **Eye check:** `shots/251_sheet.png`, `shots/251_contract.png`, `shots/251_door.png`. | ✅ DONE |
| 8f.277 | **#252 - A STAT IS A SUM, AND BOTH SURFACES THAT PRINT ONE NOW SHOW THE SUM.** *(User, 2026-08-26/27: the ladder doc reworked so *“each of my stats is computed dynamically from the stats stated here and the race, and not effectively hardcoded”*, then *“i want to see when i hover stats in inventory - how it was calculated”*, *“Things should be connected, not just hardcoded stats”* and *“and of course, build it in the main build of the game”*.)* **⛔ THE HOVER READS THE SAME WALK THE SHEET DOES, AND THAT IS THE WHOLE DESIGN.** `effStats` walked the gear, the trinket, the personality, the scars, the temporary injury, the wrong-size harness and the heavy-only clause adding into one number; a hover that re-walked them would be a SECOND implementation of one sum - the shape this build has merged out four times (`choiceNote` #150, `raceDoorPick` #159, the mood echo #176, `applyHurt` #197) and every one of them had drifted by the time somebody looked. **`statParts` IS the walk now and `effStats` is its total.** A new modifier is one `add()` call and it appears on the hover for free. ⛑ **Proved a REFACTOR rather than a rewrite by an independent oracle**: HEAD's walk re-typed inside the probe and run against the new one over **244 bodies** (the company plus 240 rolled across all three races, every trait, every armour/main/trinket, one and two scars, a temporary injury and banked levels) - **0 drift on all four stats, and 0 ledgers that do not add to the figure they print.** ⚠ **The residual is `p.st` minus the race minus the levels and it is called `born` because that is what is left**: `finishRecruit` folds `RACEMOD` in once and a promotion banks into the same field, so `p.lvUp` counts the banked ones from here on and a body promoted before this update reads its levels as part of `born` - a line that is less specific rather than one that is wrong. ⛑ **The floor is a line of its own**: `Math.max(1,…)` can BE the reason a number is what it is (a ratkin in ogre plate carrying two scars adds to less than 1), and a breakdown whose parts do not reach its total is worse than no breakdown. **⛔ AND THE LEDGER GOES BESIDE THE LADDER, WHICH IS A HEIGHT DECISION AND NOT A TASTE ONE.** Under it the tip went **455 → 519** on a clean body and **564** on a scarred one, and `moveTip` clamps against `innerHeight` rather than against `#stage` - so INT and MOR hung off the bottom of the board. ⚡ **HEAD already did it on MOR by 3px and nothing had ever measured it**; two columns fixed the new fault and the shipped one together: **440x440 on all four stats, inside the stage on all four, 0 rungs wrapped, 0 clipped, 0 under the floor - on the worst ledger the game can build.** ⚠ **`max-width` had to leave the inline `TIP.style.cssText` for that**, because an inline declaration outranks every stylesheet rule: it is `#gtTip{max-width:290px}` in the sheet now and one `#gtTip:has(.statwrap)` raises it for the one layout that needs it - the `#wDlg:has(.evstage)` shape, and it cannot go stale because every hover writes `TIP.innerHTML=` from scratch. ⚠ A source keeps its own case (`Experienced`, `Rat-bite fever`), because the header two inches above the box already spells them that way. **⛔ AND THE LADDER DOC STOPPED CARRYING TYPED NUMBERS.** `shots/204_ladders.html` is regenerated: one `G`/`RACE`/`CLASS`/`TRAIT`/`WEAP`/`LAD` model, and the givens, the race table, **every rung's own words**, the two *held at the end* lines and the whole company table are RENDERED from it. The only thing authored on a body is its four lines - **born, race, class, trait** - and its weapon; **born is 0 unless a backstory has paid for it, and today only the Captain's has.** ⚡ **Deriving it caught three numbers the typed table had been carrying for two rounds**: Ash's **to hit 80** is not reachable from AGI +2 (it is 74, and Skree at +3 is the 78 that was probably being read), the INT ladder's *held at the top* said **magic +30%** where the rungs sum to **+15%**, and the race column was in three columns while the note said four. ⚡ **And the company moved where the four lines had never actually been applied**: Vesna takes the spear class's STR lean (60 → 66), Marrow and Ilka lose the invented −1/−1 (51 → 54, dodge 11 → 14), Skree and Ash take the ratkin's own −1 STR rather than −3 (38 → 43, swing 10-14 → 12-16) and Bruht's AGI is the race's −1 plus Big's −1 rather than −3 (dodge 7 → 11). ⚠ The doc also lost its history at his word (*“this doc has historical junk”*): the was-56/was-68 comparisons, *the bottom is softer than it was*, *at your word*, the note that MORALE and AGILITY have no drawn set, and the questions posed back at him. ⛔ **And no digit is added beside a stat** (*“Число біля стата не треба додавати”*): the sheet prints one WORD and the hover carries the ladder and now the sum, which is the surface that already existed. **Gates:** `LINT()` **0** · floor `[]` on the sheet and the battle · clip `[]` on the sheet and the documented `#bField`/`#bTrait`/`#bLog` in the battle · `iChar` overlap `[]` · map 0/0/0 · smoke **0 throws** over 40 road cards, 19 camps, 59 gear hovers and every body sheet · `gt.py check` 3 scripts 0 problems · the doc page itself: 36 rungs, 18 arms, 0 broken, 0 under the floor, 0 clipped, 0 errors, and **8 sanity checks over the model** (nine rungs a ladder, exactly one capstone and it is +4, no silent rung, no rung paying the wrong way for its sign, no body off the ladder, the race pool derived from the given, no crossed swing, and `held()` monotone). **Probes:** `probes/statsum252.js` (the oracle) and `probes/statsum252b.js` (the worst tip). | ✅ DONE |
| 8f.278 | **#253 - THE ORDINARY HUMAN IS 0 ON EVERY AXIS, AND THE HOVER SAYS WHAT EACH RUNG IS WORTH.** *(User, 2026-08-27, an arrow at `born +8` in #252's own ledger: *“Born value = 0. It starts at the middle of the bar”*, *“The idea of this stats simple - human in the middle - and then class and artifacts and your choices shifts it”*, *“Bonus you write here - on the left and total bonus from stat on the right”*, *“Why the fk you deleted race and class from there - add them back”*, *“sheets should show info similar as in the file”*.)* **⛑ IT IS AN ORIGIN SHIFT AND NOT A NEW SCALE, WHICH IS THE ONLY REASON IT FITS IN ONE ENTRY.** `p.st.str` was 1..20 with the rolled human at 8; it is a signed rung now with the rolled human at **0**. ⛔ **Every modifier in the game is a DELTA and a delta is invariant under a shift** - `RACEMOD`, a trait's `str:1`, a scar's `agi:−1`, a hurt's `mor:−2`, a trinket's `bonus:{int:2}`, a banked level - so not one of those tables changed. What moved is the **32 sites that name an absolute**: eleven authored bodies, the recruit roll, the nine formulas in `D`, `init`, `arcSkill`, `thews`, the muster price and the 36 `TELLS` thresholds. ⚡ **`thews()` had already written the 8 down** (`(s-8)*0.055`), which is the build agreeing that this is where the middle always was. **⛑ SO THE BALANCE IS PROVABLY UNCHANGED, AND IT WAS PROVED RATHER THAN CLAIMED**: `probes/statzero253.js` prints every derived number on a fixed set of bodies - `D.hp/dmg/hit/dodge/flank/spell/moral/bonus/learn`, `init`, `thews` at both ranges, `bodyHp`, the word each stat is CALLED by, its icon index, and six seeded muster prices - and the two builds are **byte-identical on every line**, with only `st` itself moved by exactly 8. The eight arena fights run clean, smoke is 0 throws. **⛔ AND WHAT A RUNG IS WORTH IS ASKED OF THE ENGINE, NEVER TYPED.** `statGive` runs the SAME functions the fight calls on a stub body at rung `r` and on the ordinary human at 0, and subtracts: STRONG reads *+7 hitpoints · +11% melee*, and the day `D.hp` is retuned that text is retuned with it. ⛑ **`D.arc` is a new derived reader** - what it takes to land a working was an inline expression at the one site that used it, and a second reader with its own copy is the shape this build has merged out five times. ⚡ **THE LADDER TAKES THE FIRST TWO EFFECTS AND THE TOTAL COLUMN TAKES ALL OF THEM, AND THAT IS A MEASUREMENT**: the full line under nine rungs took the tip from 440px to **771** and off the bottom of the board. Inline on the rung's own row it is **555-580 x 424-440, inside the stage on all four stats, 0 wrapped, 0 clipped, 0 under the floor** on the worst body the game can build. **⚡ AND THE RACE AND THE CLASS COME BACK TO THE SHEET HEADER, WHICH OVERRULES HALF OF #251.** That entry's argument was that every fact on the row is on the roster tile 130px to its left; for the LEVEL that is true and it stays there, and for these two it is not - the tile carries them as a 12px corner mark and a face, which sorts four tiles at a glance and is not the same thing as NAMING what you are looking at on the row you are already reading. That is the mark-versus-word rule in `.claude/skills/event-cards/SKILL.md`, read the wrong way round. **⚠ The xp ring does not come back with them**, because `xpHover` moved to the roster plate and it would be the one fact said twice. ⚠ **A save written before this carries 1..20 stats**; `finishRecruit` stamps `p.statZero` and a body without it is shifted once on load. ⚡ **The `shots/204_ladders.html` decision page is archived** to `docs/archive/204_ladders_page.html` at his word (*“lets stop working with ladder file - you can delete it (or archive). Focus on main”*) - the ladder rework it specs is still parked #204, and what shipped here is the build's OWN scale re-based, not that design. **Gates:** `LINT()` **0** · floor `[]` on the sheet and the battle · clip `[]` on the sheet and the documented set in battle · `iChar` overlap `[]` · map 0/0/0 · `statsum252.js` 244 bodies 0 drift 0 non-summing ledgers · `statzero253.js` identical to HEAD · the eight arena fights clean · smoke 0 throws · `gt.py check` 3 scripts 0 problems. | ✅ DONE |
| 8f.279 | **#254 - THE FOUR LADDERS GO IN. THE DECISION PAGE IS THE ENGINE.** *(User, 2026-08-27: *“Where is special things from the file, that i developed? Do a ladder from there with that bonuses, instead of your current flat and boring ones. Take the number and meanings from the file!!!!”*, *“те, що було тут ... Це от база повинна бути!”*, *“add a hover to chance to hit and to dodge”*, *“quality for a race and class to race and class hover”*, *“Don’t be lazy, do new system release!”*)* **⛔ #253 SHIPPED A HOVER WHOSE FIGURES WERE THE ENGINE'S OWN LINEAR FORMULAS DIFFERENCED AGAINST RUNG 0 - HONEST, CONNECTED, AND HE IS RIGHT THAT IT SAID NOTHING.** A straight line has nothing to say: every rung of it pays the same thing in the same direction, so the ladder was nine rows of the same sentence with the numbers going up. **`STATLADDER` is the design and the engine at once.** Nine authored rungs a stat, out of his own page (now `docs/archive/204_ladders_page.html`): they ALTERNATE what they pay (STRENGTH is hitpoints, then melee, then hitpoints, then melee), they hold a capstone behind a SECOND point, and three of the four end in a RULE rather than a number. `D` reads the table, the hover prints the table, and there is no second copy of either. **⛑ A RUNG IS A STEP AND `held()` IS THE CLIMB** - every effect is the sum of the rungs from 1 up to n, precomputed into `HELD` at boot because `dmgMul` asks on every blow - **and a rung outside -4..+4 is CLAMPED**, because a ladder with no top is not a ladder. **THE GIVENS ARE HIS**: hitpoints 60, to hit 70, dodge 14, to land 69, mood 90, the opening a flat 15, a bow or a casting trade at 90% of the pool. **The race carries the body** - the pool (60/45/75, derived from one multiplier), the weight of the blow (±10%) and how easy it is to hit - and ⛔ **THAT LAST ONE MOVED OFF THE DEFENDER'S DODGE AND ONTO THE ATTACKER'S ROLL**, where it belongs: a small body is hard to hit because it is SMALL and not because it is nimble, and saying it in the dodge column was one arithmetic printed as a claim about the other. `SIZEHIT` stays underneath for everything with a size and no race. **⛔ AND THE TRADE LEANS A RUNG, WHICH IS THE LINE THE BUILD NEVER HAD.** *born + race + class + trait* is the whole answer to *“why is this one stronger”*, and until now the class answered it with a weapon and nothing else. `CLSLEAN` is one rung each; the race's lean came DOWN to one rung to match (it was ±2 and drowned the other three lines); born is **0 unless a backstory paid for it**, and today only the Captain's has. **⛑ THE THREE CAPSTONES THAT REACH THE FIELD ARE CARRIED ONTO THE UNIT**: STANDS, FLEET (+1 step, and the sheet's STEP row reads `u.speed` rather than the race table it used to name) and UNREPEATABLE, all off `holds()`, so the day a capstone moves rung nothing at the call sites changes. **⛔ TWO STATS STOPPED PAYING THROUGH A SECOND CHANNEL.** `round(0.3×STR)` is a flat 2 for everybody now - the file's *“+2 baked into every weapon row”* without editing a hundred dice rows - so STRENGTH reaches damage through the ladder and nowhere else; and `D.bonus` is 0, with the NERVE RUNG carrying what the MORALE stat used to (STEADY +5%, IT ROCKS +10%), because what a body is FEELING is a state and not a stat. **A spell is no longer dodged** at the one site that charged `dodgeOf(d)×0.35` against an arcane act. **⚡ AND THE PRICE, MEASURED RATHER THAN GUESSED**: `ARENA.match('prepared',f,15)` on seven fights, before and after. Six are inside the noise; **the brigand road went 13/15 in 10.8 rounds to 15/15 in 6.2**, which is the raised to-hit baseline resolving grinds it used to lose. The two set-pieces did not move (steading 6/15 → 5/15, snare 11/15 → 11/15). **⚡ THREE READOUTS**: TO HIT and DODGE explain themselves off the functions that COMPUTE them - `dodgeOf(u,W)` has taken a writer since #146 and nothing had ever asked for one, and `unitFrom` keeps `u.hitWhy` beside the `+=` that builds it - with **the cap as a line of its own**, because a body whose parts add to more than it prints has to be told why; the race hover names its pool, its weight of blow and what an attacker adds; and the trade hover names its lean. **⚡ AND K2 WAS ANSWERED BY THE LADDER ARRIVING**: MORALE's bands stepped by 2 while the other three stepped by 1, so the ordinary human read *Nervous*; a band that is not a rung cannot name a rung, and all four are the nine rungs now. **⚠ SHIPPED FOR FOUR MINUTES AND CAUGHT BY THE PROBE**: `finishRecruit` used `STATK`, a `const` declared ~330 lines BELOW it, and `makeParty()` runs at boot - the temporal-dead-zone throw that aborts the whole script, which `.claude/skills/world-map-sights/SKILL.md` says this project has shipped once. The four keys are a literal there now. **Gates:** `LINT()` **0** · floor `[]` · clip `[]` on the sheet and the documented set in battle · overlap `[]` · map 0/0/0 · every one of the eight hovers driven and measured (`probes/lad254b.js`: inside the stage, 0 clipped, 0 under the floor, **every ledger adds to the figure it prints**) · 244-body `effStats` oracle 0 drift · smoke 0 throws · the eight arena fights clean · `gt.py check` 3 scripts 0 problems. | ✅ DONE |
| 8f.280 | **#255 - SIX ASKS, AND THE TWO THINGS THEY UNCOVERED WERE BIGGER THAN THE SIX.** *(User, 2026-08-27, after playing #254.)* **⛔ (1) *“Does morale actually work? somehow marrow mood quite close to others”* WAS A MEASUREMENT AND HE WAS RIGHT.** Morale worked; the LADDER did not. Nerve sat on the ±1 rungs ALONE, so nine grades collapsed into three numbers - **80 / 90 / 100 across the whole ladder** - and the other six rungs paid in recovery, loss and mending, none of which shows on the bar the player is reading. ±10 a rung spreads it to **50 to 130** and every rung keeps what it had. ⚠ **This is the one figure of his that I changed** and it is filed as M1. **⛔ (2 + 6) HOW BIG A BODY IS BELONGS TO ITS RACE.** *“Big for ogres and small for ratkins - it is something connected to their race, rather then personal trait”* and *“for ratkin you can also add small in their race ... and lets that size also influence phisical damage also”*. `RACESIZE` is one row with four consequences - the pool multiplier, the weight of the blow, how easy the body is to hit, and the bulk - and every one of them was already in the build, scattered across four tables. ⚡ **`big` was a PERSONALITY any human could roll**, which made *how big you are* a coin toss the portrait did not agree with; its +16 is the ogre's `bulk` now (added AFTER the strength climb, because meat is not multiplied by how strong you are - Bruht is 99 either way), and the row is retired rather than deleted so a save carrying the key still resolves to a name. The ratkin is **SMALL** and the hover says so beside its 45 hitpoints and its −10% melee. **(3)** *to land a working* → **to land a skill or magic**, and *reach on workings and skills* → *on skills and magic*. **(4)** `PARTYBASE` **11 → 12**. **⛔ (5) BORN IS NOT ROLLED AT ALL.** *“don’t calculate this born - it is useless”*. A random ±1 on four axes is noise with a name on it: it is the one line of the four that explains nothing, and it put a `born −1` row on a hover whose whole job is to say WHY. Authored born survives (the Captain's backstory paid for +1 STR and +1 AGI); what is gone is the dice, and the row is drawn only when something paid for it. **⛑ AND THE TWO FINDINGS, BOTH CAUGHT BY DRIVING RATHER THAN READING.** ⛔ **EVERY TABLE THAT MODIFIES A STAT WAS STILL ON THE OLD 1..20 SCALE.** #253 shifted the ORIGIN and left the deltas alone, which is correct for a delta and catastrophic for a delta that big: **`vegetarian mor:6` was six of the nine rungs**, `frightening mor:5`, `stubborn mor:−5`, `deadboots mor:−6`, and four HURTS at −2. **24 modifiers rescaled** by one rule - `sign × max(1, round(|v|/3))`, capped at 2 - so a piece of gear or a personality may LEAN a body and never re-author it. The four named lines are one rung each and the whole ladder is nine. ⛔ **AND #254's CLASS LEAN HAD NEVER REACHED A SINGLE BODY THE PLAYER HIRED.** A muster hire is `G.party.push(r)` straight off `rollRecruit` and has never passed through `finishRecruit` at all, so the trade's rung reached the four founders and the three presets and nobody else. `applyLeans` is the one function and both callers use it - **which is the same *two implementations of one fact* this build has now merged out six times**, found the day after it was written. ⚡ **The arena, re-priced after all of it:** seven fights at n=8, against #254's n=15: clash 8/8 in 4.1 rounds, brigand 7/8 in 5.5, pack 8/8 in 5.4, slingline 8/8 in 5.3, steading 2/8 in 10.6, snare 7/8 in 8.4, armour 8/8 in 5.1. Every fight is in the band it was already in and the rounds are the same or SHORTER, so the mood ladder's wider pool did not make fights drag: the steading is still the hard one and the snare is the only one that moved. ⚠ **AND ONE SCARE THAT WAS NOT REAL, WHICH IS THE PART WORTH KEEPING.** Three readings in a row said the arena had gone from seconds to minutes, and every one of them was taken either on a browser still holding a KILLED arena run - the `render`-stub trap this repo already documents - or through a probe that stepped turns in a blocking loop, and the PRE-#255 build hangs on that same probe identically. `regress.js` on a clean browser runs all eight fights in under 95s and did so on every build in this session. **A measurement taken on a poisoned instrument is not a finding, and it was reported as one before it was checked.** **Gates:** `LINT()` **0** (and `never` joins `foeOnly`/`authored` as a sanctioned exception, because a retired row is deliberately unrollable and the linter saying so was the linter working) · floor `[]` · clip `[]` on the sheet and the documented set in battle · overlap `[]` · map 0/0/0 · all eight hovers driven: inside the stage, 0 clipped, 0 under the floor, every ledger adding up · ⚠ the MORALE tip needed 60px of width and two words out of its longest line, because ±10 a rung wrapped every row and took it 443 → 458 and off the bottom of the board · 244-body oracle 0 drift · smoke 0 throws · `gt.py check` 3 scripts 0 problems. | ✅ DONE |
| 8f.281 | **#256 - THE THREE THINGS #253 AND #254 LEFT PRINTING AND NOT PAYING.** *(User, 2026-08-27: *“have you fixed 253 and 254? if not - fix”*.)* **⛔ K1: ONE WEAPON, THREE DAMAGE RANGES, IN TWO DIFFERENT UNITS, LABELLED IN NONE OF THEM.** The ACTIONS line printed what the swing LANDS for, the SKILLS card and the battle’s own card printed the weapon’s raw DICE, and the two sat an inch apart under the same name. ⛑ **The arithmetic was never wrong** - the slot’s hover states the whole chain and every figure in it is derived - which is why this was a labelling fault and not a sum. `landsFor` is the one function and all three read it, so the sheet and the board cannot say different numbers about one act; the gear slot keeps the weapon’s own dice, which is what a gear slot is for, and the ACTIONS line says **lands** so the pair is named. **⛔ L2a: INTELLECT +3 PROMISED `+1 reach on skills and magic` AND PAID NOTHING** since #254. It lands on both halves the rung names - `spellRange` and the weapon’s own act - and it is guarded on the SUM rather than the rung, because a body with no working has no range to add to. Measured: INT 0 and +2 unchanged, **INT +3 takes spellRange 2 → 3 and the stave’s reach 2 → 3**. **⛔ L2b: MORALE −4 PROMISED *starts the fight one rung lower* AND PAID NOTHING.** ⛑ It is expressed as the THRESHOLD of the next rung down and never as a subtraction: a flat −20 is two rungs on a small pool and none on a big one. **`NERVE_RUNGS` is a table now** because you cannot index a ternary, and the cut-offs are unchanged. Measured: MOR 0 and −3 open **steady**, MOR −4 opens **shaken**. **Gates:** `LINT()` 0 · floor `[]` · clip `[]` on the sheet and the documented set in battle · overlap `[]` · map 0/0/0 · eight hovers driven, every ledger adding up · 244-body oracle 0 drift · smoke 0 throws · the eight arena fights clean. | ✅ DONE |
| 8f.282 | **#257 - EVERY PROMISE ON THE FOUR LADDERS PAYS, AND THE ONE THE ASK NAMED WAS INVISIBLE RATHER THAN DEAD.** *(User, 2026-08-27: *“does mood work? For example for captain it kind of should be high (because higher morale)”* and *“fix 256 do also - the things that are claiming something but not actually conected to the real stuff”*.)* **⛔ THE MOOD POOL WAS REAL AND THE SHEET DREW EVERY BODY THE SAME.** The Captain carries **110** and a spearwoman **90** - a 22% difference the MORALE ladder exists to buy - and the sheet's third pool row printed `☺ HAPPY` with an identical **137.016px** of fill on all four founders. ⛑ **The bar is a FRACTION and every body in the game opens a fight at the same 71% of its own pool**, so the one readout MORALE moves could not move. #247 made this the one row of three with no figure, on the argument that its value IS the word - true while the pool was the same on everybody, and false the moment #255's rungs started moving it. The word stays and the figure joins it, exactly as the ARMOUR row already does: **`☺ HAPPY 78/110`**. **⛑ AND THEN THE CENSUS, WHICH IS THE HALF WORTH KEEPING.** `probes/promises257.js` puts a body on each rung and measures the readout that rung NAMES - not the source, the readout. **Five more promises were printing and not paying**, on top of the two #256 closed: **MORALE +2's *loses mood at ×0.75*** (nothing multiplied a knock; it pays in `mor()`, the one site every knock comes through, **and only on the way down** - a brave body does not get less back for being brave); **MORALE +3's *mends 12 a day*** (#254 wrote the comment and left the arithmetic alone - `mend` was ONE figure for the whole company, so the rung could not reach a body; it is per body now and stacks with the rations); **STRENGTH +4's STANDS** (the flag was set on the unit and nothing read it - it is caught at the one site a body reaches zero, BEFORE the zero is written, and spends itself); **INTELLECT +4's UNREPEATABLE** (same - it goes in `mayAim`, the one gate both brains and the offer ring ask, so a refusal stated there cannot become a stall; keyed on WHO struck, cleared at the top of every round, and it refuses a BLOW and never a shove or a heal); and **MORALE +4's TWO PERK POINTS** (never read; it doubles the PERK level and deliberately not the stat level). **⚡ AND THE CENSUS'S OWN FIRST CUT REPORTED TWO FALSE DEADS**, because it grepped function SOURCE for the wiring: FLEET lives in `unitFrom` and TWO PERK POINTS in `levelUp`, neither of which was in the list it searched - and its own functional row two lines above already said FLEET pays. ⛑ **A source grep tests where you THOUGHT the wiring went; driving it tests whether it went anywhere.** Both are functional now, and UNREPEATABLE's reads exactly as the rung is written: *first blow true · same foe again false · a second foe true*. **18 of 18 rungs PAY.** **Gates:** `LINT()` 0 · floor `[]` · clip `[]` on the sheet and the documented set in battle · overlap `[]` · map 0/0/0 · eight hovers driven, every ledger adding up · 244-body oracle 0 drift · smoke 0 throws · the eight arena fights clean · `gt.py check` 3 scripts 0 problems. | ✅ DONE |
| 8f.283 | **#258 - THE GAUNTLET STOPS CARRYING ITS OWN COPY OF THE ORDERS, AND THE FIRST THING IT MEASURED ON TODAY'S BUILD WAS ITSELF LYING.** *(User, 2026-08-27: "хей, живой ли этот тул? можеш обновить его с последнего билда рабочей игры".)* **It was alive**: it live-loads the build, so six days of #250 and #252-#257 were already in it and every dependency the bridge names still resolves. What had gone stale is what it CARRIES rather than reads. ⛔ **THE STRATEGIES ARE READ OFF `ORDER_PROF` NOW.** #229 wrote its own three profiles because nothing in the build held them; #242 then put the identical `aggressive` and `steady` rows into `ORDER_PROF` for the practice field's dev orders and wrote the warning in BOTH files that a change to either copy was now a change to both, by hand. That is the shape this project has merged out five times (`choiceNote` #150, `raceDoorPick` #159, the mood echo #176, `applyHurt` #197, `payMat` #239) and every one of them had drifted by the time somebody looked. ⚑ **AND DERIVING IT WAS WHAT THE BUILD ASKED FOR: `defensive` ships marked FIRST-CUT with *"the numbers get measured in the gauntlet before they are trusted"* written beside it, and it arrived here untouched** - four orders, each chip labelled with the knobs it actually moves, the column headings keyed off whatever the build carries, and the three-copies-of-a-table problem gone with them. ⚠ **The fallback literals stay and say so on screen**, because this tool must open ANY build the iframe loads, including one older than #242. ⛔ **AND THE ONE NUMBER CERTIFYING THE WHOLE POINTS COLUMN WAS A HARDCODED STRING.** The status line read *"founders price at 100 by calibration"* - `PTS_SCALE`'s own words - as a literal, so the tool asserted its calibration instead of taking it. **Measured, the founders price at 108**: the scale has drifted +8 since the ladders, and every band on the page is read against it. It is taken once per RELOAD BUILD now and names the drift in words. ⚑ **THE THIRD ADDITION IS A BUILD PICKER, AND IT IS THIS REPO'S OWN BASELINE TECHNIQUE MADE PRESSABLE** (`ui-scales.md` §5's second tab): point the field at a `git show`n prototype, run the same grid, point back. ⚠ **The Δ store deliberately does NOT key on the build file** - comparing a baseline run against a working one is the whole reason the picker exists - so the store carries the build it was taken on and the status line says *"the Δ above compared it against X, which is a DIFFERENT build"* in words. **AND THE FIRST USE OF IT ANSWERED A REAL QUESTION.** Founders, brain, n=10, seed 1234, **8f.252 -> today**: **THE SNARE 30% -> 100%** (3.1 bodies down -> 0.2), cross-checked on the house instrument, which never sees this tool: `ARENA.match('four','snare')` reads **1/6 -> 6/6**, against its own header comment recording the fight as tuned to *"~3 of 10 in mirror-AI"*. **The act finale is no longer a threat to the STARTING FOUR.** The road moved in both directions: Sling-Line **100% -> 60%**, Broken Men **60% -> 90%**, and Blood on the Road still 100% but **5 rounds and 0.4 down -> 8 and 1.4**. ⚠ **`defensive` measures indistinguishable from `steady`**: byte-identical on 6 of 12 fights and within a body on the rest, which is the first thing #242's own note asked this tool to find out. 👤 **All three are rulings and are filed in `OPEN_QUESTIONS.md` §O.** **Gates:** the bridge's API sweep clean (every symbol it names still resolves), `doct` proved NOT to leak between cells (the #242 fault ARENA had - #229 avoided it by copying `AIP` rather than the harness, and `FULL` reads `AIP_FULL` now so it stays avoided), 480-fight four-order run with **0 engine errors**, COPY still exports, no em dash. Picture `shots/258_gauntlet_four_orders.png`. ⏳ **`tools/harness.js`'s own `runFight` still does not stub `SEENF.mark`**, so a regression run marks spoiler fights on the dev origin; harmless under `TEST.on` and worth one line on the next harness touch. | ✅ |
| 8f.284 | **#259 - THREE CLASS CARDS WERE ADVERTISING AN ACT THE ENGINE DOES NOT BUILD, AND THE ONE SCREEN THEY ARE PRINTED ON IS WHERE A CLASS IS CHOSEN.** *(User, 2026-08-27: "Double check - that stats and skills actually give what they promise".)* ⛑ **THE AUDIT CAME BACK CLEAN EVERYWHERE THE NUMBERS LIVE, AND THE LIE WAS IN THE SALES PITCH.** All 26 perks are read by the engine (no dead key); every build-time figure matches its own text to the digit, measured by differential (`ironjaw` +12 moraleMax, `stoneskin` +10 and `thickhide` +18 armour, `quickfoot` +1 speed, `conduit` spellPow 1 -> 1.2, `opportune` flankPow 15 -> 25, `eagle` +2 range through `perks.indexOf`); every strike-time perk has exactly one implementation line carrying the number its `d` field states; and `STATLADDER` is honest on all nine rungs (`thews` equals the ladder's own melee/bow figure, `D.hit` equals GIVEN+ladder, `D.dodge` likewise, **0 mismatches across rungs -4..+4**). ⛔ **WHAT WAS WRONG IS `CLASSES[k].sig`, THE ONLY PLAYER-FACING STRING DESCRIBING A BODY'S ACTS THAT IS NOT DERIVED FROM THEM.** The captain's card sold RALLY THE LINE and `unitFrom` pushes HOLD THE LINE; the archer's sold AIMED SHOT and the cutter's BACKSTAB, both of which their own code comments record as deleted in favour of CRIPPLING SHOT and DISTRACT. The acts were replaced twice and the pitch beside them never moved. ⛑ **AND IT IS CHECKED NOW (LINT 6h), BY THE ENGINE RATHER THAN BY A SECOND TABLE**: a real body of each class is built through `unitFrom()` and every act its card claims must be in the row. ⛔ **THE CLAIM IS DECLARED (`sigActs`) AND NEVER PARSED OUT OF THE PROSE** - the first cut read capitalised runs out of `sig` and was wrong inside the hour, because "HOLD THE LINE, AND COMMAND FROM LEVEL 4" yields the fragment "AND COMMAND FROM LEVEL", which is not an act and never was. A card may say more than its acts (the mage's two sell a rule, not a verb), so no regex can tell a promise from a sentence. ⚠ **AND THE FIRST ATTEMPT TO PROVE THE CHECK REPORTED ZERO THREE TIMES RUNNING, WHICH WAS THE MEASUREMENT AND NOT THE CHECK**: `LINT()` returns an OBJECT (`{findings,byCategory,items}`) and the probe read `.length` off it, so every run scored 0 - the exact shape of the trap this repo writes down about checks that have only ever agreed with you, arriving on the instrument instead. Proved properly by seeding each of the three real historical lies and watching 6h name it, then restoring to silence. ⚡ Two rulings landed with it: **SPEARWOMAN becomes SPEAR** (a class is a trade and the roster puts men, women and ogres in it; the word is 5 characters narrower than the one every box was measured around, so nothing can overflow, and only ONE of the 20 occurrences in the file was player-facing - the rest are comments about a PERSON, which stay), and **COMMAND moves from level 3 to level 4**, matching the skill-tree lab. | |
| 8f.285 | **#260 - THE FIRST THREE GENERAL SKILLS GO IN, AND ONE OF THEM ONLY WORKS BECAUSE IT WAS PUT AT THE RIGHT DOOR.** *(User, 2026-08-27, from the skill-tree lab: "Everchanging luck +1 to the worst or to best stat at the begining of the battle. Do to the main build", plus BREAK AWAY and "For shield mastery: replace naming and description: Doubles your shield bonus".)* ⛔ **EVERCHANGING LUCK IS ADDED INSIDE `effStats` AND NOWHERE ELSE, AND THAT IS THE WHOLE ENTRY.** The obvious site is `unitFrom`, where the other perk figures live - and it would have been WRONG in a way nothing on screen would have shown: `hpMax` comes from `bodyHp(p)`, which re-derives from the roster body, so a rung added at `unitFrom` moves to-hit, dodge, flank and spell power and leaves the hitpoints exactly where they were. Measured both ways: placed at `effStats`, a roll onto STRENGTH takes the body **66 -> 75 hitpoints**; placed at `unitFrom` it takes them 66 -> 66 while the sheet still says the stat went up. That is #259's promise-versus-payment split, one day later, and this time it was caught before it shipped rather than after. ⚑ **WHICH stat is rolled ONCE, by `rollLuck`, at the top of the fight** (`G.party.forEach(rollLuck)` before the bodies are built) and parked on the roster body: `effStats` is called many times a frame and a roll inside it would shimmer. It reads `statParts` and not `effStats`, or the stat it lifted last battle would look like this battle's best. **Measured over 200 rolls: 105 worst / 95 best, and never once a middle stat.** ⚑ **BREAK AWAY has ONE reader.** `disHexes(u)` is what the act pays, what the float shouts and what the MOVE card prints, because the hex count appears at three sites and three hand-typed 1s is exactly how a perk comes to promise what it does not pay. Measured: `moveBudget` after disengaging goes **5 -> 6**. ⚑ **SHIELDWORK doubles the SHIELD'S OWN figure**, not a flat number, so a buckler is worth twice a buckler and a round shield twice a round shield (+4 -> +8, +5 -> +10, +9 -> +18 measured) and the perk never becomes a reason to carry the cheaper board. ⛑ **AND ALL THREE ARE REACHABLE, WHICH IS NOT AUTOMATIC**: `perkPool` concatenates `CLASSES[cls].perks[tier]`, so a perk no class lists is a row nobody can ever buy - the dead-key shape #259 checked for. They are joined onto every class's tiers 2, 4 and 6 in one loop rather than typed into seven rows by hand. ⚠ **The patch script's first run reported two MISSes and wrote nothing, and the guard is why that was cheap**: the prototype is CRLF and the multi-line patterns were LF. `LINT()` 0 findings after. | |
| 8f.286 | **#261 - THE OLD PERK TREE COMES OUT, FOUR SKILLS STAY, AND A CARD SAYS WHERE A SKILL BELONGS.** *(User, 2026-08-27: "remove older skills from skills tree in the build", "Breaking out rename to deisingage. Give it on 2d lvl. Hide from basic options", "Leave also duelist - but +10 to hit and +5 to dodge only one enemy adjusted to you", "Near skill put kind of belongins icon (race, class, or general)", and "Save to the file curent skills, that we are deleting from the main build. Maybe we will use some of them".)* ⛑ **THE 25 RETIRED SKILLS ARE IN `docs/archive/RETIRED_SKILLS.md`, GENERATED FROM THE TABLE RATHER THAN RETYPED.** Each row carries the exact wording it had, the class and tier it sat at, and **the engine line that paid for it**, so putting one back is a row and a tier rather than an archaeology session. ⚠ **The engine code that READ them was deliberately left standing**: `has(u,'bracewall')` and its family answer false forever, nothing throws, and no mechanic changed shape. **DISENGAGE is no longer a basic act** - it is the `disengage` perk at tier 2 and nothing else, which is what makes it worth a pick. ⚠ **That is not a soft lock and the distinction matters**: an engaged body can still walk, it simply pays the parting swing, so losing the act costs blood rather than the turn. ⚠ And it is roster-only, because the push is the file's single `k:'dis'` site: no foe ever had it to lose. ⛔ **AND ONE LINE OF THIS ENTRY WOULD HAVE CRASHED THE COMPANY SHEET, WHICH IS THE FINDING.** DUELLIST's new dodge half asks `threatsAt(...)===1`, and `threatsAt` walks `alive()`, which reads `B.units` - while **B is null between fights and `dodgeOf` is called by the sheet's own DODGE row and its hover, on every body, outside any battle**. The guard `B&&` is the whole fix. It is the identical trap #259 wrote `if(!B)return false` into `wallBlocked` for, missed here one entry later, and found only by DRIVING the function: the page booted clean, `LINT()` read 0, and the crash sat one click away on a screen no probe had opened. ⚑ **The belongs-to mark is DERIVED, not a third vocabulary.** A class skill wears that class's OWN tool glyph through `cIcon`, a race skill would wear the race's own face through `RACE_ICON`, and only GENERAL authors a glyph (👥, verified unused in the file) - #102's one-thing-one-symbol rule, so the mark can never disagree with the icon the same class wears everywhere else. `kind` is a FIELD on the perk and not a guess from which tier it sits in, because a general skill is offered by every class and would be indistinguishable from a class one by position alone. ⚑ **DUELLIST reads its condition at two sites and they ask the same question** (`threatsAt(...)===1`), measured live: one adjacent foe puts *duellist, one on one +5* in the sheet's own dodge breakdown (13 -> 16, the rest eaten by the existing stack cap); a second adjacent foe makes the row vanish. ⚡ **Two saves were made defensive in passing**: `p.perks.map(k=>PERKS[k].n)` had no guard, so an existing save holding a retired key would have thrown on the roster line. It filters now. ⏳ **TIERS 6 AND 8 ARE EMPTY FOR EVERY CLASS** and that is a known, temporary state of the teardown: a body past level 5 banks perk points with nothing to buy until the tree's own skills land. It degrades honestly - `perksLeft` answers 0 and the sheet prints "Nothing to learn". | |
| 8f.287 | **#262 - THE SKILL TREE LANDS: TWENTY-EIGHT SKILLS IN THREE LANES, AND THE SHEET SHOWED TWO OF SEVENTEEN FOR AN HOUR.** *(User, 2026-08-27, with a screenshot of a three-row perk list: "no it is almost empty. Take skills logic from the file - there are class, race, and general skills - want to see them in new build".)* The tree in `tools/skilltree_lab.html` is the build's now: **11 class · 5 race · 12 general**, each row carrying `kind`, which is both what the card's mark is drawn from and what the pool gates on. ⚑ **ELEVEN OF THE TWENTY-EIGHT WERE RESTORED RATHER THAN WRITTEN, AND THAT IS #261's DECISION PAYING OFF.** That entry retired their rows but deliberately left the engine lines standing, so BRACED WALL, KILLING ZONE, SURE SHOT, EAGLE EYE, SHADOW-STEP, BUTCHER, BRAWN, THICK HIDE, STEADYING and WAR-LEADER came back as a row and a tier, on the mechanics that were already tested. `docs/archive/RETIRED_SKILLS.md` is what made that a lookup instead of an excavation. ⛔ **A RACE LANE IS STRUCTURALLY NEW.** `perkPool` had only ever asked about a CLASS, so a race skill was unreachable however many rows the table grew - the dead-row shape this file has now paid for three times. `RACEPERKS` and `GENPERKS` are the other two lanes. ⛔ **AND THE ENTRY'S OWN BUG IS THE ONE WORTH KEEPING: TEACHING THE POOL IS NOT TEACHING THE SHEET.** `perkPool` learned all three lanes and was measured correct (a level-9 ogre spearman is offered 17). The PERK CARD RENDERER never asked it: it walks `CLASSES[p.cls].perks` directly, so the sheet drew the class lane alone and that same body was SHOWN two cards. The pool was right and the screen was wrong, which is exactly the user's complaint one layer down. **`perksAt(p,l)` and `tiersFor(p)` are the one reader now** and `perkPool`, the card renderer and the class-tree hover all call them, so what a body is offered and what it is shown cannot come apart again. ⚠ `tiersFor` is the UNION of the three lanes' own keys rather than a literal 2/4/6/8, which is #174's rule about a tier list going stale one lane wider; `PERKTIERS` rose to 8 with it, or BERSERK would have been authored above the ceiling and silently unbuyable. ⛑ **EVERY SKILL WAS MEASURED, NOT ASSUMED.** Build-time by differential: COLOSSUS 66 -> 79 hp, FORTIFIED MIND 110 -> 130 morale, THICK HIDE 34 -> 52 armour, STRONG str 2 -> 3 AND hp 66 -> 75 (the `effStats` door from #260 doing its job), NINE LIVES `stands` false -> true, GOOD HAND range 5 -> 6 and EAGLE EYE 5 -> 7 stacking to 8, VERSATILE +5 with a dirk in the off hand and **+0 with a shield**, which is the promise read exactly. Damage by multiplier: base 1.25, TWO-HANDED 1.75 against armour, RAGE 1.47 below half and 1.25 above it, FRENZY 1.45 after a kill and 1.25 cold. **THICK MUSCLES was swept over all six facings**: a plain body takes +5/+15/+30 by arc and the thick-muscled one takes 0 from every one. ⚑ **It goes inside `arcOn` and not at its two call sites**, because `hitBreakdown` PRINTS the arc and `strike` PAYS it, and patching one of two is how a preview and a blow come to disagree. ⛑ **AND THEN SIX REAL FIGHTS WITH ALL TWENTY-EIGHT ON EVERY BODY**, which is the only thing that exercises the kill site, FEARSOME's wound hook and BERSERK's action refund on a real board: 6/6 wins at 6.5 rounds against a 6/6 at 7.0 baseline, no throw, `LINT()` 0. ⏳ **SEVEN LAB SKILLS ARE NOT HERE AND THE REASON IS THE SAME FOR ALL SEVEN**: ROTATION, TAUNT, MAGIC POTION, SECOND WIND, UNNOTICEABLE, TWO WEAPONS HANDLER and FAR HAND each need a new ACT or an AI targeting hook rather than a number on an existing line. Listing them without wiring them would be the broken promise #259 spent its whole audit deleting. | |
| 8f.288 | **#263 - THE OTHER SIDE CLIMBS THE SAME FOUR LADDERS, AND IT WAS ALREADY HALFWAY UP ONE NOBODY HAD CHOSEN.** *(User, 2026-08-28: "use this logic with stats and aply all 4 stats also for enemies. So they also get some of the bufs. We can imagine, that each enemy have influence of all this 4 stats and in the card in batlfield we will write this icon. Why I want to use it - in the future add bufs and debufs of it completly on the battlfield", then "if enemies stats and behavior not together - I think it is a good idea to groop it in main build", and then "but of course, enemy have thier own basic stats, that could be a bit lover then the party".)* ⛔ **EVERY FOE ALREADY CARRIED `st` AND IT WAS THE SAME FOUR NUMBERS FOR ALL 61 OF THEM** - `{str:-2,agi:-2,int:-1,mor:-2}`, typed into `build()`, with `int` the only overridable field and exactly one template overriding it. So the ladders were ALREADY reaching the enemy through every shared function - `thews` gave every body in the game a **x0.85 arm**, the mood `loss` and `climb` rungs, `D.arc` for a caster - and reaching them with a number nobody had chosen. `GIVEN`'s own note said the other half out loud: *"THE ENEMY DOES NOT MOVE WITH THEM"*. ☑ **A FOE'S RUNG IS `FOE_LEAN[race]` PLUS THE TEMPLATE'S OWN `lean:{}`**, which is the roster's *born + race + class + trait* said smaller, and the three people-races mirror `RACEMOD` exactly so a clan ratkin leans the way your ratkin do. A beast is quick, dim and unbothered; `unknown` leans nowhere. Then `hp`, `mskill`, `dodgeBase` and `moraleMax` read the rung at `build()`, after the trims and the champion multiplier, and the three capstones reach the unit off the same `holds()` `unitFrom` uses. ⛔ **AND THE GIVENS ARE NOT SHARED, WHICH IS THE USER'S THIRD NOTE AND IS NOW A MEASUREMENT RATHER THAN AN ACCIDENT.** Your people start from `GIVEN` and climb; an enemy starts from its own statblock and climbs the same rungs. Averaged over the **55 ordinary templates** (no captain, no boss, no `noTrim`): hitpoints **49.1 against 60 (-18%)**, to hit **53.3 against 70 (-16.7)**, nerve **62.7 against 90 (-27.3)**, and dodge **16.5 against 14**, the one column they lead - correct, because it is all a ratkin in rags has. That is before `TRIM` takes another 10%, `ARM` a quarter of the harness, and `HIT_EASE` tilts the roll a net +5 your way. ⛔ **There is deliberately no `FOE_GIVEN` constant**: one number behind 61 hand-tuned statblocks would be a second author for figures each of which is somebody's measurement, and the knob for the tilt already exists and is `HIT_EASE`. ⛑ **THE STATBLOCKS ARE ONE TABLE NOW (`FOE_T`), WHICH IS THE SECOND ASK.** It was fourteen tables in fourteen places: six had module-level names and eight were a `const T={}` inside their own plan function, so *what does the other side look like* could not be answered without opening eight functions and knowing which eight. **The move changed nothing and that is proved rather than claimed**: `probes/foeoracle263.js` builds every `FOE_BUILD[k]()` side on both builds and compares 19 fields a body - **0 drift on all 14 deterministic fights**. ⚠ `steading` drifts against ITSELF, because it rolls three of four ogres for its wall; that is the probe's own note now. ⛑ **THE CARD CARRIES THE FOUR MARKS**, `STAT_ICON` at 32px (never 24 - #230: a 3:4 resample smears a pixel painting), keyed on the band, which since #254 IS the rung, with `tell()`'s own word on the hover and **no number**, which is the MOOD row's rule two blocks up. ⛔ **`noFace` is refused its marks**: the Warden is a thing you cannot read, and four pictures saying how strong it is would answer the question the whole encounter is built on refusing. Driven, not read - `probes/statmark263.js` opens the real `inspect()` card over a foe, one of yours and the Warden: **4 / 4 / 0 marks, 32px drawn, inside the stage, 0 under the floor, 0 clipped**. ⛑ **AND THE SEAM THE WHOLE ASK WAS FOR IS ONE LINE.** `statRung(u,k)` reads `u.st` plus `u.stMod`, and `heldOf` goes through it - so the day a card or a working writes `stMod`, the weight of a blow, how fast a mood goes and comes back, what it takes to land a working and the four marks all follow with nothing added. ⚠ **The baked four do NOT follow it** and the comment says so: re-deriving a POOL mid-fight would have to decide what happens to the damage already out of it. ⚠ **`statRung` and not `rungOf`, and the collision was not theoretical**: `rungOf` is already the battle's MOOD ladder six thousand lines down, and the first cut threw *Identifier 'rungOf' has already been declared*. Two ladders, two meanings, one name is #102's wrong-unit bug waiting in a stack trace. ⚠ **THE BOSSES ARE AUTHORED AND ONE OF THEM COST FOUR DICE ROWS.** The Fen-Mother takes `st:{str:3,agi:0,int:-2,mor:4}` - and her rungs feed `thews` live, x0.85 to **x1.20**, so her dice come DOWN x0.71 (anvil 22-33 -> 16-23, lash 16-25 -> 11-18, gorge 30-44 -> 21-31, drag 24-36 -> 17-25) and she hits for exactly what she always hit for. **That is the point of authoring a boss on the ladder**: her numbers now say she is enormously strong and afraid of nothing, which is what the fight is about, and the damage is unchanged. The Warden is authored to the OLD default, so it is the one row in the re-stat that moves nothing at all. ⚡ **WHAT IT DID TO THE ROAD, `ARENA.match` n=15 a side against the same build without it.** The starting four: **brigand 80 -> 53%**, **pack 100 -> 80**, **slingline 40 -> 27**, tavern 93 -> 87, hold 20 -> 13. A prepared six: **circle 100 -> 53%**, **steading 53 -> 20**, snare 87 -> 80, mirehares 100 -> 93, and the eight early fights still 100 but slower and bloodier. ⛔ **AND THE ONE FIGHT `OPEN_QUESTIONS.md` O1 IS ABOUT DID NOT MOVE: THE SNARE IS STILL 93% FOR THE STARTING FOUR AT n=30** (28/30, 0.6 down). The reason is in the table: the ratkin lean gives +4 to hit and +20% on the swing and takes 5% of the hitpoints and 10 of the nerve, so on the one deck made almost entirely of ratkin it is close to a wash. **O1 still needs its own answer**, and this entry is not it. ⚠ **The re-stat made the long fights long enough to break the instrument**: `eyes.py` opens its CDP socket with `timeout=120` on the socket ITSELF, so any single eval over two minutes dies on `recv` whatever `--timeout` says, and four stacked matrix calls wedged the page. `matrix263.js` accumulates six fight-comps a call and says where it is. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · the oracle 0 drift · `regress.js` 8/8 no ERR/FATAL/GUARD · `smoke.js` **0 throws** across 40 road cards, 19 camps, 59 gear hovers and every foe hover · floor and clip `[]` on the sheet and the documented set in battle · map 0/0/0 · no em dash. | |
| 8f.289 | **#264 - THE RACE LANES FILL OUT, THE LIST GOES TWO COLUMNS, AND GOOD HAND CANNOT BE WHAT WAS ASKED FOR.** *(User, 2026-08-28, on the shipped tree: "it should be definently more racial skills - now i see only 1", "You can write skills in 2 raws", and "Good Hand: this should work only for throwbles, +1 range and +50% damage to hitpoints from throwbles".)* ⚑ **SEVEN MORE RACE SKILLS AND NOT ONE LINE OF NEW ENGINE.** The lanes were 1/1/3 - a human or a ratkin was offered a SINGLE race skill in a whole career - and they are 4/4/4 now: human STUBBORN, SECOND WIND and PATIENT; ratkin SCURRY, PACK TACTICS and SLIP AWAY; ogre STONE HIDE. Every one of them is a key whose engine line has been standing since #261 retired the ROW and deliberately left the code, so a race lane three deep cost seven table rows. **The name is the race's and the key is the engine's**: `quickfoot` pays a hex of movement whatever it is called, and renaming the KEY would have thrown the working line away. ⛔ **AND THE ONE THING I ADDED THAT WAS NOT NEEDED IS THE FINDING.** `armourMax` ALREADY read `stoneskin` - that is an engine line, and #261 left every engine line alone - so wiring it again paid the perk TWICE: **+20 armour on a card that says 10, measured 34 -> 54 where 44 was wanted.** Only the differential caught it; nothing threw and `LINT()` read 0 with the bug in. ⚑ **The rule under it: restoring a retired perk means restoring its ROW, and nothing else. Check whether the engine line exists before wiring one**, because the half of the pair that was deleted is not the half you remember. ⚑ **THE LIST RUNS IN TWO COLUMNS** as a grid on the container rather than a rewrite of the card, so a card can never break across the fold the way `column-count` would let it, and the LEVEL headings span both. Measured: two distinct left edges at 45 and 333, `283.5px 283.5px`, headings at column 1. ⛔ **GOOD HAND WAS NOT CHANGED, AND THE REASON IS THAT THE GAME HAS NO THROWABLE.** `slot:'bag'` is a deliberately EMPTY promise - the code's own note says *"NOTHING IN GEAR{} CARRIES slot:'bag' YET, on purpose"* - and the only throw in the build is the ogre's own racial PICK UP AND THROW. Rewritten as asked, the perk would pay exactly nothing to anybody except an ogre, which is the dead-promise class #259's audit spent itself deleting. It is left as the working ranged perk and the decision is the user's: build throwables first, park GOOD HAND with the other seven act-shaped skills, or keep it ranged. ⛑ Verified: 35 skills, **11 class · 12 race · 12 general**, `LINT()` 0, every new key differential-measured (STONE HIDE +10 and +28 stacked with THICK HIDE, SCURRY +1 hex, PACK TACTICS +10 flank), and six fights with all 35 on every body at 6/6 and 5.0 rounds against a 5/6 at 6.5 baseline. | |
| 8f.290 | **#265 - THE OTHER SIDE BREAKS LESS EASILY, A CHAMPION IS FOUR LEVELS ON, AND THE SWEEP FOUND A FIGHT THAT HAD GONE OFF THE BOTTOM OF ITS OWN SCALE.** *(User, 2026-08-28: "i feel we need to add more morale for enemies, similar to us. So it is a little bit harder to break them", "And then run polishing sequence one more time - ensure things works properly", and "For a champion - it could be 4 the lvl - so 2 random perks from that pool and at least 1 stat up".)* ⛑ **THE NERVE IS ONE KNOB AND NOT 61 EDITS**, which is `HIT_EASE`'s own answer to the same shape of question: what a row AUTHORS is how steady that body is against the others on its side, and what the two SIDES are worth against each other is decided in one place. `FOE_NERVE` is **1.25**, and it is a MULTIPLIER rather than a flat add because the authored pools run **24 to 120** - a flat +16 is +67% on a wedding guest and +13% on a mirehare, and would quietly delete the one thing the wedding deck is tuned around. ⚡ **Measured, both sides built: your six average a 101.7 pool against the road's 63.6, which is -37.5%; at 1.25 the road reads 79.4, which is -21.9%** - closer, and still the lesser side, which is *"a little bit harder"* read exactly rather than *the same*. The ladder is FRACTIONAL, so a quarter more pool is a quarter more morale damage before BREAKING and nothing else about how they break has moved. ⛔ A `noTrim` boss is exempt: 460 already means *it does not react*. ⛑ **AND A CHAMPION IS NOW A BODY FOUR LEVELS ON, WHICH IS WHAT THE MULTIPLIERS WERE ALWAYS TRYING TO SAY.** #245's rule is *one of these, but better*, as one word on a plan row; the multipliers stay the BODY and the same word now also buys what four levels buy on your own roster - `levelUp` alternates a perk point and a stat point, so level 4 is two perks and two stat points, and the ask names two perks and *at least* one rung. **The perks are rolled from the body's own lanes** (race + general, which is `perksAt` minus the class half a foe does not have) and **the rung is DERIVED, not rolled**, because which stat is the one thing here that is not a coin toss: a shooter is better quicker, a caster cleverer, everything that closes and swings stronger. A rolled rung would put INTELLECT on a dog. ⛔ **AND THIRTEEN OF THE 28 PERKS WOULD HAVE PAID A CHAMPION NOTHING**, because they are read off `p.perks` inside `effStats`/`bodyHp`/`unitFrom` - the ROSTER path, which no foe takes. That is exactly the promise #259's audit was run to delete and #260 was caught by. ⛑ **So the answer was to PAY them rather than to ban them**: `foePerks()` is the foe's copy of those build-time lines (COLOSSUS, FORTIFIED MIND, STONE HIDE, SCURRY, PACK TACTICS, NINE LIVES, GOOD HAND and EVERCHANGING LUCK), which takes a human champion's pool from **three keys to five** and lets a champion come up tanky, lucky or long-armed instead of always the same. ⚠ **Three are deliberately still absent and the comment says why**: DISENGAGE would put a new ACT on a body neither AI brain has been taught to spend one on (#224's greyed-act trap), and SHIELDWORK and VERSATILE both read a piece of GEAR a foe has no `eq` to hold. ⛔ **AND THE POLISHING SWEEP EARNED ITS KEEP: `snarejoin` MEASURED 0/20 ON A FIGHT ITS OWN TABLE TUNES TO 40-53%.** Three builds, n=20 a side, same instrument: **30% before #263, 15% after it, 0% after #265**. The Hold host is nineteen coats of the two bodies that gained most from both changes, and #263's own matrix never covered it - `snarejoin` is not on the road list. ⛑ **The fix is the dial that function's own note already names** (*"one bill is worth twenty-seven points of win rate and a crossbow is worth nothing measurable"*), turned the other way for the first time: three bills out - the two the party-size clause used to add, and one from the wall - landing on the row that table already measured, **17 coats, 5 bills, 3 bows**. It reads **8/20, 40%**, which is #245's own figure. ⚠ **At n=15 the same build read 33% and the monkey-patched sweep read 55%**: this is the high-variance fight its note warns about (*"the first three passes here read as noise"*), so it is measured at 20 or it is not measured. ⚠ **The first cut of that sweep was WRONG and reported 57% where the fight reads 0**, because the reconstructed plan omitted `holdHost`'s `>=6` clause and measured a 15-body host against the real 19. **A tuning sweep that rebuilds its subject has to rebuild all of it.** ⚠ **The ranking against the honest finale cannot be restored here**: that note wants this road slightly easier than `snare`, and `snare` has drifted to ~80% on this instrument, which is O1 and not something this function can fix by losing bodies. **Gates, run twice over:** `LINT()` **0** · `gt.py check` 3/0 · **the oracle says only `morale` and `moraleMax` moved**, on every fight, with the Warden byte-identical and `snarejoin` correctly 19 -> 16 bodies · `regress.js` 8/8 no ERR/FATAL/GUARD · `smoke.js` **0 throws** · the card driven again: 4/4/0 marks, 0 under the floor, 0 clipped · floor and clip `[]` · map 0/0/0 · no em dash. ⚠ **The longer fights broke the instrument again** - fewer routs means more fights fought to the end - so the road matrix had to drop to a nine-row focused sweep at n=12; at that size every row but `snarejoin` is inside the noise. | |
| 8f.291 | **#266 - THE POOL IS 100, `STEADY` IS THE BAND A FIGHT OPENS IN, AND NOTHING IS BORN WITH ANYTHING.** *(User, 2026-08-29, nine asks off the company sheet.)* ⛔ **`GIVEN.nerve` 90 -> 100** (*"basic start is 100% (I don`t know, why some of my people have 90)"*). 90 was never a decision anybody defends - it is the figure the MOR ladder's +-10 a rung was authored around, so an ordinary body carried 90 and the Captain 110. ⚠ It is YOUR side only: a foe's pool is `t.mor` off its own statblock and has never come through `GIVEN`. ⛔ **A SIXTH BAND, AND IT IS WHERE A FIGHT OPENS** (*"Add intermediate more (steady) ... And your guys basic start on the middle of stead (aprox 55-60)"*). `LADDER` is **It rocks .90 - Happy .72 - Steady .42 - Shaken .30 - Breaking .15 - Broken 0**, and `NERVE_RUNGS` moved with it because `restate`'s own comment says the two are one fact in two places. #228's merge is KEPT - both free rungs cost nothing and both are mechanically `steady` - and what it left the game without was a NAME for where a body stands: a fight opened in the middle of a 38-point band called Happy, so the first word every player read was the second-best rung in the game with nothing under it but bad news. ⛑ The two free rungs say different things rather than the same thing twice: one is a step from the top rung, the other a step from the first penalty. ⚡ **AND THE NUMBERS ARE THE ONES THEY ARE BECAUSE ±15 IS EXACTLY HALF THE BAND.** Steady runs .42-.72, its middle is .57 and that is `START_NERVE_YOU`; `moodFx().start` runs -15 to +15, so a company at HIGH SPIRITS opens on the top edge of STEADY and one ON THE BRINK on its bottom edge - the company's mood is worth precisely one band and cannot be worth two. Measured on the shipped four: **57/100 at AT EASE, 72 at HIGH SPIRITS, 42 ON THE BRINK**. `START_NERVE` went .78 -> .64, which keeps #244's measured 7-point gap between the sides EXACTLY and puts both of them back in one band, so the note beside `mor0` (*"the enemy starts on the same rung you do"*) is true again after two entries of drift. ⛔ **THE MOOD BAR'S DIVIDERS WERE TYPED INTO CSS AND TWO OF THE FOUR WERE WRONG.** `.bar.nrv::before` read `15 / 40 / 70 / 90` and the ladder's gates have been `15 / 34 / 52 / 90` since #129 and #228 - so for two entries the battle card drew ticks at rungs that do not exist, out by up to 18% of the pool, and nothing said so because a stylesheet cannot ask `LADDER` anything. `--nrvticks` is BUILT from the table, beside it. ⛑ **And the dividers came to the sheet** (*"Bars with health do more in style of the game - and feel free to add some dividers (like in battle)"*): #249 had already given both surfaces the socket and the crown and left the DIVIDERS behind, so the sheet drew the same three pools as blocks. One `:is(#bPlq,.ibars)` selector, which is #219's arithmetic - `:is()` keeps the 1-1-0 those rules had. ⛔ **AND ONLY THE PICTURE FOUND THE LAYER BUG**: a generated `::before` is the FIRST child and the `<i>` fill is a real one, so the mood ticks painted UNDER the fill while the blows hatch (`::after`) paints over it - three bars in one column, two divided end to end and the third only in its empty half, on the battle card since #102. Both readings are `background:<gradient>` on an `inset:0` pseudo and **no probe in the project can tell them apart**. ⛔ **NO BODY IS BORN WITH A LEAN** (*"No Born !"*, the row boxed on the STRENGTH hover). #255 stopped ROLLING one and kept the Captain's authored `{str:1,agi:1}`; the ask is the same ask pointed at the row that survived, so he is -1 STR and -1 AGI and what is left of him is four named lines. ⛑ **AND DELETING THE ROW WOULD HAVE DELETED TWO SHIPPED REWARDS, WHICH IS THE FINDING.** The residual is not birth and not backstory any more: it is `p.st` raised IN PLAY - a night at THE WARM SPRING (`springStay`, +1 MOR, *permanently, for as long as they live*) and the wish off the falling star (`starWish`, +1 on their weakest axis). Both are written straight into `p.st` on purpose and this is the only row that pays them. **So the word goes and the row stays**, and the new word is the true one: `the road`. ⛑ **THE TOTAL IS THE HEADLINE** (*"Show bigger total bonus - i feel it is quite important"*): `--fs4` -> `--fs6` on the sum and `--fs1` -> `--fs2` on what it BUYS, brightened off the dimmest brown on the card, the sources deliberately not moved (what makes a total read as a total is the gap to the rows above it), and the column 154 -> 176 paid by the tip rather than the ladder. ⚠ **`#gtTip` IS A CHILD OF `document.body`, SO BOTH `ui-scales.md` §5 COUNTERS ARE BLIND TO IT** - they walk `#stage *`. The one box this entry made bigger is the one box no gate can see; measured by hand instead, 8 stat-hovers x 2 bodies, **0,0 overflow, the total's right edge exactly the column's, `mor` at the 640 cap and still clean**. ⛑ **THE PERSONALITY LINE STOPS SAYING THE LEDGER** (*"don`t like this line"*, on a header reading **Experienced · +1 STR and +1 MOR**). #234 authored that string at his own word and it is right where it is READ - the muster wall, the promotion card, the battle hover, all places with no ledger beside them. On this sheet the stat hovers name EXPERIENCED as a row and total it, and this entry just made that total the loudest thing in the box. **A filter and not a rewrite, on this surface only**: a clause that is ONLY a stat restatement comes off, a clause that says something else keeps every word (DOES NOT RUN keeps *Cannot rout, ever*). `SHEET_TRAIT_STATS=true` puts it back in one word, which is #240's switch and the reason that entry cost one line when he changed his mind hours later. ⛑ **THE LEVEL COMES BACK WITH THE FIGURES ON IT** (*"return lvl with expirience - so i can see it in details"*). #251's argument was that the roster tile 130px away says it twice - true of the LEVEL, never true of the EXPERIENCE: a 42px arc round a bust says *somewhere along* and the two exact figures were on a `title`, which is the one readout this project has twice recorded as not being a readout. ⛑ **AN OFF-HAND PIECE SAYS WHAT IT DOES** (*"For offhand dirk and shield show what they exactly do"*). `shortGear` had a weapons branch and a coats branch and the slot they both missed is the OFF HAND, so a shield read `RARE · OFF` beside a cook-pot reading `COMMON · ARMOUR · stops 12` - the one slot whose whole job is a modifier was the one with no figure on it. Derived off the same fields `gearLine` reads, so a field added to a GEAR row appears on the row and in the hover untaught; `tipFor` gained #244's `meleeHit`/`meleeDmg`, which had reached `gearLine` and not it, so the piece with the most conditional rule in the game opened a hover with a flavour paragraph and not one number. The dirk's prose went **39 words to 20** and it is affordable because both halves of the old sentence were describing fields the row now derives. ⛔ **AND A COMMON WEAPON IS NOT A PRIZE, AT BOTH DOORS** (*"For after battle don`t give a common weapon as bonus"*). The rule was already written one function away: `rollRack`'s note says in capitals *THE COMMON ROW IS NEVER A MAIN-HAND WEAPON ... a cheap sword is the thing a company already has six of*, and `rollFind` is where it never got applied - `common` is only reached there as the step DOWN from an emptied rare pool. ⛑ **Proved by making it fire**: with the whole rare pool bought out and six common main-hand weapons sitting in the pool, 300 finds returned **0** of them. The other door is authored, so **LINT 8j** asks it - and the two rows it names are the ones the player actually meets, a Boar spear off the clash and off the Snare, both cut. ⚡ **PRICED, AND THE FLAGS WERE ALL SAMPLE.** `ARENA.match` n=15 a side over 14 road fights x 2 comps, against `git show HEAD:` in a second browser: **starting four 56% -> 55%, prepared six 87% -> 86%**. Four cells moved 20 points or more and **every one of them evaporated at n=30 on both builds** - four/brigand **80% vs 80%** (24/30 each), four/slingline **23% vs 23%** (7/30 each), four/armour 13% vs 17%, prepared/hold 93% vs 97%. A 20-point swing at n=15 is three fights, which is this repo's own written note (*n=6 cannot tell 0% from 20%*) arriving one sample size up. ⚡ **AND THEN #265 LANDED ON MAIN AND THE WHOLE PRICE WAS TAKEN AGAIN, ON THE MERGED BUILD, BECAUSE THAT ENTRY IS ALSO ABOUT ENEMY MORALE.** `FOE_NERVE` 1.25 gives the other side a quarter more pool; this entry takes the opening fraction down for both sides. The two pull opposite ways and had to be measured together, not reasoned about: **starting four 58% -> 50%, prepared six 86% -> 84%** at n=15 a side against main. ⛔ **THREE CELLS FLAGGED AND TWO OF THEM INVERTED AT n=30, INCLUDING THE ONE THAT IS THE GATE.** THE THREE BELLS read 93 -> 60% at n=15 and **90 -> 97% at n=30** (27/30 against 29/30), so the tutorial brawl is not merely still winnable for the starting four, it got easier; the SLING-LINE read 27 -> 0% and is **7 -> 30%** (2/30 against 9/30). ⛑ **A 20-point swing at n=15 is three fights, and this entry has now watched six of them evaporate.** ⛔ **THE ONE THAT SURVIVED IS THE FEN-MOTHER, AND IT IS A REAL INTERACTION RATHER THAN A SAMPLE: 77% -> 57% for a prepared six at n=30** (23/30 against 17/30), 14.5 -> 16.5 rounds and 2.4 -> 3.1 bodies down. **She is the one body in the game whose BROKEN state is stronger than her whole one.** `desperateAt` fires at `morale/max < 0.16` and #38's note says why she has it: she does not rout, she goes DESPERATE, loses hitpoints a turn and hits harder. A lower opening mood is therefore a BUFF to her and to nothing else on the road - 300 x .78 is 234 and 300 x .64 is 192, so she starts 42 points nearer the state she is dangerous in. ⚠ **`desperateAt` itself did not drift**: .16 still sits just above BREAKING's .15, which this entry did not move. What moved is how far she starts from it, which is the same change every body took and reads with the opposite sign on her alone. **Gates:** `LINT()` **0** on a fresh page · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented set on the world and the battle · overlap `[]`, slack **109** · map 0/0/0 · the stat tip measured by hand, 0 overflow on 8 · `shots/266_sheet.png`, `266_ledger.png`, `266_bars.png`. | |
| 8f.292 | **#267 - THE LIZARDS OF THE BURNT ROAD: FOUR CREATURES, FIVE FIGHTS, FIVE CARDS.** *(User, 2026-08-29 and 30, over four messages: a fire-spitting small one, a big one that pulls, a giant with a tail that sweeps, mutations later; then the stats redone on the cut scale, four tiers with a Heroes-of-Might-and-Magic piercing breath; then* **"slug hide 15 / 5. Ash drake 25 / 6 and fiy skil. The third furnance damage 10 and tail hit push 2- tiles. Tail has 2-3 other tiles ocupation. 120 health"** *and* **"create and add very short 1 sentence one choice event - to fight"**.) ⛑ **THE STORY WAS ALREADY IN THE LORE BOOK AND NOBODY HAD SPENT IT.** §09 says the Snare Clan took Ashmoor's foundry last winter and **could not work two of its furnaces**, and gives no reason. The reason is that they were not empty. That one sentence makes the four creatures one family, makes the fires on this road a TRAIL rather than weather, and gives the boss a name nobody has to be told twice: THE THIRD FURNACE. ⛔ **EVERY FIGURE IS SOLVED BACKWARDS AND NOT ONE OF THEM IS TYPED.** `build()` puts a template through `TRIM` (.9), `ARM` (.75), `shooterThin` (.9 for a body with a ranged damage act) and then `HDA_CUT` (1/3.5) unconditionally, so what a fight FEELS like and what a statblock SAYS are two different numbers - the steading ogres' own note (*the figure in a statblock is not the figure on the board*) on its second surface. Authored 30/5, 65/23, 97/28 and 420/160 land as **salamander 7/1, slag-hide 15/5, ash-drake 25/6, the Furnace 120/46**, with a **9-12** bite and a **5-7** tail: the asks, to the point, measured on the built bodies. ⛑ **THE ENGINE COST FIVE WORDS AND ONE OF THEM WAS FREE.** `fireborn` is guarded at all three doors a body can catch fire through and the AI's ember preference takes the unit now, so a slag-hide will stand in the burning patch it is hunting beside. The **veil** is ONE clause in `mayAim` - the file's own gate, so the offer ring and both brains refuse together and none of them can stall on it. An act may write `ember`, which is `bog` with the element swapped and goes through `charHex`. **Flight is one word in `wetproof`**, which is where *does the ground get to charge this body* is answered for the gills, the Fen-Mother and `groundwise`; a drake crosses water, bog and its own fire at a hex a hex and needed no second movement system. ⛔ **AND THE FOUR-HEX BODY WAS NEARLY FREE, WHICH IS THE ARGUMENT FOR HAVING HAD ONE PLACE THAT KNOWS.** `u.tail` still means what it meant (every drawing rule reads it and none had to learn anything) and `u.tails` is the whole chain; `hexesOf` is the one reader. Because `udist` measures to a body's NEAREST hex, a `sweep` off a four-hex animal already catches everything standing along her whole length - **her tail IS the arc**, and not a line of new geometry was written for it. `push:2` is the ask and the fire combo was built in #186: *shoved INTO the burning ground, it lights them*. Walking is a snake - every hex takes the one in front of it. ⛑ **THE BREATH IS FOUND IN PIXELS AND NOT IN ARITHMETIC**, and that is load-bearing: on an offset hex grid `DIR[r&1]` is two different tables, so *straight on* is not a repeatable column step and TWO of the target's six neighbours are plausible continuations. A dot product against the breath's own direction is the only thing that tells them apart. `PIERCING` is a module flag rather than an argument because `strike()` is called from eleven places. ⛔ **THE TRAIL IS DEALT ANYWHERE AND MET IN ORDER**, which sounds like a contradiction and is the design. *Random places* and *small at the start* are both asks and the pool cannot honour the second by itself. A card carries `trail` and `dealEvents` sorts the trail cards it happened to deal into road order across the slots they landed on: where they fall is random, the order is not, and no company meets the Furnace before it has seen what she leaves behind. ⛔ **LINT CAUGHT THE ONE THING REASONING HAD NOT: `nodeType:'battle'` COLLIDED WITH THE HOLD'S MEN.** The five cards were authored as battle nodes because they ARE fights, and `p1` is both a floating slot and that card's band stop - *the stop would stack two fights onto one arrival*. The older rule is right: the road does not announce itself, so a card with a battle door behind it is an ordinary `event` node until you stand on it, exactly like the toll and the circle. ⚡ **PRICED WITH `ARENA.match`, AND THE BIG FIGHT WAS THE ONE THAT DID NOT WORK.** Two drakes and four hangers-on read as the tier-three encounter on paper and measured **100% in 6 rounds for the prepared six** - the same figure as THE GLASS ROAD, i.e. third in the story and second in the difficulty. A 25-hitpoint body is not a wall however good its verb is, so the answer was the CROWD the verb comes off: three breaths and a champion, **93% and 8 rounds**. The ladder now reads **emberpatch 100% / warmstones 100% (5-9 rounds) for the STARTING FOUR, glassroad 40% for four and 100% for the six, ashdrakes 93%, the Third Furnace 53%** (n=15 a side). `regress.js` clean on all eight old fights, the Fen-Mother included, which is the check that mattered after the great-beast refactor. ⛔ **AND ONLY THE SCREENSHOT KNEW SHE WAS A FENCE.** Every probe passed her - four hexes, all resolving to her, nobody overlapping, LINT 0 - while the picture showed **eighteen identical vertical bars**, because the seams were drawn as uprights repeated six a segment over three segments. Redrawn along her instead of across her, she became a **bench**: a flat-topped slab on eight identical legs. The third cut is a curved back on overlapping ellipses with one lit split running edge to edge and a tapering tail. ⛑ **The head was also facing the wrong way, and the Fen-Mother's painted pair is what said so**: a great beast is exempt from the ordinary facing flip, her tail hexes run east, so art with the skull on the right of its own canvas points the head into its own shoulder. #234's lesson (*two probes said the geometry was right and the screenshot said the meaning was wrong*) on a fourth surface. ⚠ The bestiary face went 🌋 to 🐉: a volcano over an animal reads as TERRAIN, on a board that is already half on fire. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented set on the battle · overlap `[]` · map 0/0/0 · emdash false · every door carries an intent, `mystery` still 1 · `regress.js` no ERR, no FATAL, no HIT GUARD. | ⏳ The art is temporary silhouettes; the brief for the generator is [`art/LIZARD_PACK_GPT_TASK.md`](../art/LIZARD_PACK_GPT_TASK.md) and the queue's §G. The three rulings this entry raises are **S1** to **S4** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md), marked on the `SHIPPED.md` row: the pool going 16 to 21 against six slots, the one-sentence card being out of band by request, flight built as a movement property rather than an act, and the breath cooking the drake's own vermin. |
| 8f.294 | **#269 - THE TWENTY-FIVE-ASK BATCH.** *(User, 2026-08-31, twenty-five numbered points off eleven screenshots.)* ⛔ **COLDHARROW SOFT-LOCKED ON EVERY VISIT AND HAD DONE SINCE #243.** The market row's label became a FUNCTION that day so its figure could be derived through `foodCut`; `drawVillage` hands its labels to `resGlyphs`, which calls `.replace` on them - so the whole `$('wChoices').innerHTML=` expression threw before it was ever assigned, the card drew its prose with **zero buttons**, and the one card in the game with no walk-away of its own had no way off the node. Reproduced 100% of the time, in one probe. `evLabel` is #240's own resolver, written in capitals for exactly this trap; this was the renderer nobody told. ⛔ **THE THREE ARMOUR BANDS ARE ONE RULE: EVERY SUIT STOPS 70%.** #79's LIGHT/MEDIUM/HEAVY each carried an absorb, a through and a dodge price, derived off a hidden threshold and printed as a word that looked like a name - a second, invisible ladder to learn before the bar in front of you could be read. What replaces it is the thing the game already had and was hiding: **the piece**. `planks` costs a rung of AGI, `shroud` gives one, `mail`/`plate`/`ogrescale` each cost one, and those are on the rows and in the hover. The band's dodge price goes with the band, because #79's own note calls it *the price of a band*. ⚡ **PRICED, AND IT IS NOT FREE:** over 28 fight-comps at n=15 against a `git show HEAD:` baseline, most cells sat inside noise, and two did not - **hold/four 30% -> 5%** and **circle/prepared 55% -> 25%**, both re-read at n=20 because n=15 cannot tell 27% from 0%. Isolated on hold/four at n=20, one knob at a time: **absorb .70 -> 0-5%, .60 -> 5%, .50 -> 25%, and the surrounded change alone accounts for 15%.** So the driver is the absorb and the dial is `ARMOUR_ABSORB`, one number. ⛔ **SURROUNDED IS 5 A HEAD AND WAS 15** (*"Suriunded - 5 to swing for each adjustment ally"*): on a three-body ring that was +30 to hit on top of the arc, so surrounding was not a tactic, it was the answer to every question. `curen unit doesnt count` was already true and stays; `FORM.weightMor` - the morale half he asked to keep - is a different rule in a different table and is untouched. ⛔ **XP FELL 3.5x ON 2026-08-29 AND NOBODY NOTICED** (*"somehow i feel with lower health you started to give less xp"* - he is right, and the connection is arithmetic). `XP_PER_HP` is XP **per enemy hitpoint** and every hitpoint in the game was cut 3.5x at the source; `XP_TABLE`, which is what a level costs, did not move. It reads `1.5/HDA_CUT` now, so the harness figure the dial was SET at (n=20 over the eight fights) is restored exactly and it follows the knob the day it moves again. ⛔ **TEN OF THE NINETEEN PERSONALITIES PROMISED A FIGURE THEY DO NOT PAY** - the visible half of which is *"somehow in twitchy things doubles"*. TWITCHY read *+2 AGI · −3 MOR* and pays +1 and −1; PACIFIST said +8 MOR and FRIGHTENING +5 MOR and both pay +2. They are pre-#254 numbers left standing when `st` became a signed RUNG, and the sheet's own stat ledger has printed the true figure in the next column ever since. `traitFigs` substitutes the number in `up`, `cost` AND the paragraph, so the sentence survives and the digit cannot drift - FERAL keeps *breaks sooner*, NOFEAR keeps *there is not much of them*. The doubling itself was three rows whose `d` was their own short line again; all three have a paragraph now. **LINT 15 holds it, proved by making it fire.** ⛔ **A SIGNATURE THAT SWINGS A WEAPON NEEDS ONE** (*"Archer without bow couldnt use cripling shot"*): *THE WEAPON DECIDES THE SIGNATURE* is written six lines above the rows and it only ever decided WHICH one - every row carries a fallback constant for when there is no weapon, so a stripped archer was offered a range-4 shot with nothing to shoot and a stripped brute a 20-29 SWEEP with his fists. Asked per trade, because the answer differs: the archer needs something that shoots, the spearwoman a pole, the brute anything at all; DISTRACT and the captain's orders read no weapon and stay unconditional. ⚠ It made LINT's own class check report two false promises at once - the fixture was the Captain wearing every class with the Captain's sword, and a trade comes with its tool. ⛑ **THE TWO POOLS NAME THEIR MODIFIERS** (*"In the morale and health on hover show all modifiers"*): the four stats have named theirs since #252 and the two derived figures since #254, and the two pools a fight is actually decided by named nothing. `bodyHp(p,W)` takes the same ARRAY collector `dodgeOf(u,W)` has carried since #146 and every row is the difference the step makes, taken beside the step - so a multiplier reports as the hitpoints it is worth on THIS body and the column adds up by construction. `u.morWhy` is built beside the sum in `unitFrom`, the way `hitWhy` already is. ⛑ **AND THE CAPSTONE SAYS WHAT IT DOES.** `capD` has been authored on all four top rungs since #254 and printed nowhere, so the one rung that costs two points was the one whose effect could not be read: FLEET now says *+1 hex of step, every turn* under its own name. ⛔ **A DOG IS A DOG** (*"Miraheres start battles without armor (put their curent armor into health)"*, and one fight along the same of the pack): `bare` is a word on the template and the fold is in `build()`, after the trims and `HDA_CUT`, so what moves across is what the board would have shown. ⚠ It is NOT `isBeast`: the slag-hide's twenty-three points ARE its hide and half of what #267 measured five fights against. ⛑ **CRIPPLING SHOT COULD NOT TOUCH A MIREHARE AND NOTHING SAID SO** - CRIPPLED halves `moveBudget`, and a `noWalk` creature has no MOVE act, so half of nothing is nothing on the two things in the game whose whole content is how far they travel. `hopLen` is one function and six readers. ⛑ **AND THE BUCK CHARGES THROUGH HIS OWN** - same side is stepped OVER and never landed on, so the hex is still never offered as a place to end. ⚡ Priced alone at n=20: mirehares/prepared **100% -> 70%**, and 100% again with the pass-through switched off inside the same build, which is what makes it that change and not the armour. ⛔ **THE PACK IS TWO HEXES, NOT THREE** - the rule was already live and measured (five of seven dogs inside it, the two at four hexes carrying nothing); what was wrong is the radius, because a pack that deploys as a column is inside three hexes before anybody moves. ⛑ **FOUR RARITY TIERS AND THE KEYS DID NOT MOVE**: `rare`->UNCOMMON, `epic`->RARE, and a real fourth key `legendary` in gold on the four unique artifacts. This file's own precedent twice over (#212's *a key is not a name*, #234's `harness`); `legendary` is safe because every body on it is `unique` and `findPool` refuses uniques, so it reaches no drop table and costs no measurement. ⚠ **A row authored `rarity:'epic'` now prints RARE**, and that is the one thing to know before writing a new gear row. ⛑ **ADJUSTABLE WAS ALREADY IN THE BUILD AND HAD NO NAME ANYWHERE ON SCREEN**: `size:'any'` is what `sizeMismatch` asks, so rags, the cook-pot and the bucket have been adjustable since they were written and the only way to find out was to put one on an ogre and watch the number not move. Derived, never a second boolean beside `size`. ⛔ **THE CUB LIFTS THE COMPANY AND THERE ARE TWO DOORS**: the outcome prose already read *"somehow that is worth something"* while the receipt charged six morale for it. The third door that went is KILL IT TOO, which was dominated - 2 gems against LEAVE IT's 3 gems and 120 crowns, 12 morale against 4, and the cub ends either way. ⛑ Also: POISON THE BLADE is **POISON** · Asha has **Half human** (+1 STR, +1 MOR, −1 AGI), authored so it can never be rolled onto anybody else · EVERCHANGING LUCK is **+2**, off one `LUCK_RUNGS` the perk's own sentence and `effStats` both read · the working stave reaches **1** and keeps its `spellRange` · the enemy inspect card wears **⛨ ♥ ☺**, the plaque's own three marks in the plaque's own order · the STEP is back on the plaque, in the slot #95 parked and documented (*"if a later session wants the number back on the panel it plugs in here"*) · `#bZoom` is gone from the battle screen and the WHEEL has stepped between the three stops since #66 · the Marching Drum is a tier up (price 70 -> **120**, measured) and its bar chip draws its painting rather than the ◍ that stood in for it. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented set on the battle · overlap `[]` · map 0/0/0 · emdash false · every door carries an intent, `mystery` still 1 · `regress.js` 8/8 no ERR, no FATAL, no HIT GUARD. | ⏳ The absorb is one number and two fights are 25-30 points harder for it; the isolation table above is what to argue against. The three rulings are **T1** to **T3** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md). |
| 8f.293 | **#268 - THE ELEVEN-ASK BATCH.** *(User, 2026-08-31, eleven numbered points off five screenshots.)* ⛔ **HIGH SPIRITS PAID +10 DODGE AND THE ASK IS +5** ("for moral 'It rocks' + 10 to hit and + 5 to dodge"): the top mood rung's `fx.dodge` was already 10, matching the `+10 to hit` half; only the dodge half moved, 10 -> 5. Measured on the Captain: `dodgeOf` **9 at AT EASE, 13 at HIGH SPIRITS** (was 9/16). ⛑ **KICK'S NOTE SAID "ALMOST NO DAMAGE" AND NEVER A NUMBER**: it now names the real figure, 1 or 2 points, and why - most of the raw 2 is eaten by the target's armour before it reaches hitpoints. ⛑ **THE WORLD BAR'S BLOOD CHIP DROPS ITS `%`**: `heartSVG(hs.st)` already draws three fill states (full/half/empty) off the company's own average, so the heart alone carries the fullness the way #150 built it to; the exact percentage is still on the hover, which `WTIPS.blood` already prints. Same cut on the PAYDAY chip - the word comes off the always-visible chip (`◉21 IN 3D` alone now) and stays on `WTIPS.keep`'s own hover title, the way the seats glyph already lost its caption to the same rule. ⛑ **THE FIRST CHASE LEAVES A TROPHY WEAPON.** `LOOT.chase` already rolls a purse, a jack or salvage; a fourth prize would only land one haul in four, so this rides the SAME slot the aftermath's `rollFind('battle')` bonus already draws, forced once (`G.chaseTrophy`) to a rare-or-better melee weapon (sword, maul or crossbow) - never common, which is #266's own rule against a bonus weapon reading as junk. ⛔ **THE ROSTER'S SELECTED-ROW RAIL IS GONE**: `.rmem.on` kept a 3px gold `border-left` on top of its own background swap, and the ask ("it works only when character selected") is a description of exactly that rule; the background alone still says which tile is picked. ⛑ **THE SEATS TOOLTIP NOW NAMES WHO IS CARRYING THE COST**: it explained the *ratkin 1 / human 2 / ogre 3* rule in the abstract and never who on THIS roster is paying which figure; `WTIPS.party` now lists every member, race and seat cost, the same per-body shape `WTIPS.keep` already uses for wages. ⛑ **THE THREE POOLS ARE A STEP SMALLER, THE DRIVE ROW A STEP BIGGER.** `.sbar`'s `--sbh` 20 -> 17 and its figure `--fs5` -> `--fs4` (13px, `ui-scales.md` §3c's own clean floor for that size - no more "1px over"), on BOTH the sheet and the battle plaque, which #251 pinned to the same numbers on purpose; the DODGE/TO HIT/STEP figures came off the `--fs1` floor to `--fs3`, and the weapon's damage figure `--fs4` -> `--fs5` to keep it the loudest of the four. ⛑ **THE TEXT EDITOR REACHES A THIRD KIND OF HOVER.** `tePick` read `#gtTip` (rebuilt live on each hover) and a `title=` attribute; a `.pop` card - the slot, skill and wagon-row tooltips built with CSS `:hover` alone - was neither, and could not even be reached by a click (moving toward the panel loses the `:hover` that was showing it, same trap #196 solved for the live tip). It is found rather than caught, because unlike `#gtTip` it sits in the DOM the whole time; `popbox` joins `TE_KINDS` and the kind-switcher already built for text/tip/title picks it up for free. ⛔ **"THIS BLOCK WITH PERKS" WAS THE SKILLS LIST, NOT ANOTHER PERKS BLOCK** (clarified: the SKILLS section - ARMING SWORD, KICK, HOLD THE LINE - printed on the sheet's always-visible left column, under the doll, saying "what they can do" beside gear the player was already looking at). It moves into the PERKS tab, first, above the tree in both its branches (a class-less hire's trade pick and the ordinary perk list), so the one place this sheet talks about what a body can DO is the tab that also sells the next thing it will be able to do. Measured on all 16 body x state cases: the left column's worst-case slack went **38px to 156-236px**, none clipped, none overlapping. ⛔ **A MID-ROAD EVENT NOW STOPS THE WALK FOR GOOD, INSTEAD OF RESUMING ON ITS OWN** (clarified: change the behaviour, not fix a bug). `leg()` breaks a multi-day road into stops and used to hand `rollCampAt` a `next` that always continued to the following stop the moment a card was answered; it now tells apart a QUIET tick (`rollCampAt` calls `next` synchronously, nothing was drawn) from a CARD ANSWERED (`next` arrives later, off a button) with one boolean, and only the second one pauses - `G.moving` stays true the whole time (so the token stays exactly where `walk()` left it and every other node stays inert) and `G.walkResume`/`G.walkDest` are the one thing `travel()` checks before its own guard, so clicking the SAME destination again is the one click that gets through. Simulated with `walk`/`rollCampAt` stubbed for both shapes: a quiet tick never pauses; a card answered pauses with `G.at` unmoved, and a second click on the same destination finishes the leg and arrives. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor and clip `[]` on the sheet, the documented set on the battle · overlap `[]`, slack positive on all 16 sheet cases · map 0/0/0 · no em dash · `regress.js` 8/8 no ERR/FATAL/GUARD. | |
| 8f.295 | **#270 - THE NINE-ASK BATCH, AND ONE OF THEM WAS #268's OWN FEATURE ARRIVING WITH NO BUTTON ON IT.** *(User, 2026-08-31, nine numbered points off four screenshots, opening "another buch:".)* ⛔ **THE MID-ROAD PAUSE WAS A SOFT LOCK AND THE SHAPE IS THIS REPO'S OLDEST ONE.** #268 built the stop (*"party stops in the middle, click to continue"*), held `G.moving` true through it so the token could not drift, and put the resume in `travel()` ahead of its own guard - *"clicking the SAME destination again is the one click that gets through"*. It proved that by CALLING `travel(k)`. Nothing ever proved there was a BUTTON to call it with: `drawNodes` binds `onclick` on `st==='live'&&!G.moving`, and measured on the shipped build with the pause forced, the map had **28 nodes and 0 with a click on any of them**, the destination included. The destination is a third state now (`waiting`, not `walking`) because the second one's plate reads *· on the way* and its own design note says *nothing on the map is clickable*: it is lit like `live`, its plate says **· press to go on**, it takes the click, and `leg()`'s pause `say2`s one line instead of stopping in silence. Driven through the button: 1 node clickable, the resume fires, `G.walkResume`/`G.walkDest` clear. ⛔ **AND THE MENU REFUSED THE ONE SCREEN HE WAS STUCK ON.** `menuBtn`'s world branch said *Finish what you are standing in first* on `G.pending||G.moving`, written when `moving` meant actually walking - so the #268 pause, which holds it true while nothing happens, was exactly where the door was shut. The guard was never protecting anything: `loadRun()` normalises all three fields on the way back in, and #131 already re-offers an unresolved card off `G.openEv`. `MENU_ON` gains **`spoils` and `epilogue`** (`promo` is not added - nothing calls `show('promo')`); the report ASKS first and clears `pending`/`openEv`/`queuedEv` the way `afterPromotion` does, or a player leaving mid-report would be offered the fight whose haul they had just banked. ⛑ **#268 DELETED THE WRONG ONE OF TWO IDENTICAL-LOOKING RAILS.** (*"The line near portrait when i was picking guy was good. But the line near each portrait on the left - i didnt want. You did it wronhly - can you please revrse and do right"*.) `.rmem.on` drew a 3px gold rail on the PICKED tile; #249 drew a 2px race-hued inset on EVERY tile. *"it works only when character selected"* was a description of the first and a complaint about the second. The race rail goes, the selection rail comes back - **as a `box-shadow` and not the old `border`**, because a border ate the content box and stepped the portrait 1.5px right the moment it was picked (measured: `.rbust` at 12.5 from the tile edge against 11 elsewhere; both read 11 now). ⛔ **THE REPORT STOPS EXPLAINING WHAT THE CARD UNDER IT ALREADY SAYS** (*"Don`t need extra explanation about wen down and killed (screen) - because below ther is explisit infomation"*). `hurtTeach`'s spotlight is off behind `HURT_LESSON=false`; `crewCards` prints `hurt · rattled · 10d` on the body it happened to and the sub-line says `1 OF 4 CARRIED OFF`. ⚠ `hurtWarn` is untouched: that one fires on the FIELD, where nothing else says so. And the aftermath's chips drop the resource WORD (`SPOILS_RES_WORDS=false`) - `◉+55 ❦+2 ▤+6`, the same glyphs the bar this card is about to change has always carried. ⚠ Scoped to that renderer and not to `.fxl`, which is shared with the road card and the contract receipt. ⛑ **A SECOND CAP, ON A DIFFERENT AXIS: FOUR RATKIN** (*"In the party maximum 4 ratkins (write then - if more they will kill each other)"*). The wagon's cap is SEATS and a ratkin costs one of twelve, so folding this into `COST` would have PRICED the fifth out instead of refusing it. `RACE_MAX`/`RACE_FULL`/`raceRoom`/`roomTag` are one table and one derived phrase, read by `hasRoomFor` (so every event door greys with *four ratkin is as many as will share a fire* instead of *no room left*), by the muster row, by `pickChoice`'s two recruit branches and by the clash's allies. ⚠ The returner at a fire and the body who walks out of the sinkhole are NOT gated: both were already in this company. ⛔ **AND ONE OFF THE WALL** (*"Давай ограничим наем токо 1 воина из трех в пункте наема"*). `HIRE_LIMIT=1`, the rule is on the card before the press (*They will let you take one of them*), and the second-hire surcharge's two lines of prose come off with it - `hirePrice` and its constants stay as the way back. ⚠ **Caught by driving it**: the first cut left `hirePrice(r,G.hiredHere)` on the dead rows, so a 72-crown stranger read **108** on a row that can never be bought - a promise nothing pays, in the one place this card is arithmetic. ⚡ **THE TOP MOOD RUNG BUYS A PAYDAY** (*"When the mood is maximum ... your guys can skip payment 'travel with you is too good'"*). `fx.freePay` on HIGH SPIRITS, read by `passDays` and printed under the rung by `moodEffect` like every other figure on the row. Taken AFTER the day's accrual so the waiver zeroes what is standing rather than doubling the next one, and never over `behind` - a company owed three days does not stop being owed because it cheered up. ⚠ An `else if` and not an early return: `passDays` is a loop over days. Measured: HIGH SPIRITS spends 0 on a payday, AT EASE spends 7. ⛑ **THE WAGON GROWS A SECOND LADDER AND A FIFTH FITTING.** THE RACKS (`RACK_COST` 4/8/14, `RACK_SLOTS` 2/3/4/5) is a slot cap that can be UPGRADED, which is the thing #238's *"the money is the cap"* could never be; THE KEPT CRATE takes one bolted fitting into the next run, pinned by pressing it (`◆ KEPT` / *this is the one that rides on*), written to `LEGACY.carry` only by a company that BANKED - `toDefeat` walks to the wagon with `bankIt` false and its own card already says the haul is lost. ⚠ Only the FRAME is `wide`: seven tiles with two full-width rows measured 28px past what `.bd` can show, and the last row landed under the fold. ⚠ SLOTS and not RACKS in the header, caught on the screenshot - *RACKS 3 OF 3* beside a rack on tier I is the wrong unit for the number. ⛑ **AND THE HOLES IN THE GROUND ARE AN OBSTACLE AGAIN** (*"If you use terrain with pits use at least more then one ... And also feel fre to add other stuff here and there"*). `digPits`' floor is 2 (was 1), the ridge's ceiling 3, the cluster grows off a RANDOM dug hole rather than always the first one, and `spoilHeaps` lays what came out of the hole beside it - at most one per hole, three a board, and only where the hole keeps THREE open neighbours after the rock is there. Swept over 200 seeds: **63 boards of two holes, 63 of three, none of one**, rubble 0-3 (avg 1.17), worst open neighbours of any pit **3**. All of it inside the `TF.pits` guard, so #250's claim that no ground without the dial can shift is untouched. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor and clip `[]` on the sheet, the documented set on the battle · sheet overlap `[]` and slack positive on all 16 body x state cases, byte-identical to a `git show HEAD:` baseline driven to the same screen · map 0/0/0 · no em dash · `regress.js` 8/8 no ERR/FATAL/GUARD. **Eye check:** `shots/270_roster.png`, `shots/270_wagon.png`, `shots/270_pause.png`. | |
| 8f.296 | **#272 - THE ROAD KEEPS ITS OWN STATE.** *(User, 2026-08-31: "I want system of good and evil in the world. That world have states (as in deamon sols (evil, neutral, good))", with a screenshot of a segmented arch, "So far just track it and show, don`t make any active changes in the events around it", and "it should be clear, where is good and where is bad (for example bloody knife and pigeon as oposite)".)* ⛔ **THE ASK CONTRADICTS THE CONCEPT DOC'S OWN PILLAR IN THREE PLACES, AND SAYING SO WAS THE FIRST DELIVERABLE.** `01_GAME_CONCEPT.md`'s *"You cannot afford to be good to everyone"* - the section line 34 calls *"the yardstick every event is costed against"* - says **"No karma meter; the company is the conscience"**, **"Consequences are personal, never global ... there is no meter"** and **"A deed changes the future options, not a bar"**. The ask is a bar, with none of the changed options. ⛑ **THE RESOLUTION IS NOT A COMPROMISE, IT IS A DISTINCTION THE USER HAD ALREADY MADE**: every one of those three lines scores a COMPANY, and Demon's Souls tendency scores a PLACE. His answer to the question was one word, **road**, and a new section beside the pillar draws the line in a table and states what has to happen if the arc ever pays. ⛔ **NOTHING IS AUTHORED AND NOTHING NEW IS SAVED.** `G.deeds` is #224's tally, already derived off every door's own `ico` and already in the save blob - so this reads a fact the build has kept since 2026-08-21, works on a save made yesterday, and a card written next month joins it untaught. ⛔ **THE FIVE VERBS WEIGH NOTHING AND THE DOOR VOCABULARY IS WHY.** `.claude/skills/event-cards/SKILL.md` states *"the verb comes first and the moral colour second"*, so `fight`, `take`, `trade`, `rest` and `leave` carry no weight: `ROAD_W` is `evil -2, help +1, honor +1` and nothing else. Measured over the shipped content, **167 doors across EVENTS and CAMPS, of which 15 are evil, 20 help and 12 honor** - which is what makes ±5 a cap a run can actually reach. ⛔ **TWO DOORS EITHER WAY TURNS THE ROAD, AND THE FIRST CUT GOT IT WRONG IN CAPITALS.** The gate was written `w<=-2` under a comment claiming *"ONE door never turns the whole road"* - and a cruelty weighs -2, so a single evil door hit that gate exactly and turned the road black on its own. **Only driving the eleven rungs said so**; the gate is -3, the first weight two cruelties reach and one cannot. ⚑ The asymmetry is the point: in DOORS the two gates are even, while a cruelty still moves the needle twice as far, so three of each reads **BLACK** rather than grey - the pillar's own *"bad deeds must actually pay"* on the readout that watches them. ⛑ **THE MARKS AND THE COLOURS ARE BOTH READ OFF THINGS THAT ALREADY EXIST.** The two end glyphs are `CHOICE_ICO.evil.g` and `CHOICE_ICO.help.g`, so the mark on the arc is the mark on the button that moved it (the user picked this over the dagger and dove he had asked for, once both were drawn); the three colours are #218's own door rails, `--blood-lit`, `--ore` and `--grain`. Two fresh glyphs for a meaning ☠️ already carries would have been #102's wrong-unit bug arriving on the one screen that had escaped it. ⛑ **IT SHOWS A STATE AND NEVER A FIGURE**, which is `ui-scales.md` §3c's law, and the HOVER holds the line too: it names what has been DONE (the same counts `deedTally` prints at the end of a run) and never the weight they add to. Its last line is *"It changes nothing on the road. Not yet."*, which is load-bearing rather than modest - a readout that visibly moves and pays nothing gets filed as broken. ⚠ **WHERE IT SITS IS WHAT THE BAR ALLOWS, MEASURED.** He asked for the top right corner; `#wRes` ends at 1148 and `#wWagon` takes 1156..1194, which is the whole of the 86px the button group reserves, so the far right is spoken for. The chip takes the right-hand end of the middle air, **831..942 hard against the chest**, and travels onto the sheet with the other readouts through `show()`'s #239 reparent - before `#wRes` on the map, after it on `#iBar`, which is the outer end of the readouts on each. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` on the world and the sheet, clip the documented set · overlap `[]` · map 0/0/0 · emdash false · `regress.js` 8/8 no ERR, no FATAL, no HIT GUARD · the chip 111x32 inside the bar on both hosts, `0,0` overflow, **0 collisions** with anything beside it, bar right edge 1194 against its 1194 cap · the hover 290x177, `0,0`, not offscreen. **Eye check:** `shots/272_road_black.png`, `shots/272_road_grey.png`, `shots/272_road_white.png`, and the pre-build mockup at `shots/272_alignment_mockup.png`. | ⏳ Nothing reads it, by design. The three rulings are **W1** to **W3** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md). |
| 8f.297 | **#273 - THE ROAD'S STATE TAKES THE ZOOM ROW'S CORNER, AND THE ZOOM ROW GOES.** *(User, 2026-08-31, hours after #272: "i want it here - instead of zoom - zoom can remove from the screen (keeping functionality on map scroll)".)* ⛑ **THIS IS #269's OWN EDIT ONE SCREEN ACROSS AND IT COST ALMOST NOTHING FOR THE SAME REASON.** That entry deleted `#bZoom` from the battle screen on the identical argument (*"living option to zoom in and zoom out with your mouse"*) and left `camBar()`'s `if(!bar)return` guard doing the work; `wcamBar()` has carried the same guard since it was written, so removing `#wZoom` changed **nothing** that calls it. `WCAMS`, `wcamSet`, the three stops and the remembered `gt_wcam` are all untouched, and the row is one line away if it is ever wanted back. ⛔ **THE WHEEL AND THE DRAG PART COMPANY, WHICH THEY DID NOT NEED TO WHILE THE ROW EXISTED.** `wpanInit` excluded `#wZoom` from BOTH gestures. With the button row gone the wheel is the **only** way to zoom, so a 111px dead patch in the corner would now be a hole in the one gesture that is left: the wheel steps over the chip (measured `2->1` with the pointer on it) and the DRAG still skips it, because a press on a hover target is reaching for the tooltip. The card's own exclusion (#206's, the one real scroller on the road) is untouched and still reads `1->1`. ⛔ **AND THE HOVER WOULD HAVE DIED SILENTLY.** Tips are bound by `querySelectorAll('#wBar [data-w],#iBar [data-w]')` - a selector whose own comment, written in #239, is about exactly this failure - and the chip is now in neither bar. It is named directly in that line. ⚠ **A COMMENT OF MY OWN WAS STALE WITHIN THE HOUR AND THE MEASUREMENT CAUGHT IT.** The first cut of the markup note reasoned from #220 (*"`#menuBtn` sits at 82 BECAUSE `#wZoom` ended at 75"*) - and that has not been true since **#249 deleted the world override** (*"the world's override IS the default now. The rule is deleted, not moved"*). Driven on the live map, ☰ MENU is on the bar's own row at **y 6..30** and the chip is **50..78**: 20px of air, 0 collisions over the corner's five boxes, and the note now carries the reading rather than the inference. ⛑ **IT COVERS NO NODE AND NO NAME PLATE AT ANY OF THE THREE STOPS**, which is #240's `audioBox` check one element across - the fault that probe exists to catch is a corner widget grown over the board beneath it. ⚠ **THE COST IS THE SHEET**: the chip was in `show()`'s #239 reparent and is not any more, because it is positioned against `#wMap` and a chip that travelled onto `#iBar` would be leaving the screen it is measured against. **THE COMPANY SHEET NO LONGER SHOWS THE ROAD'S STATE**, which is one `insertBefore` to undo and is filed as **W4**. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]`, clip the documented `#wMap` · the three stops intact `road:1 near:1.22 close:1.5` · wheel over the map `1->2`, over the chip `2->1`, over an open card `1->1` · drag starts from the map and not from the chip · hover bound, 290x177, `0,0`, not offscreen. **Eye check:** `shots/273_road_corner.png`. | ⏳ The sheet lost the readout (**W4**). |
| 8f.298 | **#274 - THE TWELVE-ASK BATCH: THE LIZARDS FIGHT, THE TONGUE STOPS HITTING EMPTY GROUND, AND THE SKILLS COME HOME.** ⛔ **THE THREE AI COMPLAINTS WERE ONE LINE AND THE MEASUREMENT IS WHAT SAID SO.** *"while they where able to move and hit me - they where standing"*, *"Slug hides doesnt use their abilities at all"* and *"Salamandra ... dont go in front by themselves"* all come off `disposition`'s shooter test. #267 gave the salamander a SPIT at three hexes and the slag-hide a TONGUE at three, both carrying `dmg`, so a warm-stones side counted **four shooters against your two**, took the hold stance, and came out `rooted` with `shotInRange=false` and the nearest of you **ten to twelve hexes away**: they stood at the far edge of the map refusing to close on the strength of a weapon that cannot touch what they are refusing to close on. ⛑ **`outRanged` IS THE PREDICATE AND FOUR IS MEASURED RATHER THAN PICKED.** Swept over every ranged damage act in `FOE_BUILD` and a prepared six: **range 3 is the spit and the tongue and nothing else**, range 5 is every bow, sling and crossbow, and the three `warp` castings are `arcane` (always a standoff). So one line moves the two lizards and leaves every shooting fight in the game where it was. It is read in THREE places and stays one predicate - the stance, `rooted`, and the cohesion weight - which is #240's own *a body counted INTO a stance has to be held BY it*. ⛔ **AND THE COHESION CLAUSE IS THE SAME SENTENCE FROM THE OTHER END.** `cohere` exempts a SHOOTER from caring about the line, and `iShoot` said yes to a three-hex spitter - so the salamanders were the one body on the field with NO cohesion and ran out in front of the animals they came with. ⛑ **A beast also keeps station on the heavier beasts of its own side** (`herdBig`, `hpMax*1.4`), which is the ogres' `herd` with the question asked of the BODY instead of the race, scoped to `beast` so the dog, the Warden, the mirehares and every measured ratkin fight are untouched. ⛔ **And the band lets go while the shot is cooling**: the tongue is `cool:1`, so on every other turn a band of 2 read off that same tongue parked the animal one hex outside its own BITE with nothing it could reach. `onCool` and not `canUse`, or every archer would lose its band on the turn it walks. ⛑ **"SLUG HIDE STARTED TO ATTACK EMPTY TILES" IS A COORDINATE, AND ONLY DRIVING IT SAID SO.** Measured on the warm stones with the victim two hexes off: the body stood at pixel **(1086,127)** and finished at **(1295,197)**, and the `hitflash`, the blood and the −N were all appended at **1086,127** - the hex the tongue had just emptied. Everything that says MOVEMENT was correct; everything that says a blow landed was painted on empty ground. `paintBlow` defers those three and nothing else, **in the same tick** (all of `strike` runs in one), so `hexPt(d)` answers with the hex the drag put them on; `say` stays outside it so the log's order is untouched. Re-measured: flash at **780,479** against a body at **780,479**. ⛑ **AND THE PERK TREE HAD BEEN SHOWING ON THE STASH TAB SINCE #264.** `.pk2{display:grid}` is 0-1-0, exactly what `.itabbody{display:none}` is, and it sits LATER in the stylesheet - so the whole tree was stacked under the stash on every sheet in the game, which is the screenshot the ask was reported with. Neither §5 counter can see it: nothing clips and nothing overlaps, the column is simply twice as long as it should be. `.pk2.on` closes it, and it is the identical trap the note on `#iStashBody.on` four blocks up already spells out in capitals. **SKILLS go back into the character column** (#268 read *"this block with perks move in perks section"* as covering both blocks; they are two questions - what a body CAN DO is true of a level-0 hire and is read beside the gear that decides it, what it MAY LEARN is a decision that wants the tab's width). ⚠ **It costs the column 127px, measured over all 16 body x state cases: worst slack 141.8 -> 14.8, scroll 0, overlap 0, clip 0, floor 0.** The next thing added to that column takes room from something. ⛑ **THE ENEMY CARD SAYS WHAT AN ACT DOES** (*"Have no idea what this gone do for them"*, on ✧ TONGUE / ✦ BITE). `noteLine(a)` is the sentence the player's own act popover has printed since #91 point 9 and this card was the one surface that did not; `!own`, because your own acts carry it on the card you press and printing it twice on one screen is the second-receipt rule. ⛑ **AND THE BOARD ANSWERS *WHOM COULD I HIT FROM THERE*.** #234's figures are measured from the hex the body is ON, which is the whole of a shooter's question left unanswered; `paintHand` now stands the body on the hex the step is aimed at, asks `hitBreakdown`, puts it back in the same tick, and prints the figure in the live one's slot with a dotted underline. **One figure on a hex** - #98 point 7's own *two numbers on one hex want a hierarchy*. It asks whether the shot is still affordable after the step, so a two-action stride carries none, which is the truth and is the same fact ask 12 hands the turn over on. ⛔ **#247 REFUSED THE UNDO ACROSS THE HAND-OVER, #251 PUT IT TO HIM, AND HE HAS NOW ASKED FOR IT.** The refusal's reason was real and is answered rather than ignored: *"running `beginTurn` twice on somebody - two morale ticks, two poison ticks, two banked actions"*. A turn handed back is **RESUMED** now (`turnHeld`, above every tick and below `B.seq++`), so nothing ticks twice; what #247 could not do was the guard, not the offer. **The 3.15s hold is deleted** and the turn goes at once. Driven: after the stride `cur()` is the next body with `B.undo` still standing and the button live and NOT draining; ⟲ puts the mover back on its own hex with both actions and the turn with it; and when the order reaches the interrupted body again its **mood is unchanged at 63 and its actions are 2**. Plus: **the enemy's two washes trade weight** (the arm is `.34` and the legs `.17`, because the question a player acts on is the OUTER ring and it was the fainter of the two on every board) · **the padded jack is UNCOMMON** (⚠ price 45 -> 70 and break 10 crowns -> 25 crowns + 2 salvage; `gearPrice` reads the HDA_CUT armour, so both figures are the tier FLOOR) · **five armour points to a section** (`SEG_ARM` 3 -> 5, so both pools are sectioned in the same unit). **Priced, `runFight` n=15 a cell against `git show HEAD:`, and the lizards are the census:** emberpatch/four 100->93, /prepared 100->100 · warmstones/four 93->73, /prepared 100->100 · glassroad/four 53->60, /prepared 100->73 · ashdrakes/four 13->0, /prepared 100->60 · furnace/four 27->7, /prepared 67->93. **The two cells that moved 20+ were re-read at n=30 and both held**: ashdrakes/prepared **93 -> 63**, glassroad/prepared **100 -> 83**. ⚡ **The road fights read as NOISE and n=30 disagreed with n=15 on the sign**, which is this project's own *n=6 cannot tell 0% from 20%* arriving again: slingline/four read 40 -> 27 at n=15 and **20 -> 53** at n=30, snare/four 80 -> 67 then **80 -> 87**; clash identical both ways at both sizes. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented `#bField`/`#bLog` on the battle · sheetOverlap `[]` · map 0/0/0 · emdash false · `regress.js` 8/8 no ERR, no FATAL, no HIT GUARD. **Eye check:** `shots/274_sheet.png`, `shots/274_perks.png`, `shots/274_peek.png`, `shots/274_step.png`. | ⚠ **Ask 10 (*"when I hover on enemy - i stopped seeing automatical atack on range atacks"*) could not be reproduced** and is **X2** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md). ⏳ The lizard fights are 20-30 points harder now that they fight (**X1**). ⏳ The undo dies at once when the next body in initiative is a foe (**X3**). ⏳ The character column is down to **14.8px** on its worst body. |
| 8f.299 | **#275 - THE TWO STACKING CAPS, THE DISENGAGE THAT IS NOT THERE, AND A CLOAK.** ⛔ **`DODGE_SOFT` WAS 12 AND A STARTING BODY ALREADY DODGES 14**, so the cap was biting every body in the game on the day it was rolled: it was a flat tax on the stat rather than a ceiling on stacking it, and a player who spent a level, a shield and a trait on footwork was handed 60% of the third thing they bought. **25 is above what a fresh body brings and under what a built one reaches**, which is where a soft cap belongs. The to-hit side had no cap at all, which is why the ask names both in one breath: **`hitOf` is `dodgeOf`'s shape** - one function, a collector for the hover, the cap applied LAST to the sum - and the plaque, the enemy card, the company sheet and `hitBreakdown` all read it, so the figure a player sees is the figure the roll uses. `HIT_SOFT` is 100 and it is the BODY'S rating, not the whole roll: an act's `aim` is a fact about the weapon and is added after, the same way the target and the ground are. ⚠ The raw `u.mskill` stays raw, written by a dozen `+=` lines and read by `u.hitWhy`; capping it in place would leave the hover unable to say what it took off. ⛔ **AND THREE READOUTS PROMISED A CARD MOST BODIES HAVE NOT CARRIED SINCE #224.** DISENGAGE became a PERK and the brawl's CIRCLED lesson - the FIRST teaching card in the game, on a Captain who has not got it - still said *"DISENGAGE first"*, with its own spotlight falling through to the whole action row; so did HELD GROUND on every hex in a zone of control, and so did the ⚔ chip that prices a step off. `hasDis` asks the ACT ROW and all three read it: driven, the lesson now says *"Nobody here has learned to step out of a grip yet"* and the note says **Two ways out** where it said Three. ⛔ **THE TWICE-A-TURN TALLY WAS BEING READ AS A PENALTY AND THE CAPTAIN WAS CONFIRMING IT.** `learn_twice` said *"Doing the same thing twice in one turn is always the weaker half of it"*, which is true of MOVE (a hex shorter) and **of nothing else in the game** - `u.used[k]` is a CEILING. So a player swinging twice watched a card count 1/2 then 2/2 with the damage and the odds unchanged while being told something had got worse. The lesson says what the tally is, the card's own receipt line says `· 1 OF 2 THIS TURN` beside the cost and the cooldown, and **the register's own note - which defended that lesson on exactly the claim that turned out to be false - is corrected**. ⛑ **A HOVER BOX MAY NOT SIT ON THE LESSON.** `#tutDim` is z-index 61 and `#gtTip` is 99, and the tip lives on `document.body` (`moveTip` clamps it against the window), so a HELD GROUND or PALISADE box opened a second before a card fires paints over the dimmed sheet - reported as *"HELD GROUND repeatedly covered the central battlefield while tutorial cards were also active"*. One line in `tutPaint`, which is the ONE function that puts the sheet up and writes `B.tutLock`. ⛑ **AND THE VERY FIRST SWING OF THE GAME LANDS** (`TUT_FIRST_SURE`, one word to switch off). `SURE_AFTER` already exists for the player's frustration and cannot reach the first swing because two misses have to happen first; the brawl only, one body's first blow only, decided inside `hitBreakdown` for SURE_AFTER's own reason - the card reads 100%, the roll cannot miss, AUTO knows. ⚠ **THE FIRST CUT OF THAT TEST READ `a.hits` AND `a.misses` AND A DRIVEN PROBE CAUGHT IT**: both are incremented inside personality clauses (`hitLift`, the ogre's rage), so on an ordinary Captain both stay 0 all fight and **every landing swing in the brawl would have been a guaranteed 100%**. `a.swung`, set at the top of `strike`. ⛔ **THE DISGUISE CLOAK IS A BAG ROW AND FOUR NAMED EXITS.** Uncommon = `rare` (the vocabulary fact the draught's note already records), 70 crowns, in the rare-bag find pool beside the draught and the shuriken. One action, once a fight: **nothing on the other side may aim at it at all** - which is what makes it a second field rather than a second meaning on the lizards' `veil` (*cannot aim from further than a hex*). It comes off when you swing or anything lands on you (`strike`, the one blow door), when something with an INTELLECT rung of 2 or better ends up beside you (`spotAround`, off `beginTurn` AND `walkTo`, i.e. it was already there or it has just walked up), when something walks into the ground you are standing on, or after two ROUNDS (#46's own ruling on venom: a tick clock lasts a different length of time depending on how many bodies are on the field). ⛑ **THE WALK-INTO IS THE ONE THAT NEEDED ENGINE WORK, AND IT IS TWO LINES IN `reachMap` PLUS THE BUMP IN `walkTo`.** `passable` refuses an enemy body outright, so the hex had no `dist` to keep and *"triyng to take the place where your disgies character stands"* could not happen at all; it is entered as a destination and **not expanded from**, so a route never runs PAST the cloak, only ever ends on it - and `walkTo` refuses the last step, drops the mover on the last hex it actually crossed and reveals. **Driven, all six**: unaimable at 15 hexes, aimable at 1 hex from an INT +3 body, NOT aimable at 1 hex from an INT -1 body, holds no zone of control while still crowding, off on a swing, off on the bump with the mover moved, not on top, and **one body on the hex**. ⚠ **The dodge cap was NOT priced over the road matrix, at the user's word** (*"maybe dont do it that pricacly with dodge cup through battles - it is your unit stat"*); the baseline half had been taken and is in the session record if it is ever wanted. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented `#bField`/`#bTrait`/`#bLog` on the battle · sheetOverlap `[]` · map 0/0/0 · emdash 0 prose · `regress.js` 8/8 no ERR, no FATAL, no HIT GUARD. **Eye check:** `shots/275_cloak.png`. | ⏳ The dodge cap is unpriced against the road (**Y1** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)). ⏳ The brawl's free first swing is a taste call (**Y2**). ⏳ A hidden body still counts as CROWDING for the surrounded bonus (**Y3**). |
| 8f.300 | **#276 - THE FOE DEX, THE STATBLOCKS COME HOME, AND THE THREAT PRICE IS RE-DERIVED.** **The foe dex, the statblocks come home, and the threat price is re-derived.** ⛑ **`tools/enemies.html` is one table of every enemy the game can field** (62 bodies, 20 fights): the battle token, the four ladders and every figure `build()` actually makes of the statblock, the skills with MOVE and a champion's two ROLLED PERKS, the threat price as a share of a starting body, the whole cast that stands beside it, the cards that start the fight, and a one-sentence hover line. **Derived off the running build by two probes and a generator**, so a statblock change moves it by itself; its one authored file is the hover line, which does not exist in the build. Edits live BESIDE the row and export, so a regenerate can never cost anybody an afternoon · ⛔ **the seven remaining statblocks moved into the ONE block** beside `FOE_T`, finishing #263 at the user's second asking of the same question; **their essays stayed with their plan functions** and a pointer stands at each old site. **Proved by running the oracle TWICE on each build**: four kinds drift against THEMSELVES (`steading`, `snarejoin`, and `ashdrakes`/`glassroad`, which the rule file only ever named two of), and on the sixteen deterministic kinds the cross-build diff is empty · ⚡ **five defects in `unitPts`, every one of them a readout**: `PTS_SCALE`'s own comment claimed the four founders sum to ~100 and they summed to **37** (HDA_CUT cut both halves of a geometric mean); a caster's damage was clamped to a flat **26** that HDA_CUT never touched, so all three casters priced off it against raw dpr of 7/7/14; every act was weighted by `mskill` when **a working is aimed with the INTELLECT** (arc 41 on the one-word ogre, 76 on the sniffers); a cooled act priced as if it fired every turn; and burning ground, `bounce` and `veiled` were worth nothing. Fixed with **every constant measured off the build** - the dial re-derived AFTER the other four (**1.275**, and NOT 0.42x100/37, because the flat 26 was making Marrow the second most valuable body in the company), `PTS_ARC_THROUGH` **1.35** off `(hp+.8*armour)/hp` on the two reference companies, the ember term off `EMBER_HIT`/`BURN_TICK`/`BURN_TURNS`. **Priced every body and every fight against a `git show HEAD:` baseline**: pure rescale x3.04, the lurcher and the runt **+15 to +21%** (`bounce`, which is their entire design), the one-word ogre **-23%**, both warp-sniffers **-29%**; every fight within 7% of the rescale except the pack (+15%) and snarejoin (+10%), the dogs both times. **The bands could not move and did not**: `simBands` is ratios of the same net · ⚡ **and the dex's orphan half found six paintings and three statblocks nothing can reach** - THE DESERTER, SPEAR (cut from `brigands()` in a balance pass, statblock and painting both still there), the FEN-THING and the BLOOM-SPITTER (two statblocks, three paintings, a bestiary row, a map sight and two wide stages, for a fight nobody wrote), `ogre_you_guardian`/`ogre_you_maul`, plus EVJ9/EVJ28, MAPEV10/MAPEV28, the `hire` art and six item icons. **All of it proved by DRIVING the cascade and the builders**, and three of that probe's first findings were its own blind spots (a `slot` name `GEAR` does not use, no body above level 1, and it could not build Asha): **a filter that matches nothing looks exactly like a finding**. **Gates:** `LINT()` **0** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented `#bField`/`#bTrait`/`#bLog` on the battle · sheetOverlap `[]` · map 0/0/0 · emdash 0 prose · all 20 fights start and field the right cast. **Eye check:** `shots/foedex_enemies.png`, `shots/foedex_fights.png`, `shots/foedex_groups.png`, `shots/foedex_orphans.png`.  | ⏳ the 55 hover lines are written to the battle-screen spec and are NOT wired into it · ⏳ three statblocks and six paintings are finished and unreachable (👤 **B18** in [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)) · 👤 **B19**: 5 of 8 ogre statblocks have no SWEEP and two different bodies are both named "Ogre, club" · ⏳ the salamander is still priced on its bite (biting twice scores 9.0 against 8.25 for a spit plus its ground), which is the formula answering honestly rather than the ember term failing |
| 8f.301 | **#277 - ONE CREATURE, AUTHORED ONCE.** **One creature, authored once, and a fight names only what differs.** (User: *"to merge same units (ogre club and ogr clan-hired). It is one unit in two contexts. Same with smaller rats. I prefere to handmade specific characteristics in the specific battle, rather then havin multiple entieties"*.) ⛔ **#263 put the statblocks in one registry and #276 put them in one block, and neither stopped the same creature being written out three times.** A RATKIN SLINGER was authored in the snare, the clash AND the sling-line; the clash's OGRE, CLUB and the snare's OGRE, CLAN-HIRED are the same animal wearing two names; and the Hold hound's own comment already read *"Same lurcher the deserters run, better fed"*. **`FOE_BASE` is the creature and a fight's row says `from:'<base>'` plus only what that fight tunes** - the ask in one line. **13 rows became 5 bases and 10 deltas**, the longest of which is five numbers and the shortest is `{from:'lurcher',bare:true}`. ⛑ **`foeInherit()` expands them once, at declaration time**, so `build`, `LINT`, the dev bench, the foe dex and the oracle all go on seeing a complete row and not one of them had to learn what `from:` is; a resolver called from `build()` would have to be called from six places and forgotten by the seventh. `acts` wins WHOLE, which is `t.st`'s rule one field across, and the bases are chosen so exactly ONE member of each family restates a weapon. ⛔ **NOT ONE FIGURE MOVED, AND THE ORACLE IS WHAT SAYS SO - AFTER CATCHING THE ONE THAT DID.** The clash's slinger dodges 20 and its new base dodges 22, and the delta list left `dodge` out: a merge advertised as changing nothing moved one number on one body. That is the single failure mode of this whole shape - **a field you forget to list silently takes the base's value** - and nothing but a field-by-field diff of both sides can see it. Fixed, re-run, and **empty on all sixteen deterministic kinds** (the four that roll their own cast are told apart by running the oracle twice a build). ⛑ **LINT 8k holds the other end**: a base nobody inherits is a statblock the game cannot field and nothing in a run would ever say so, which is 8h's finding on the statblocks. `foeInherit` leaves `_from` behind for it to read. **Proved by making it fire** - a scratch base reported, removing it returned the linter to silence. ⚡ **And the foe dex folds the same five creatures into five rows**: the primary is the body the base is NAMED for (sorting by price alone put the Hold hound at the head of the lurcher family, the tail naming the dog), and each context folds under it in teal showing ONLY what its fight tuned - *THE BROKEN MEN / Lurcher / HP 8->7 - armour 0->1*. ⛑ **A redraw checkbox sits under every unit picture** (*"near unit images create the checkbox - weather or not it needed to be redisigned"*): it is an EDIT like any other field, so it survives a reload, rides EXPORT as a named `redraw` list an artist can be handed straight, and RESET EDITS clears it. Writing it into the row would put an art decision inside a derived file and lose it on the next regenerate. ⚠ **`LINT()` returns a COUNT in `.findings` and the list in `.items`**, and this session's own gate probe read `(LINT().findings||[]).length` for two entries - which is 0 whatever the linter says. A vacuous check reads exactly like a passing one. **Gates:** `LINT()` **0 findings, 0 items** · `gt.py check` 3 scripts 0 problems · floor `[]` and clip `[]` on the sheet, the documented three on the battle · sheetOverlap `[]` · map 0/0/0 · the four founders **101** · all 20 fights start and field the right cast. **Eye check:** `shots/foedex_contexts.png`.  | ⏳ the steading's four ogres are NOT in the merge: three of the eight ogre statblocks have SWEEP and five do not, and two different bodies are both named "Ogre, club" (👤 **B19**) · ⏳ the Bitch stays her own creature - an aura and a crest is a different animal from a lurcher · ⏳ the four dogs were merged without being named in the ask, on the strength of the Hold hound's own comment; one line each to back out |
| **NEXT** | **The plan lives in [`00_PLAN_AND_BACKLOG.md`](00_PLAN_AND_BACKLOG.md). Nothing in this file is next.** | - |
| 8g | **First 15 Minutes Pass - DONE.** The consequence numbers are gone and the linter exists; the stat wall went with THE ARITHMETIC (8f.26) and the first battle now sells screening and facing (8f.29's three tougher ogres). | ✅ DONE |
| 8h | Painted portraits P1–P5 + three event illustrations | |


## 8f.105 - the removed send block, kept whole for restore

*Here because 8f.105 removed a working feature to test a guess, and a guess that turns out wrong
must cost one paste to undo. This is the code exactly as it stood at 8f.104, in
`prototype/grimtoll_slice.html`, inside `drawNotes()`. Restoring it means putting all four pieces
back and deleting the `nSend` button and its handler.*

**1. The address, above `const POLL=[`:**

```js
const FEEDBACK_TO='dmytriyvihrov@gmail.com';
```

**2. The href, after `const L=own?letter():null;`:**

```js
/* built once so the length can be checked before the anchor is written */
const mailHref=(()=>{
  if(!L)return '';
  const full='mailto:'+FEEDBACK_TO+'?subject='+encodeURIComponent(L.subject)+
    '&body='+encodeURIComponent(L.body);
  if(full.length<=1900)return full;
  return 'mailto:'+FEEDBACK_TO+'?subject='+encodeURIComponent(L.subject)+
    '&body='+encodeURIComponent('RABBLEBOUND, playtest notes'+
      (JOURNAL.who?' from '+JOURNAL.who:'')+
      '\n\nIt was too long to put in the link. It is all on my clipboard. Pasting it here:\n\n');
})();
```

**3. The markup, where the `nSend` button now sits:**

```js
'<a class="popt sendit big" id="nMail" href="'+esc(mailHref)+'">'+
  '<b>✉ SEND IT TO ME</b><i>opens your mail addressed to '+esc(FEEDBACK_TO)+
  ', with the answers already written · the journal goes on your clipboard in the '+
  'same click, paste it under the line</i></a>'+
'<p style="margin-top:11px">No mail on this machine? Copy the box and send it to <b>'+
esc(FEEDBACK_TO)+'</b> any way you like. '+
'It is plain text — nothing in it but what is on this page and what you typed.</p>'+
```

**4. The handler, where the `nSend` handler now sits:**

```js
const mail=$('nMail');
if(mail)mail.onclick=()=>{
  try{ta.focus();ta.select();document.execCommand('copy');
    if(navigator.clipboard&&navigator.clipboard.writeText)
      navigator.clipboard.writeText(ta.value).catch(()=>{});}catch(e){}
  const note=mail.querySelector('i');
  if(note)note.textContent='the journal is on your clipboard. Paste it under the line.';
};
```

**And one line inside `letter()`**, whose tail read
`'\n\n----------\nThe full journal is on my clipboard. Pasting it below.\n\n'`
before the clipboard started carrying both halves at once.


## Built backlog entries - the full text

**Moved on 2026-08-21 to [`archive/CHANGELOG_BUILT_ENTRY_TEXT.md`](archive/CHANGELOG_BUILT_ENTRY_TEXT.md)** (220 KB, roughly #1 to #123, all shipped). It is a file rather than a `git show` so the reasoning stays greppable; see that file's header for why.
