# RabbleBound - the plan and the backlog

> **This file is the WORK.** What we are doing now, what waits, and one line per entry.
> The full spec of any entry lives in [`archive/BACKLOG_ENTRY_SPECS.md`](archive/BACKLOG_ENTRY_SPECS.md).
> ⛔ **Open the one entry's section there, never the file: it is 199 KB.**
>
> Orientation is [`README.md`](README.md) · the record is [`CHANGELOG.md`](CHANGELOG.md) ·
> what shipped is [`SHIPPED.md`](SHIPPED.md) · your test bench is [`WHAT_TO_TEST.md`](WHAT_TO_TEST.md).

---

# 🔴 THE CURRENT FOCUS

> ## ⛔ 2026-08-22: THE "NOTHING IS ADDED" RULE IS LIFTED. THE FOCUS IS FINISHING THE BUILD.
>
> The user, 2026-08-22, while ordering #238 (the wagon): *"and change the rule add no more, i get 7
> feedbacks, waiting for 3 more. Need to sinish build"*.
>
> So: the rule below (*nothing is added; cut, delay, merge*) no longer decides an argument, and an
> entry is judged by whether it **finishes the build**. Everything the clarity pass found stays as
> the measuring habit (measure the box before the font, a sign is not a readout, the receipt is
> derived) and as history; it is no longer the veto. The first entry under the new rule is **#238**,
> and its spec still passes the gate the old way: rules, a picture, then code.

> ## ✅ 2026-08-25: **THE TEN-FRIENDS GATE IS PAID. TEN PEOPLE HAVE PLAYED IT.**
>
> The user: *"10 people played, I slowly implement feedback."*
>
> ⛑ **So the gate below stops being a thing to reach and becomes a thing that is RUNNING.** The
> question it was built to ask - *does it survive somebody who is not you* - has been asked ten
> times, and the answer arrives as a queue of feedback rather than as a verdict. **What that changes
> for this file: an entry earns its place by being on that queue, not by an argument made here.**
>
> ⚠ **And it retires the phrase *before the friends play*, which is the sorting rule 🟡 NEXT and
> ⚪ LATER were both written against.** Neither bucket means what its heading says any more: NEXT was
> *"still before the friends play"* and LATER was *"after the friends have played"*, and both of
> those moments are now behind us. **Read them as cost, not as time** - NEXT is what is small and
> finishable, LATER is the depth pass that is still deliberately not started.

> ## The feedback, 2026-08-10, in the user's words
>
> > *"The biggest thing: lack of clarity and too many options/systems from the start."*
>
> **And the ruling that came with it: *"I think this version we will work only on it."***
>
> ### The second round arrived the same day, with a screenshot of the battle panel
>
> > *"1) What is hard - understanding what is going on even in the menu - very small type and lots
> > of numbers everywhere. 2) It took me a very long time to see where to read a character's step
> > count - also hidden in small type. 3) Make the hot keys either smaller or not overlapping."*
>
> **#88 shipped against all three on 2026-08-10** *(8f.117, see [`CHANGELOG.md`](CHANGELOG.md))*, and
> the finding is worth carrying into the rest of the pass: **all three were one measurable cause.**
> The skill list was a two-column grid, a 133px column leaves a card 116px of inner width, and the
> longest skill name in the game wants 128px of it. **The card was structurally unable to show its
> own contents, so the type could not be raised - there was nowhere to put it.** One column gave it
> 213px and paid for all three fixes at once. ⚑ **The lesson: "the type is too small" was a
> SYMPTOM of a box that was too narrow. Measure the box before you argue about the font.**
>
> **⏳ MORE OF THE FEEDBACK MAY STILL COME.** Until it does, the two blocks above are the whole
> brief, and the work below is scoped to them.

## What that sentence actually says, and it is two complaints, not one

| | the complaint | what it means for the build |
|---|---|---|
| **A** | **lack of clarity** | the player does not know what is happening or why. This is a **legibility** problem, and it is fixed by explaining less at once, not by explaining more. |
| **B** | **too many options / systems from the start** | the player is handed the whole game in the first fifteen minutes. This is a **pacing** problem, and it is fixed by **taking things away from the opening**, not by adding a tutorial on top. |

**⛔ The rule for this whole pass, and it decides every argument in it:**

> ## NOTHING IS ADDED. THE ONLY MOVES ARE CUT, DELAY, AND MERGE.
>
> *(⛔ **LIFTED 2026-08-22** by the ruling at the top of this section. Kept as the record of the pass.)*
>
> A new tutorial, a new hint layer, a new card explaining the cards: all three are the wrong
> direction, because the report is *too much at the start*. If a system cannot be cut, it gets
> **delayed** until the player has a reason to care about it. If two things say the same thing, they
> become one thing.

**Why this outranks everything else in the file.** The build is feature-complete for one act and has
survived a full QA playthrough with no soft locks. It does not need more. It needs the first
fifteen minutes to stop being a wall, and that is a subtraction job.

## What the pass has shipped, and what it taught

*One line per entry, newest first. **[`SHIPPED.md`](SHIPPED.md) is the registry** and it names every
open remainder; the reasoning is in [`CHANGELOG.md`](CHANGELOG.md) under the build-log number. This
section used to carry each entry in full, which made the work file a second changelog. Cut back on
2026-08-14.*

| | the subtraction | log |
|---|---|---|
| **#265** *(08-28)* | **the other side breaks less easily, and a champion is four levels on**: `FOE_NERVE` 1.25 as ONE knob (the road's pool goes -37.5% to -21.9% against yours), a multiplier rather than a flat add because the authored pools run 24 to 120 · a champion keeps #245's multipliers and gains two rolled perks and one DERIVED rung · `foePerks()` PAYS the eight build-time perks a foe could never have been paid for, rather than banning them ⛔ **and the polishing sweep found `snarejoin` at 0/20 on a fight tuned to 40-53%** (30% before #263, 15% after, 0% after #265); three bills out by its own documented dial, back to 40% | 8f.290 |
| **#264** *(08-28)* | **the race lanes fill out**: 1/1/3 becomes **4/4/4** with no new engine code, on keys whose lines #261 left standing. The perk list runs in **two columns**. ⛔ **`armourMax` already read `stoneskin` and I wired it again**: +20 armour on a card that says 10, caught by differential (34 -> 54, LINT 0 with the bug in). Restoring a retired perk is restoring its ROW, nothing else. ⛔ **GOOD HAND unchanged: there is no throwable in the game**, so the asked-for version pays nothing to anybody but an ogre. 35 skills, 11 class / 12 race / 12 general. | 8f.289 |
| **#263** *(08-28)* | **the enemy climbs the same four ladders**: every foe already carried `st` and it was the SAME four numbers for all 61, so `thews` had been paying every body in the game off a rung nobody chose · a foe's rung is `FOE_LEAN[race]` + its own `lean:{}`, and hp/skill/dodge/mood read it · the 14 scattered statblock tables are one `FOE_T`, proved at 0 drift by a 19-field oracle · the four `STAT_ICON` marks on the inspect card (`noFace` refused) · `statRung` reads `u.stMod`, the seam battlefield buffs will use · ⛔ **the givens are NOT shared and are now measured**: an ordinary foe sits -18% hp, -16.7 to hit, -27.3 nerve under the party | 8f.288 |
| **#262** *(08-27)* | **the tree lands**: 28 skills, **11 class · 5 race · 12 general**, each with `kind` driving both the card's mark and the pool's gate. Eleven restored from `RETIRED_SKILLS.md` onto engine lines #261 left standing. A RACE lane is new - `perkPool` had only ever asked about a class, so race skills were structurally unreachable. ⛔ **Teaching the pool is not teaching the sheet**: the renderer walks `CLASSES[p.cls].perks` directly and drew 2 of 17 until `perksAt`/`tiersFor` became the one reader for both. Every skill differential-measured; THICK MUSCLES swept over all six facings (plain +5/+15/+30, perked 0); six real fights with all 28 on every body. ⏳ Seven lab skills still need new acts. | 8f.287 |
| **#261** *(08-27)* | **the old tree comes out**: 25 perks retired to `docs/archive/RETIRED_SKILLS.md` (generated, with each one's tier and paying line) · DISENGAGE becomes a tier-2 perk and leaves the basic act row · DUELLIST to +10 hit / +5 dodge, one question asked at two sites · every card wears a DERIVED belongs-to mark (the class's own glyph, or 👥 for general). ⛔ **One line would have crashed the company sheet**: `threatsAt` reads `B.units`, B is null outside a fight, and the sheet calls `dodgeOf` on every body - the same guard #259 wrote for `wallBlocked`, missed one entry later and caught only by driving the screen. ⏳ Tiers 6 and 8 are empty until the tree's own skills land. | 8f.286 |
| **#260** *(08-27)* | **the first three general skills**: EVERCHANGING LUCK, BREAK AWAY, SHIELDWORK, open to every class. ⛔ **Luck goes in `effStats` and the obvious door was wrong**: at `unitFrom` it pays to-hit and dodge and leaves hitpoints alone, because `bodyHp` re-derives from the body (measured 66 -> 66 against 66 -> 75). The roll is taken once at the top of the fight, worst or best only (200 rolls: 105/95, no middle stat). BREAK AWAY has one reader for its hex count; SHIELDWORK doubles the shield's own value. | 8f.285 |
| **#259** *(08-27)* | **the promise audit**: every perk is READ and every build-time figure matches its own text to the digit (`ironjaw` +12, `stoneskin` +10, `thickhide` +18, `quickfoot` +1, `conduit` 1 -> 1.2, `opportune` 15 -> 25, `eagle` +2 by a second code path), and `STATLADDER` pays exactly what it prints on **all nine rungs, 0 mismatches**. ⛔ **What lied was the class PICKER**: captain sold RALLY THE LINE, archer AIMED SHOT, cutter BACKSTAB - none of which `unitFrom` builds, all three replaced long ago with the pitch left behind. Held now by **LINT 6h**, off a DECLARED `sigActs` compared against the engine, never parsed out of the prose. ⚠ **The first proof reported 0 three times and the instrument was the bug** (`LINT()` returns an object; the probe read `.length`). ⚡ Plus SPEARWOMAN -> SPEAR and COMMAND at level 4. | 8f.284 |
| **#258** *(08-27)* | **the Gauntlet reads what it used to carry**: the strategies come off the build's own `ORDER_PROF` (#242's hand-synced duplicate, caught before it drifted, and `defensive` arrives derived - which is what that entry marked it FIRST-CUT for), the founders' calibration is TAKEN rather than asserted (**it reads 108 against `PTS_SCALE`'s stated 100**), and a build picker makes `ui-scales.md` §5's second-tab baseline pressable. ⚑ **Its first use is the finding**: founders vs **THE SNARE 30% → 100%** between 8f.252 and today, confirmed on `ARENA` (**1/6 → 6/6**) against that fight's own tuning note of ~3 of 10, with the Sling-Line going the other way (100% → 60%). 👤 Three rulings in `OPEN_QUESTIONS.md` §O | 8f.283 |
| **#257** *(08-27)* | **every promise on the four ladders pays**: the mood POOL reaches the sheet (110 vs 90 drew an identical bar) · a census that drives each rung found **five more printing and not paying** - MORALE's `loss` and `heal`, STANDS, UNREPEATABLE and TWO PERK POINTS · 18 of 18 now pay | 8f.282 |
| **#256** *(08-27)* | **the three remainders of #253 and #254**: one weapon printed three damage ranges and labelled none (`landsFor` is the one reader) · INTELLECT +3’s reach and MORALE −4’s *one rung lower* both paid nothing and now pay | 8f.281 |
| **#255** *(08-27)* | **six asks after playing the ladders**: the mood ladder moves the bar on every rung (80/90/100 → 50..130) · `RACESIZE` makes SMALL and BIG a fact about the race and retires the `big` personality · *to land a skill or magic* · seats 12 · born is not rolled · ⚡ **and two findings**: 24 stat modifiers were still on the old 1..20 scale, and #254's class lean had never reached a hired body | 8f.280 |
| **#254** *(08-27)* | **the four ladders go in**: `STATLADDER` is the decision page made into the engine - nine authored rungs a stat, a two-point capstone, and STANDS / FLEET / UNREPEATABLE on the unit · the givens, the race's pool and weight of blow, and *to be hit* moved onto the attacker · the trade leans a rung (the fourth named line) · TO HIT and DODGE explain themselves · priced in the arena over 7 fights ×15 | 8f.279 |
| **#253** *(08-27)* | **the ordinary human is 0 on every axis**: `p.st` becomes a signed rung by an origin shift, so every delta in the game is untouched and the 32 absolute sites move - **byte-identical derived numbers on both builds** · the stat hover's ladder says what each rung is worth, computed off `D` and `thews` · the race and the class come back to the sheet header · the #204 decision page is archived | 8f.278 |
| **#252** *(08-27)* | **a stat is a sum, and both surfaces that print one show it**: the sheet's stat hover carries a ledger beside the ladder (`born · levels · race · gear · personality · scars · the harness that does not fit → the figure`), off **`statParts`, which IS the walk `effStats` used to do** - 244 bodies, 0 drift against HEAD's own re-typed walk · ⚡ the ledger sits BESIDE the ladder because under it the tip fell off the board, **and HEAD already did on MOR** · `shots/204_ladders.html` regenerated as a model that renders itself, which caught three typed numbers that were not derivable | 8f.277 |
| **#251** *(08-26)* | **seven small fixes**: the contract card's **+88 CROWNS / +3 HEADS chips are deleted** - prose and receipt were the same event a second apart - and its door takes the shoe beside the scales · the sheet's **header mark line goes** (all of it is on the roster tile since #246/#249; `xpHover` moves with it) · **the three pools take the battle card's 20/`--fs5`**, free on the worst body because the tells are the taller column there · **the doll is a body**: hands either side of a raised chest, trinket and bag centred under · **the stash reads two up** · ⛔ the **undo report was not reproducible** and the real fault was the click being swallowed, so the window is a maximum now and any board click ends it | 8f.276 |
| **#250** *(08-26)* | **three asks**: a **pit** is a walkable ground whose rim nothing physical crosses - shoved in is 10 through any armour, walked in is free, getting out is the turn, and no spell is worked from down there. On `rocky` only, in the back half of the map, both gated off the map's own shape. **The practice field is shut until the road has been walked**, off three flags the save already had. **The mood float is one triangle at three sizes** instead of an arc and a count. | 8f.275 |
| **#249** *(08-26)* | **fourteen asks**: the three pools are sockets with the figure on the bar's own midline, 20 and 16 tall, and ⛔ **#247's stated reason for the 22px floor is wrong** - the `/max` never voted, it is the display face's 1.27em content area, ladder `13->17 · 15->21 · 17->23` · the mood mark stops being a colour emoji (**B1**) · ⛔ **the movement wash went the OPPOSITE way to the ask**, measured over the board's own ground: *darker* reads 74 against the far half's 72 and inverts at 59, so the near half went UP and the gap is 19 points against 8 · the turn ends itself again, on a 3.15s hold with a drain bar · ☰ MENU in one corner on all five screens · the roster 148 -> **80** with the class on the portrait and `▲` for unspent (**B10**) · six gear slots in two rows, **88px banked for the perks** · the wagon is the map's cart · 12px between blocks · ⚡ a banked stat point squeezed all four stat values to **0px wide on HEAD** | 8f.274 |
| **#248** *(08-25)* | **the maintenance pass**: the one thing in the last ten entries recorded as UNRESOLVED is closed - the arena's `DRAW/STALL HIT GUARD` is a HARNESS defect and never was the game. A spotlight card goes up mid-fight, `nextTurn` takes its `tutLock` door, and a synchronous loop can never let `later()` release it; a player clicks the card. ⛔ **The flag that prevents it existed, said in its own comment that the harness sets it, and the harness never did** - #202's finding one layer down - and it sat on `tutFlush` while `capSay` reaches the same sheet down a fourth path, so the gate moved onto `tutPaint`, the one door both end in. Trapped with a `defineProperty` setter rather than argued; **4 of 4 stalling before, 0 of 6 after** · the guard DUMPS its state now, which is what #239 asked for and could not find · `tutNext` stops handing a WORLD-TOUR step to a battle · the three scale counters are closed again · the concept, README and lore book stop saying `nerve` and `provisions` · 878 doc em dashes swept and **gated in `record.py check`** · 61 one-off probes deleted and the keep-or-delete test written down | 8f.272 |
| **#247** *(08-25)* | **fourteen asks**: each pool is one row - a mark, a taller bar, the figures inside it - on the battle card and the sheet off one `.sbar` block, which swallowed a third private hitpoint ramp · the card's health is red on everybody and the bars over the heads are untouched · ⛔ **three asks were features that had shipped and could not be SEEN**: the far half of the stride was one click since #246 and still refused the ring (57 lit against 59 not), UNDO after a two-action move was real for **140ms**, and ☰ MENU has been on the battle screen since #130 underneath the character card · OPTIONS with the two sound switches moved into it, out of every corner · the roster tile is a portrait, a class mark and a clock, 176 → 148 measured against the body that wraps · ♥ −9 · CLICK TO WEAR · 0 KILLED · a coin breaks down into a coin | 8f.271 |
| **#246** *(08-25)* | **twelve asks**: the two-action stride is standing and clickable (#244 built it and only the card nobody clicks ever wrote it) · the zone of control stops pricing steps you cannot take (27 arcs, none in reach, at the Snare's opening) · the roster tile is a name and two marks, 224 → 176, the 48 to the stash · a MOVE blinks its cost off the hex under the pointer · the target card loses the act sentence that described your own weapon · hover wait 2200 → 1400 · chips lose their words · rarity on a stash row · the mood tip's drift rate, derived · ⚡ a 25x performance scare that was the loaded machine, settled by COUNTING CALLS rather than the clock | 8f.270 |
| **#245** *(08-25)* | **eleven asks, and three of them found things that were BUILT AND UNREACHABLE**: the mirehares had never once been dealt (`f2` was authored with `ev:'mirehares'` AND listed in `SLOTS_ON_MAP`, 0 survivals in 20 deals) - it is a fixed place now and **LINT 8h** asks whether a painted key can ever reach a node, which caught `wynn` on its first run · #192's item pack was specced, painted and embedded and **no row was ever pasted**, so the BAG has four items now to that spec's own numbers · the enforcer finale had drifted to 53% against the honest finale's 27% and the preference was a regression report (40% now, off +2 bills; the champions did not move the win rate and the entry says so) · champions are five multipliers on a plan row · a ringed body is not left alone · PROVISIONS → SUPPLIES off one table · the days sit on the road · the muster and market re-open · a zone-of-control note · Hoom costs six supplies | 8f.269 |
| **#244** *(08-25)* | **sixteen asks, by screen**: the zone of control is the shared SIDE, not the region's outline (44 segments to 17), and `.backer` - the part-circle under the sprite - goes with it · `moveBudget2` puts the whole stride on the board with the second action's half at half alpha (34 near / 41 far / 0 far clickable) · the plaque's pips blink what a picked card would spend · `spriteHurt` composites blood INTO the sprite with `source-atop`, three tiers, cached per picture, so it lunges and flips with the body · the dev KIT bench: 55 artifacts and 54 derived skills onto any body mid-fight · `START_NERVE_YOU` .71 against the foes' .78 · the dirk is +5 hit / +5 damage behind a blade and a CARD behind a bow · ARMOUR NONE · `3 KILLED` shut, heads open · `+2 TO A STAT · PICK A ROW` · no hover on a taken off hand · `Let them go.` · ⚠ two asks were not bugs: the working's price WAS charged and was being said twice, and the road's mood drift is #161's and had never been printed | 8f.268 |
| **#243** *(08-25)* | **eighteen asks, by screen**: the company's mood was **`Math.min(0,G.morale)`** and could only ever subtract, so the two happy rungs paid nothing - every rung carries an `fx` block now (-15 / -10 / nothing / +10 / +15 on the starting nerve, +10 hit / +10 dodge / +10% xp at the top, 5% a day that somebody walks off at the bottom) and the map's hover is the whole ladder · **the board was framed before `render()` built the grid**, so the first fight of a page session opened at tx 515 instead of 33 and stuck there · `camBand` gives the hand the camera back at FULL (91x120px) and clamps every stop to the ground · the zone of control is the OUTSIDE of the set (44 segments of 84) instead of a red wash · MAGE with the globe alone · +3 HEADS on the contract · dodge `%` · no stash nag · a red BREAK DOWN · the stave reaches 2 · provisions -30% at `pay()` · rations mend the injury clock too · a wagon objective off a `GOALS` table · the roads ahead measured back to visible (1.27 to 1.51 against walked 1.77) · the sheet's MOOD bar was a literal 78% · the tutorial fight off the front door | 8f.267 |
| **#242** *(08-24)* | **the dev bench** (⚙ only): a hand-picked OTHER SIDE off a 53-body catalogue DERIVED from the fight builders · a perk editor over the level policy · ORDERS for both sides with AUTO as the autobattle · a board brush writing the terrain's own words. The twin fight-to-builder chains merged into `FOE_BUILD` · ⚠ **a LINT check keyed on a function's SOURCE dies with the refactor of its subject**, and a wall must be seeded PER builder run or the catalogue flickers | 8f.265 |
| **#241** *(08-24)* | **the map is wider than the fight**: an apron of 1 hex above, 2 below and 3 either side, painted, unmarked and unwalkable - **padding on `#bGround` plus a bigger canvas, not hexes**, so nothing joined the pathing or the 195 buttons · the camera clamps to the GROUND box and rests on the PLAY box, which keeps FULL pixel-identical and lets the two closer stops push a rank clear of the chrome (bottom rim: the skill cards go from 10 and 9 playable hexes to **0**) · the painter keeps its origin, the apron gets its own seeded stream so a repaint cannot reshuffle it, and the rim fades to nothing at the field's edge · `APRON_LATTICE` is the one value that draws the band's hexes if the ask meant visible tiles · ⚡ **round two (8f.266)**: the paint was one apron off its tiles because an absolute child is laid out against the PADDING box, which every gate in the project read as clean - `align243.js` measures paint against tile now · 2 hexes left / 4 right, a 48px paint bleed, and a harder rim over a fixed 84px | 8f.264 + 8f.266 |
| **#240** *(08-24)* | **nineteen asks, by screen**: a shot stops getting the flanking bonus (the card's `surrounded` row was HONEST, so the rule was what was wrong) · a death costs a FRACTION of the line it started as, so a big fight stops cascading and a company of five is unchanged · the sling-line actually holds, and the first cut priced it at +400 which is +0 when every candidate pays it · the Fen-Mother comes when the cub is hurt, once a POCKET FIX stopped overwriting her anchor · a spear reaches round the corner of a Г, because `hexLine` returns one of two routes · one mood float a beat, and it is the total · the ogre throws one of yours AT somebody · fire 6/3 and `EMBER_AI` 14 -> 60 · THE STEADING-LINE counts the ogres it will field (`c:` may be a function now) · three event-only artifacts flagged `unique` · the kit `!` fires on arrival instead of standing true all run · music and sound are two switches · the hover card stops being floored by a row it is not over · the sheet's stat rows lose their labels (and Skree's and Bruht's stop clipping) · four stat pictures a stranger on the muster wall · the crossbow's winding pays for ONE discharge and an archer holding one gets PUNCH THROUGH · the end-of-run screen is THE OLD CAMP | 8f.263 |
| **#239** *(08-24)* | **twenty asks, by screen**: salvage halved above 3 at ONE door (a run holds 23-24 at the last fire against 42) and the material list merged out of five appliers · xp x0.8, a gem pays a level's WORTH of xp, and a level is worth +5% hp / +1 hit / +1 dodge by itself - which forced `bodyHp` and caught the sheet printing an archer 50 against his plaque's 45 · mercy per kneeling head · the rout offer waits a full round · a beast keeps its tag and loses its face unless `boss:` · a body is drawn holding what it is holding · the tap-room gives you the barman and nobody else · the goal node fades and its trophy does not · a blow in hand stops the WALK preview competing, which is the whole reason a spear's reach was invisible · the shops roll one common / one rare / one epic at a derived price and Coldharrow buys in one click · the road bar's readouts follow you onto the sheet · the two-hands rule finally implemented · BREAK DOWN at 10-16% of the buy price | 8f.262 |
| **#238** *(08-23)* | **the wagon comes on the road**: ⛔ salvage had **no in-run use** - it was banked at the epilogue, so every `+N ▤` on a road card was a receipt for nothing this run. One card from the bar: THE FRAME at 6 · 12 · 24 ▤ for +1, +2, +2 seats and four fittings at their shipped prices, as picture tiles, grey meaning cannot-afford and green meaning BOLTED IN. Per-run (`G.wagon` rides the save, `PARTYCAP` derived at load), `FIT_SLOTS` retired because the money is the cap. Deleted: the village wheelwright row and two between-runs stations · ⏳ **the weak joint is `PARTYBASE`, not the wagon** - a seat upgrade is only candy when the cap binds | 8f.261 |
| **#237** *(08-22)* | **the new world map, and the five permanent places stop being abstract marks**: the painting replaced at 2560x1276 (#233's 1:1 match kept) · a SECOND art table keyed on the NODE - `site:` -> `MAP_PLACE` -> `placeFor` - at **64px against an event's 96**, because a place is not an encounter and because `hold` has no `ev` for `sightFor` to read · the Last Muster written out as a second explicit key rather than left as the one abstract glyph among 27 paintings · `build_assets.ps1` embeds `MAP-*` where it embedded `MAP-EV*`, which is what wired four sources that had been sitting built on disk | 8f.260 |
| **#236** *(08-22)* | **the damage figure comes off the body and the bar learns to say *maybe***: `.hdmg` deleted, `.hodds` into its slot on every foe in reach (y 6.5 -> 16.5, chest not head) · the preview band split SOLID minimum + HATCHED window, which is #234's own rejected min/max split becoming necessary the moment ask 1 deleted the figure that carried the pair · the gear hover says `TIER · WHERE IT BELONGS` (TWO HANDS included) · no epic armour and no stave FOUND until past Blood on the Road, derived off the rows and anchored to a node with `LINT 8g` · Marrow's off hand starts empty | 8f.259 |
| **#235** *(08-22)* | **the working file stops carrying 27 MB of base64**: 30.2 MB -> **3.4 MB**, because 88% of it was two GENERATED art blocks nobody reads and everything paid for - every page load, every grep, every scripted edit. They are `art/embed/art_data.js` and `art/embed/j_pack.js` now, tracked, loaded by two classic script tags, and `build_site.ps1` pours them back inline · ⛑ **nothing about what ships changes**: `index.html`, the player build and the itch zip are still ONE self-contained file, measured at **350 `data:image` payloads against the shipped page's 350** · ⛑ **not one line of game code changed**, because a top-level `const` in a classic script is a global lexical binding · `inject.ps1` and `inject_j_pack.ps1` stop touching the prototype, which retires the claim gate, the whole-file-rewrite hazard and #209's reason for a second injector · ⚠ `art/embed/` is tracked and must stay tracked, or a fresh desk gets a game with no art · ⛔ two self-inflicted bugs, both syntax inside a comment that ends the comment, and **the gate grew to fit**: `gt.py check` follows `<script src>` and strips HTML comments, proved by making it fire | 8f.258 |
| **#234** *(08-22)* | **the board answers again, the blow is drawn on the bar it comes out of, and the sheet's header pays for its own air** *(seventeen asks in two messages)*: the hit % back on whatever is already in reach (`offA`, i.e. #91's fall-through inside #231's gate - the ask's own *"without movment"* is what overrules it, and `udist` was always measured from the hex you stand on) · **the damage drawn on the armour and health bars**, `dp.arm` and `dp.hp`, one body at a time, ⛔ **lighting the doomed run rather than darkening it** because the empty track is already dark and the first cut read as a bar that was that short · the banner 3px further right (an EDGE anchor, so the overhang stays constant at every cut) · **SHAKEN flies again** at .34 against Breaking .55 and Broken 1 · **the hand on you breathes red while a step is weighed**, `actorpulse` in red on the same `aiming` gate the price rides · ⚑ **ask 6 was already true and is now measured** (foe's turn and mid-walk: 0 aiming, 0 zodds, 0 zhit, 0 odds, 0 bands; `paintHand()` runs at the foot of `render()`) · a forced-movement verb names itself over the body that swung it · the chase's ratkin swaps the DOCK HOOK for a SHORT KNIFE (the Snare keeps the only pole) · **Skree carries the bow again**, three fields and no art, #201's own bill paid · the brawl's fifth man holds the doorway one round (`holdTill`, `!engaged` so he still swings back) · the Captain's trait is **Experienced**, +1 STR +1 MOR, condition dropped, with `authored:true` as the second sanctioned exemption beside `foeOnly` · CUT A GEM hidden on an empty pouch · ⛔ **the sheet's header paid for itself**: `ON THE FIELD` measured **85.2px** of vertical caption against the 58px token it labelled and was what made the row 95.2 tall, so deleting it bought the portrait 104x80 (store 208x160) AND `#iChar{gap}` `--p2`->`--p3`; against a `git show HEAD:` browser driven to the same screen, **all sixteen body-and-state cases have more slack than HEAD and the two that scrolled 15px now scroll 5** · every item name in its tier's colour through one `rarityName()` at five surfaces · the three weakest BRAINS to 18/21/24 wide, because a height-normalised family let its width cap make the four weakest pictures the four widest | 8f.257 |
| **#233** *(08-22)* | **the new world map is canon and seven portraits are repainted**: ⛑ **the map is 2560x1276, a 1:1 match to the 2x canvas store**, so the NEAR stop is a downscale for the first time - #223 recorded that the blur *"only new art moves"* · same aspect to four places, no node moved, `build_assets.ps1` unchanged (it embeds verbatim and reads its key out of the prototype) · **the dim was re-measured**: 22 / 46 / 84 spread 62 against #223's 27 / 61 / 96, darker at every quantile and closer to the median 48 the plates were designed for, so the filter stands · seven portraits to their handoff's hard guards, **POR-07/08/10/R05 byte-identical**, all 220x220, and **12 of 12 embedded payloads byte-compared to disk** | 8f.256 |
| **#232** *(08-22)* | **the pool is called MOOD, the Captain gets his own trait, and the stat block is re-laid out of its own hole**: NERVE -> MOOD on 48 player-facing surfaces (internals deliberately unchanged) · the ladder cut to its figures, ⚠ **and the sweep caught two shipped lies dead since #228** (the reference listed six rungs with `Ok`; `feral` promised *Wavering*) · the banner flies at **It rocks only** · a mood hover that says what moves the pool, every figure read off `MORALE{}` · `CLASS_ICON.captain` = ★ · **SLEEPS IN THE HARNESS** (+1 STR +1 MOR, **-2 MOR under HEAVY**), the condition in `effStats` beside `sizeMismatch`, ⚠ `bandKey` lifted 6,800 lines to dodge its own TDZ · ⛔ **the drive line moved into a 55px hole that was there on every body** (bars 69 vs tells 124), **31px back** · ⛔ **`.itl .tvw` was `flex-end`, so the WORD's length decided where the ICON began**, 46px apart on the mage · `.iact` 78→90 with the damage in flow like `.act` · THE BODY deleted, SKILLS renamed, the field token to the right of the header, the short sword out of the stash. Crew slack 14.6→32.4 | 8f.255 |
| **#231** *(08-21)* | **the board answers when it is asked**: the hit % only while a skill is picked or hovered, the damage only on the body the pointer is on, both **20% smaller through `--fs0`** (a measurement: the battlefield is scaled, so 8 design px is 13.1-20 REAL px) · the opportunity-attack price is **one figure under the body that pays it**, `1 - Π(1-p)`, text and not a plate · the mood flag 12px anchored to the token's right edge, 3.7px righter with 0 overhang at any width · the build stamp on the bottom row left of FEEDBACK, one rule for twelve screens (⛔ the corner itself has a four-pixel clear band) · the hover card stops at the top of the skills row, off two measured numbers instead of a literal 300 · WHAT IS ON IT deleted, its rows kept | 8f.254 |
| **#230** *(08-21)* | **the stat icons land: four families, nine grades, 32px**: 36 paintings embedded verbatim, HANDS/BOOTS/BRAINS/HEARTS on str/agi/int/mor, in TELLS order (strongest first, the pack ships the other way) and **verified byte-for-byte against both the file and the word**. One `<img>` builder, `statIcoAt`; the hover ladder's own copy had drifted. ⛔ **the row went 17.4→33 and the column had 30px of slack, not 53** - the worst case is a CREW member, not the Captain, because his dismiss foot is 2px against their 25 - paid in `--p` steps off `#iChar`, `.itl` and `#iStats`. ⛔ **`#iBody` pinned `flex:0 0 auto`**: it was silently shrinkable and the doll PAINTED OVER the skills label, which §5's clip counter skips by design; **HEAD ships that at 12.6px** on a CIRCLE body with a point banked | 8f.253 |
| **#229** *(08-21)* | **the Gauntlet, the campaign battle bench**: `tools/gauntlet.html` live-loads the build into a hidden iframe (the dramaturge's rule: the game file is the only source) and runs every fight through the game's own mirror-AI loop - a bench for any company at any level, and a campaign walk where the founders march the road with XP paid by `payFightXP` and the joins editable. Each cell is win% / rounds / down / hp lost under three ally strategies, which are `AIP.you` profiles and nothing new: THE BRAIN (all 1), AGGRESSIVE (the rush with its aim kept), STEADY (`cohere:3`). Seeded, with a Δ column against the previous run, so "update perks -> see what changed" is press-reload-run. ⚠ **A tool, not a player system** - the same licence as #216, and not one line of the prototype changed. First findings: the #216 bands mislead both ways, the strategies split per fight (STEADY holds the palisade 100% vs 60%, AGGRESSIVE eats the Broken Men 90% vs 70%), and a fifth body or a third level can FLIP the Sling-Line from 10/10 to 3/10 (real, cross-checked vs ARENA; mirror-AI caveat stands) | 8f.252 |
| **#228** *(08-21)* | **the flag moves off the head, fades until it is all the way out, takes every nerve readout with it, and the ladder loses a rung**: the pole sits on the token's own 18.5 centreline so the flag flies to the RIGHT, **1.7px inside the edge** and no more · ⚡ the captain's star never constrained it (it owns top 13, the flag is 14px from top -2, they miss vertically at every offset) - **the size was the constrained axis and the position was free**, which is the opposite of what #227's note suggested · solid only at the two ENDS of the ladder, halfway everywhere between · ⛔ **all six sites that printed the ladder's emoji changed through one builder**, because the plaque sits inches from the hover and one fact may not wear two vocabularies · **Ok merges into Happy as one free band .52-.90**, ⚠ taking the LOWER threshold or it would have been a nerf in a buff's clothes, with `u.state` moved in the same edit and 18 boundary points driven · ⚑ **`FLAG_SHOWN` followed six rungs down to five untouched**, because #227 derived it off `LADDER.length` instead of writing literals - #129's lesson paying out one entry later · **mean win 80.6% -> 79.8% over eight fights x 16**, i.e. neutral, ⚠ though n=16 cannot resolve the swings it reported | 8f.251 |
| **#227** *(08-21)* | **the road stops handing you a second one of what you have, a level-0 hire is half price and unarmed, and the morale flag is painted**: ⛔ **better armour was a 4% event** and nobody had chosen that - the rare pool is 12 rows with five trinkets and one mail in it - so the slot is rolled before the piece, nothing already held is offered, and a weapon has to out-hit the weakest one somebody swings; **4,000 rolls say find 30->46%, weapon 14->22.6%, armour 4->15.3%, duplicates 0** · ⚡ the advance test passed everything on its first cut because a caster's stave put the bar at a mean of 8 · the stranger goes .75 -> .5 and loses the cudgel (**41 -> 28 crowns**), ⛔ which costs him every attack he has (`if(main)` in the act builder) so the row says `comes with no weapon` before the press, proved by running all eight fights with him standing in them · ⛑ **the flag replaced TWO marks and only the code said so**, the mood emoji and the 🏳, or a routed man would have worn two white flags - #98's own collision on #98's own anchor - and **14px is a probed ceiling**, because the captain star is crossed at 15 | 8f.250 |
| **#226** *(08-21)* | **the build names itself, the seats mark joins the number it counts, and a walked road stops being transparent**: a `dev`/`8f.NNN` stamp in the stage's top right corner, ⛔ offset to row 42 on the three screens whose corner was already taken (map MENU, battle zoom, sheet BACK, all y 8..33 in a 42px bar) - which only a probe over every visible box on twelve screens said, because none of the three is near the others in the stylesheet · ⛑ **the number is the changelog's and not a commit hash**, because `deploy.ps1` builds before `git add -A` and a hash read at build time is the parent's · `SEATS_SVG`, two expands crossed and DRAWN because unicode has none, weighed against the digit it stands beside (30.1% coverage against the `8`'s 29.4) and moved onto its own figure: it had been 6px from the headcount and one space from the seats · ⛑ **a walked road is #194's node finding arriving on the lines between the nodes** - live 3.93, walked 1.79, *a road you cannot take* 1.29, so half a point separated the two that mean opposite things; the casing fixes it and not a brighter line, because the .55 alpha was tuned against a procedural near-black map before #190 painted a valley floor under it (**1.79 to 2.35**) · and **#222's own declared sweep was run**: three live `2>$null` sites, proved by making them fire, plus a guard `deploy.ps1` was missing - an unreadable branch silently PASSED the check that keeps a work branch off the live link · ⛑ **and the map's third counter was 3 and is 0**: `coin`/`chase`/`mirehares` had rows, exported masters and an updated build-script comment, and only the injection had never been run - the silent-failure shape #190 wrote down, found by running the gate rather than by anybody noticing three nodes wearing the abstract mark. ⚡ Its cost was measured BEFORE the 10 MB rewrite: the built art data diffed against the embedded block at **exactly three lines** | 8f.249 |
| **#225** *(08-21)* | **the injury ladder: a fall costs more every time, and the third one is the last**: backlog **#34** shipped on the user's own shape rather than the spec's, so it no longer waits on the unbuilt #4+#17 painter - the middle rung is a TEMPORARY injury with a clock (**sound -> injured -> scarred -> gone**, Captain two scars). ⛔ **`takeFall` is the one applier and `nextRung` the one answer**, asked by the fall, the badge under the feet, the warning card and the sheet, so the game cannot promise a scar and hand out a funeral. The **7-14 day** band was measured off all eight roads (22-26 days, 3-6 map battles, median five apart) and replaced the 5-20 that was guessed. Nine injuries, one penalty each, priced against DRUNK and CRIPPLED; ⚠ `despActs`' floor of 2 would have handed the DEAD ARM its swing back. ⛔ Two text faults, both found by DRIVING: the first warning card told a company somebody was scarred who was not, and `fst(proseName(p))` printed *"the is hurt"* for a Captain nicknamed *the Reasonable* - a fault that only appears on some runs. `p.deadBattle` and `.abcrew.dead` have been designed and unset since #187; `lostSoul()` gains #85's fourth state, so THE DREAM finally has a corpse to be about. ⏳ **AUTO still walks a last-rung body into a losing exchange, and `armourSac` hands one up the ladder without saying so** - both are #34's own spec and both are the next work here | 8f.248 |
| **#224** *(08-21)* | **seven small asks in one desk, and three of them were the file already having the parts**: the parting-swing chance moves off the swingers and onto the hex the step is aimed at (⛑ the HEX and not the mouse, because `paintHand` already folds the pointer through the MIRROR and a chip on the pointer's own pixels would price a hex nothing happens on · three figures then `+N`, measured - two is 71 design px against a 37px hex and six was **211**) · `chase` and `clash` roll `open` only with no fire and near-dry ground, ⛑ **named fights and never a counter**, because a rule phrased against PROGRESS through the run breaks silently on a reorder (#71) - clash 19 blocked hexes to 0, chase 40.3 to 13, and n=16 a side says neither fight moves · the wedding withdrawal is FREE until you kill somebody and impossible after, which makes a `NOWITHDRAW` row a FUNCTION and made the receipt learn to JOIN its parts (concatenated it read *"no spoils · −0 mood"*) · **THE SECOND SHAPE**, a trinket casting an illusion of its bearer: ⛔ not in `B.order` (the clock is the caster's, because *two turns after the cast* is a sentence about the caster) and ⛔ not a survivor as far as `checkEnd` is concerned, or a company whose last body fell with its own illusion standing would leave the fight running with no turn that could ever be taken · the kill tally becomes `race mark × N` off `RACE_ICON` - ⛔ the heads were a **fifth race vocabulary** that #196 and #211 both missed, printing ⬩ for a ratkin two inches under the painted mark on the same body - plus a paw for beasts, and it leaves the top of the sheet where #136 point 10 had already argued it did not belong · `G.deeds` counts every intent taken, derived from the doors' own `ico` the way #160's `sins` is, read out on both endings · **the crossbow**: two hands, shoves a man-sized body a hex, 31% adjacent against the bow's 5%, and one action on the crank - ⛔ with a branch in BOTH brains, because the shot greys itself and a company under AUTO would otherwise fire once and carry a stick for the rest of the fight (#128's spear wall exactly) | 8f.247 |
| **#223** *(08-21)* | **Nine small asks, and four were one sentence nobody had measured**: ⛔ the card's scroll was dead because #219 put `pointer-events:none` on the scrolling box (the advance bubbles to `.evstage` and never needed it), and #206's scrollbar rules never followed the scroll from `.bd` to `.evflow` · ⛔ **the ◎ CIRCLE chip's hover was 100% clipped** - 121px of a 117px box, by a 24px `#iLedger` - so a feature built in #136 had never been seen, and three more popovers were cut 61/21/21px by `#iChar`; the three hosts are `position:fixed` with one placer and `.lowpop` goes with the bug · ⛑ the map's flat wash cost **44% of local contrast**, so "blurry" and "less light" were one number: a brightness/contrast filter is darker AND sharper-reading, and the canvas is 2x because it was being enlarged 1.22 · the payday hover names every wage off `wageOf` · SEND returns on the 👎 branch only, reversing #191, because the engineering was right and the silence was the bug · ⤢ replaces the word SEATS, picked by rendering the candidates at 10px · ⚠ the MENU/♪ swap overran `#wRes` by 17px until the bar's reservation was measured against the button now in it | 8f.246 |
| **#222** *(08-21)* | **`claim.ps1 verify` threw instead of answering, and the `2>$null` was the bug**: under `$ErrorActionPreference='Stop'` redirecting a native command's stderr is what promotes a warning to a throw, and one tracked file with LF endings makes `git diff HEAD` warn · ⛑ it hid because `verify staged` reads the INDEX, so the pre-commit hook stayed green while the BY-HAND check - the one run before a number is written into the docs - was unusable · ⛔ and it may not fail quietly either, so `$LASTEXITCODE` is read and a failed diff refuses loudly instead of reporting "clear" · ⚠ **this row is late**: the code landed with no changelog, shipped or backlog row and was found by a completeness sweep, not by anybody missing it | 8f.245 |
| **#221** *(08-21)* | **the payday and the card stop standing on each other**: ⛔ #112's ladder says *content outranks chrome* and `#wNews` has sat at **40** against the card's 30 the whole time, so the one line drawn in the MIDDLE was drawn in the middle of the card - and since #209 a 1180x620 stage leaves nowhere else for it to stand, so it is a turn-taking fix and not a z-index one · both halves: a loud line raised while a card holds the map waits in `G.newsQ` for `worldTick` (the `f.days` path, a door that costs a day), and an arrival waits for a line already playing · ⛔ the guard is `G.moving`, held across the wait, because `firstTime` is already false and a player who walked off in the gap would never hear from that place again · the payday is a receipt: `◉36` at `--fs8` over one clause that names the next payday, the word PAYDAY off the front of both faces · a receipt runs 3.4s and loud PROSE keeps 6.5, with `say2` READING the duration back out of `getComputedStyle` so the number lives in one place · ⚡ twenty minutes went on a hang that was the CDP driver and not the build: `Debugger.pause` landed in an idle `setInterval`, and the same one-connection script passed identically on HEAD | 8f.244 |
| **#220** *(08-21)* | **the world bar counts bodies, the mood is a face, and the menu moves to the map's top right**: ⛔ the headcount he asked for had never been on the bar - `partyUsed()` is size-weighted SEATS (ratkin 1, human 2, ogre 3), so four bodies read `8/13` · the party chip is the race mark + the bold count + `SEATS 8/13`, and the word goes BETWEEN the two figures because `4` `8/13 SEATS` measured 6px apart and reads as *48/13* · the Captain's 24px bust is off it, so `--barChip`'s 30 stops being derived and is documented as a declared height · `.cchip .rart` is 22 rather than 16, because #211's 16 was the ceiling for a mark in a LINE OF TEXT and a chip is a flex row with `min-height` pinned · the mood chip is the face alone (96px to 41) and `.mface` is `--fs5`, because a picture standing alone at 10px is the item-icon smudge again · ⛔ **the menu's first row went straight through the word CLOSE**: `top:52` is the first clear row under the bar by arithmetic and it is `#wZoom`'s, which nothing in the stylesheet names - found by probing every visible box in the corner, moved to 82, and scoped `body:has(#world.on)` so the battle keeps it bottom left | 8f.243 |
| **#219** *(08-21)* | **the opening's two cards are on the event stage**: THE THREE BELLS and THE MAN IN THE CORNER were the deck #209 never counted - prose and doors, drawn by `proCard` into the 800px column the rest of the game left behind - so `prStage()` is the fifth caller of `evStageOpen` and **not one word of either card changed** · ⛔ the stage had `$('wDlg')` hard-wired in `evFlow`, `evFront` and the beat pips, and `#wDlg` lives inside `#wMap`, which does not exist yet at the opening: which box the stage is in is state now (`EVSTAGE.dlg`/`evHost()`) and `placeDlg` is opted out of, or it would have moved the WORLD's card · ⛑ the CSS is `:is(#wDlg,#prDlg)` and not a shared class because `:is()` keeps an ID's specificity exactly, and a class loses to `#wDlg .bd{overflow-y:auto}` and gives the stage its own scrollbar · ⚑ three things came free and derived: the lord's two quoted paragraphs are cast as HIM, the fight door is red without being told, and the opening fans ONE plate against the contract's TWO · ⛑ the contract card is the first flow in the game that does not fit (**698 into 518**, against 439 for the heaviest road card): the picker goes to one list per row, the strip's heading and divider are hidden inside the stage, and the last 99px fades at the top edge instead of slicing a line · ⚠ the art is the old 460x190 plates cropped, by his own call, and a wide master is one row in `JSTAGE` keyed `prologue`/`contract` | 8f.242 |
| **#218** *(08-21)* | **the card unfolds, and a door's edge says what kind of act it is**: the painting parts from a lit seam, the frame opens with it, the column arrives in reading order, the doors cascade - **470ms**, off the animation timeline rather than a sampling loop that was measuring itself at 725 · ⛔ nothing is a class on `#wDlg`: `.evstage` is a fresh element every open, and the gate is `.on:has(.evstage)` because a card closes without clearing its own HTML · ⛑ **`backwards` and never `both`** - a forwards fill outranks a declared transform and had silently killed `.choice:hover`'s 4px slide · the 2px left rail is `iv-<key>` off the door's own `ico`, the OUTLINE stays the state readout, and `leave` gets no colour because it is 28 of 158 doors and the null option · ⛔ the first cut read `ico.slice(-1)` and THE WARM SPRING's two-VERB `['take','leave']` door broke it; the stylesheet's row order decides now | 8f.241 |
| **#217** *(08-21)* | **a beast is a race of its own, and the dogs stop being ratkin**: `build()`'s derivation named two beasts and swept the rest into 'ratkin', so every dog in the game shipped as ratkin - it reads `BEAST_KIND` now, the list #215 had already hoisted out of that same function · **the field had five readers, not the three that were reported, and two of them are stats**: the Runt's authored stride of 6 was being clipped to 5 by `RACESTEP` under a comment that says monsters keep their authored speeds, and every dog was drawing a silent +5 off `RACEDODGE.ratkin` · the swarm is KEPT and moves to `FORM.swarmRaces` `['ratkin','beast']`, which also merges the two hardcoded `'ratkin'` literals the rule and the ≋ badge each had · the dodge points were moved onto the statblocks rather than deleted, so the shipped number is unchanged · barks and the AI temperament turned out to need nothing: `bark()` gates on `monster` and `doctrine()` returns early for a dog · `sfxStrike` would have dropped a dog onto `hit_human` and had already been doing that to the mirehares since #193 · ⚠ and `claim.ps1 verify` had to learn that a number already in the committed record ON THIS BRANCH is one an ancestor spent, not one this commit is spending - a stacked desk could not cite the desk under it, and `Get-ShippedNumbers` now takes a `$Ref` so the branch record and main's go through one parser | 8f.240 |
| **#216** *(08-21)* | **the practice field prices both sides of every fight on one derived scale**: `unitPts` off the two unit factories, founders = 100 by calibration, and the seasoned six lands at 202 against the six-body Snare's 199 - the exact fight #160 measured at 45% · a hand-picked crew bench (recipe rows rebuilt on every read, levels spent by policy, every artifact priced as its margin on the wearer) · six surprise fights locked until a campaign shows them (`gt_seen_fights`, marked beside the journal write, dev mode opens all) · dev-only ⚔-points on fight doors and on the map's battle nodes · ⚠ this ADDS at the user's direct ask, against the pass's own rule, and the addition is a balancing instrument rather than a player system | 8f.239 |
| **#215** *(08-21)* | **the mirror, and three things the battlefield was saying too loudly or to the wrong creature**: the parting-swing `⚔NN%` now speaks only while the pointer is on ground the red hand is over (`#bGrid.aiming`, stamped by the one pointer tracker rather than `:has(:hover)`, because the mirror needs a tracker anyway and two hover mechanisms for one pointer is the shape this file keeps deleting) · **a beast is not a people**: a Lurcher's readout said `🐺 PACK-BEAST · 🐀 RATKIN` and the plaque drew `C1_Ratkin_Rank_and_File` on every dog and mirehare - the class rule was written twice in this file and never covered the race or the portrait; `BEAST_KIND` is `build()`'s own `ANIMAL` list hoisted, one array two readers · the mood face turned down a second time (.66 -> .40, loud .95 -> .78) · **THE MIRROR**: `SIM.rule` on the practice field folds every board click across the vertical line through the acting body - move and blow alike, because `clickHex` is the one door both go through, and the fold sits on the POINTER so AUTO and both brains play straight · a cube-space x/y swap, so the row never moves and it is a true vertical mirror in pixels · off the board is a refusal that spends nothing · reading the board is NOT folded, and the landing hex wears the ring `.lit:hover` already draws | 8f.238 |
| **#214** *(08-20)* | **fifteen more J-stage masters wired in, and one row had been wrong since #209**: `steading` (four ogres on a mountain road) was pointing at `packev`'s art (dogs through a ruined steading) - both events say "steading" in their own prose and the first pass matched the filename instead · `packev` had no art row at all · every new key matched by reading the prompt against the event body, never by the filename slug · ⚠ `claim.ps1`'s entry ceiling was stale at 200 against a real count of 213 and handed #211 back out; raised to 300 | 8f.237 |
| **#213** *(08-20)* | **the price of leaving a zone of control moves off the body and onto the hand**: #205's ⊘⚔ over the mover's torso is deleted - it is `--curWalkZoc` now, the walk cursor in red with crossed blades, on `.lit` ground only · ⛔ the ⊘ deliberately does NOT come with it, because ⊘ is what the three refusal cursors mean and this step is allowed · each swinger carries `⚔NN%` off the same `hitBreakdown` the swing will roll, exact because `partingShots` fires before `walkTo` · ⚡ the first placement measured 31px wide in a 33px box and went straight across the odds: there is no free slot on a 37px hex, only the cheapest collision | 8f.236 |
| **#212** *(08-20)* | **an ally in the archer's lane costs accuracy now instead of the shot, and two notes about Asha**: ⛔ `losState` counted BODIES (`seen.length>1 -> BLOCKED`), so the commonest way to lose a bow for a turn was **your own line closing up in front of it**, and a withdrawn offer reads as the game taking the turn away rather than as a position to fix; the block comes off the FOES in the lane alone and your own cost **CROWDED, -20** · ⚑ it keeps #82's *worth the worst thing in it, never the sum* (one enemy is -22 whether or not your shieldman is there too) and the one-word ladder has been sorted by cost since #82, so -20 placed itself · ⛔ **the refusal it replaces was copied into four places**, so `laneClean` off `LANETIER` is the one predicate both brains ask now - and the preference had to GROW, because #36's *an archer who puts one through his own shieldman reads as broken* got more likely the day the lane stopped refusing him · ⛑ two board lies found by reading the DOM: `clickHex` still said *“you would be shooting your own people”* for a case it can no longer reach, and `inWay` marked ONE body in a state that is by definition about several · ⚑ proved against a `git show HEAD:` baseline on the same board: **BLOCKED / no shot -> CROWDED / 29%** · she is **Asha** and the `id` stays `ash`, because five non-screen readers and every saved run reach her through it · **`afterNode` is the fifth gate in `campPool` and the first that is a PLACE**: THE WRONG COLOUR waits for The Black Fen instead of landing at the second fire of the run, anchored to the node the user named rather than to a day count, with `LINT` proving the node exists AND that every road walks it | 8f.235 |
| **#211** *(08-20)* | **the three race marks are the painted ones everywhere, not only on an event door**: `rIcon` reads `RACE_ART` and nine screens followed without a line of their own · the emoji is the fallback and the WORDS are untouched · ⚠ the read is in a try/catch because `RACE_ART` is a const a thousand lines below it and `typeof` does not save you from a TDZ · ⚡ 16px, measured against a HEAD baseline in a second tab: 17px costs the sheet's race chip a pixel · and `vertical-align:middle` removed a shipped clip on `#bClass` | 8f.234 |
| **#210** *(08-20)* | **the chase on the wheel-rut road, and the tap-room had never recorded who walked out of it**: ⛔ `tavernAfter` counted the dead and threw the number away, so a card naming Harl would have been lying on any run that killed him - it stores `u.variant` now and `chaseWho()` is the ONE reader for the prose and the board both · ⛑ and its first cut read two survivors as one, because the second slot was reserved for Weft alone; caught by rendering all six survivor lists · ⛔ **five nodes in 236px of map**, so the road dips south: the plate rule allows three columns and `f1` owns the middle of what is left · ⚑ **Grausen Hold 330→276 and 20px east, because #190 painted 50% open water under where it was standing** · ⛑ all three map counters 0 undealt, 0 across 40 deals, and then MADE to fire on purpose · ⛔ three doors, three currencies (3 provisions · 40 crowns · blood), and losing charges **the same bill the other two quoted** through a new `take:{}` on `BEATEN` - which is also how “1 provisions” and the same defect in the Circle's line got found · ⚠ the fight is the CHEAP fight and the arena says so (14/14 · 5.9 rounds · 0.4 down against clash's 7.1 · 2.2), and what it charges is blood: 82% of the company's hitpoints two days before the clash · ⚠ the purse did NOT move at the user's word, so the opening is two days poorer and reaches Blood on the Road with 86 crowns | 8f.233 |
| **#209** *(08-20)* | **every card in the game is a painted stage, style J out of the sketch, and not one of the 99 was rewritten**: the painting is the whole card at 1180x620, the company stands in it on the left, the prose arrives a beat at a time, the doors are the last beat · ⛔ **every part of it is DERIVED** - the paragraphs are the beats, the paragraph's first character is the voice, `needRace` is the second face and the door's actor, `G.fireTeller` is the first (**and only when the card's own text says `{TELLER}`**, or the coin's finder stands in the wedding three nodes later), and the fire's two people come off the prose names it was already cast with · ⛔ **one stage, three decks, one `evStageOpen`** at his *"form of all events new ... it is canon now"*: the road, the fire and the vignette had two and a half renderers, which is the drift #150, #159, #176 and #197 each deleted once already · ⛔ **the mode follows the CONTENT** (`:has(.evstage)`), because twelve functions open `#wDlg` and only one is `openEvent` · ⛔ **the doors are on the game's own palette** at his word: the sketch's teal is this build's battle chrome · the eight intents and three races are paintings, plus eight wide 1672x941 stages, in their **own** `/*__J_PACK__*/` marker so a redraw never touches the 10 MB paintings block · ⚠ `placeDlg` measures the card instead of reading `DLGW` · ⏳ **32 of 39 road cards and every camp card still wear a 586x212 letterbox cover-cropped into a 1.9:1 box**; each is a re-export at 1672x941 plus one row in `JSTAGE` | 8f.232 |
| **#208** *(08-20)* | **skill groundwork, the three rulings the battle could already say**: HOLD THE LINE is the captain's starter at ONE action (at 2 it lost every argument with COMMAND) · COMMAND unlocks itself at level 3, no perk point, so the arm comes before the voice · SPEAR WALL refuses to go up with an enemy already in reach and the greyed card says why on hover, off the same adjacency predicate wallLive() asks, bracewall exempt. The fourth ruling of the same message, DISENGAGE as a general perk with a 2-hex step, waits in `tools/skilltree_lab.html` with the rest of the tree: pulling it off the basic set before the tree exists would soft-lock every scrum the ZoC economy builds. | 8f.231 |
| **#206** *(08-20)* | **five on battle readability, and three of them were a line nobody had written**: ⛔ **no hex had a `:hover` rule anywhere in the file** - 22 to 38 hexes of walk wash and nothing saying which one the cursor was on; `.hex.lit:hover` draws the ivory ring `.hex.acting` already uses · ⛔ **the board cannot be moved out from under the chrome and the arithmetic says so** (964x682 at FULL, and 774px of clear ground at the bottom between `#bPlq` and `#bEnd`), so the chrome yields: `veilChrome()` halves any control with 8% of a body behind it and restores it on hover - `#bEnd` fires in the Snare, which IS the report · ⛔ **the map's wheel handler ate every notch over the road card** (`defaultPrevented` true, `WCAM.i` 1→0 on one notch) though its own `pointerdown` had excluded `#wDlg` since it was written; plus visible scrollbars, because macOS hides overlay ones, plus **the card's outer scroller deleted** - ten cards measured and it had never once scrolled · ⛔ **an act that applies automatically has no selected state to draw**, because COMMAND and its family resolve inside `selectAct`; the radius rides the card's HOVER through `paintPeek` and paints OCCUPIED hexes, `aura:` now read by all four sites · ⛔ **the lane word is off the hex** at the user's ruling, and `hitBreakdown` still names it in words on the readout | 8f.228 |
| **#205** *(08-19)* | **three out of the feedback round, and every one of them was a measurement before it was a fix**: ⛔ **the token health bar's FILL is `2px - 2 × border` and the border is scaled by the board's camera `zoom`** - 0.513px at one stop (0.97px of red survives) and 1px at another (**exactly 0**), which is why one machine showed a hairline and the next showed nothing; health row 4px and armour 3px carry their own border now and the anchor drops to `--head - 9` so the 1px of clear air over the head is the same 1px · ⛔ **the parting swing was only ever announced AFTER it was charged** - **⊘⚔** on the hex you are leaving and a **red-lit sprite on every body that will swing**, off `partingRisk()`, which is now the ONE list `partingShots`, the UNDO `clean` test and the picture all read (#173 named four places that must agree; this is the fifth) · ⛔ **the tap-room could deadlock on Harl's nerve**: measured at phase 2 / round 17 / no wave, then at a round-4-to-55 oscillation once unlatched, because `capTick` zeroes him and `beginTurn` hands **8 of 52 = .154 against a .15 threshold** back - the scripted exit hangs off the script's flag now · ⛑ and behind it **`tavernPlace` fell back to an OCCUPIED hex** (two bodies on one hex is one body) and `checkEnd`'s net **won the fight on the round the knife walked in** and **never fired wave three at all** | 8f.227 |
| **#203** *(08-19)* | **the itch.io build, and the storage that is not there**: ⛔ **an itch game is a third-party iframe and a blocked browser makes `localStorage` THROW**, so three top-level reads above the whole build gave the front door a title, the pillar and **no buttons** - one shim on a configurable accessor covers all ~40 call sites, the probe WRITES because a readable store can still keep nothing, and `STORE_OFF` says so on the front door · ⛔ **the embed is 1280x720 because `fit()` scales the stage as one piece** (960 measured 0.75, i.e. the 10px floor at 7.5) · `tools\build_itch.ps1` builds the zip and reads it BACK, and writes forward-slash entries by hand because `Compress-Archive` does not · `docs/ITCH_PAGE.md` is the page text with every number taken from the running build, five screenshots and a cover shot from the EXTRACTED zip · ⛑ the shoot found the aftermath breaking a nickname mid-word at six crew, measured and deliberately left | 8f.225 |
| **#202** *(08-19)* | **the run-through, and the playtester build**: the act driven front door to epilogue with zero JS errors, all desks landed · ⛔ **the real typefaces (#189) were under boxes measured around the fallbacks** - `ROLE{}` capped at twelve glyphs and `LINT` 14 holds it, the sheet's tells and the battle nick get their descenders back without the rows growing · ⛔ **the mercy and cub screens still typed their receipt and echoed the mood** ("barely worth a word" on a −14 door); both render `choiceNote` now · ⛔ **LINT 6f was cited by two documents and did not exist**; it does, scoped to EVENTS + CAMPS + MERCY, proved by seeding · five `needRace` doors stop saying "needs a ratkin" to a company that has one · **`deploy.ps1 -Player`** builds `play/index.html` with the cog deleted and `TEST` forced off, both pages read back and checked against each other's marker; **`-Branch`** pushes a work branch and says the live link is unchanged · `WHAT_TO_TEST.md` cut to the last three days. ⚠ **on `work/playtest-build`, not main** | 8f.224 |
| **#201** *(08-19)* | **the albino half-ratkin joins with the ratkin, and the fire card where she talks**: `Ash "Nobody's"` replaces two presets rather than joining them, because *"1 or 2 ratkin join and she is one of them"* is only true on every ratkin path if she is on both · ⛑ **`dutiful` was already the sentence he wrote about her** (+8 dodge beside the Captain, −4 away), so no new mechanic was written for the character at all · `CAMPS.palefire` cost three one-line mechanisms (`castMember`, `c.art`, `hurtCast`) and ⛔ **the camp handler injured a RANDOM body**, so the third door would have said *you hurt her* and charged Marrow · the art is three script-made derivatives, the 36x37 sprite ships and the 70x72 "preferred" one does not, because every allied ratkin on the field is 33-39 wide | 8f.223 |
| **#200** *(08-19)* | **nine notes on the company sheet, and the two with findings under them were both the sheet quietly carrying something in the wrong container**: ⛔ **the block he called "weird stuff" was a 56px row of chips holding two 40px buttons** (measured on HEAD, 63 of content in 33 on the mage and 116 in 56 on the Captain), so the doors moved beside the name, where the portrait tile means they cost **nothing**: `#iHead` is 117 before and after · ⛔ **a gear slot drew two tooltips at once and the second printed the description twice inside itself**, 173 words in two boxes down to 50 in one, and the repair found `spellRange` living only in the working stave's prose with neither renderer reading it · ⛔ **the width's ceiling is the ACT ROW, not the doll**: 560 passed the figure's own arithmetic and wrapped the Captain's six cards, which cost 83px in a column that is exactly full and clipped the FIGURE, so 530 is the number the widest act row allows and the perk wall still gains 18% · ⚡ **the narrowing un-clipped the stats**: one column of four rows gives each tell 259px, and HEAD's own overflow probe reports one finding this build does not · the bust is square and 48px because the READOUT changed shape (a `<rect>` takes `stroke-dasharray` like a `<circle>`), the roster prints the personality's name with the clause on a `title` (never `tipOn`: `drawInv` destroys the row under the cursor), and the amulet's lecture is deleted. | 8f.222 |
| **#199** *(08-19)* | **the em dash leaves the game**: grep counted 1,338, the lexer said the player reads **188** (1,071 block comments, 68 CSS, 11 in raw HTML no table sweep reaches, 2 inside `LINT` that must survive because they are the check) · ⚑ **the replacement style was measured out of the file, not chosen** (` - ` is the house dash-break, ` · ` is spoken for, `–` is the range) and the 195 sites judged one at a time: 83 hyphen, 65 colon, 24 comma, 12 full stop, 9 en dash · every replacement the same length or shorter, so not one box moved | 8f.221 |
| **#198** *(08-19)* | **a `grimtoll_text_edits` ledger of 24 marks, applied - and the instruction *"if you see some pattern or similareties that could be done for other tasks - do it"* is what found most of it**: ⛔ **the ledger edited ONE of the contract card's three branches and all three carried the purse**, so applying it verbatim would have put the purse on the wood twice on the two branches a player reaches • ⛔ **"He throws a bag of coin" contradicts the painting on its own card** (`EV00E` is locked to *he never throws it*), so he puts it down instead • ⚑ **a paragraph break on that card is a layout change**: the split I added took it from 683px to 696 in a 684 ceiling with an IDENTICAL line count - one 9px gap - and his own text had been one paragraph • ⛔ **the `?` was asked for twice and is illegal both times**, once because `mystery` eats the whole sub-line and that one carries what the fight fields, once because LINT 6d forbids a mystery door that charges a day; both got the half a `?` is for, which is the cut he made by hand two marks later • ⛔ **"forget about your teammate" would be a lie**: `strandOne` gives them back on a 50% roll per leg • ⛔ **THE HOLD'S MEN's battle receipt was false** ("the hounds" when `holdFoes` fields one, no crossbow, and a roster that scales with party size, so no static count can be right) • ⚑ **his `note` op was buildable, not just a pin**: `G.coName` exists, so the serjeant says the name back - and on BOTH branches, because the driven build showed one still saying "this company" • ⚑ **the 64th mood echo**, which survived #176's sweep of 63 because that door is built at the renderer instead of in `CAMPS{}` - **a table sweep misses every row the code writes at run time** • one spelling for BATTLE across eleven doors, SECONDARY OBJECTIVE on all four surfaces, and one stale word-count comment fixed as an open defect. | 8f.220 |
| **#197** *(08-19)* | **seventeen asks in one morning, and three of them turned out to be the build lying quietly**: ⛔ **nothing in the game was ever a permanent injury** - the picker branched on `hurt.lasting` and not one card set it, while five injuries were AUTHORED as stat losses and said so in text the player never saw, because the temporary branch prints only the name; and **the camps picker had its own applier that never read `lasting` at all**, so a card marked permanent would have shown the new label and charged hitpoints. One `applyHurt` now, and the five are real. • ⛔ **the ogre's join door was never missing** - it was live, said *an ogre may join*, and checked the room INSIDE `pickChoice`, so a full company spent its one choice to be told afterwards; derived off `recruit`/`recruitPreset` now, which fixed four preset doors nobody had reported. • ⛔ **rarity did not exist**, so *"more rare and epic items"* had nothing to count: 51 rows tiered on the row (21/22/9, 14 `unique` and never drawn), and the rate raised by ADDING a piece rather than re-weighting tables that are authored flavour about the body they came off - 30% after a fight, 60% from a cache, measured at 70/23/7 and 40/42/18 over 20k rolls. • **a card opens in the middle** (the node anchor was clamped against an edge for half the map, so the painting landed wherever the node was). • **the map's foot took three readings**: 121px and six nodes at the NEAR stop is the ZOOM; camera space mixed with screen space says a caption sits 81px below its own node; the real number is 22px and three captions - and **the first fix hung the plates above the paintings and took `spacingViolations` 0 → 2 against a `git show HEAD:` baseline**, so each caption lifts by its own overflow and nothing more. • two events deleted, THE DEBT's two morale prices deleted, the last fight's marsh door deleted, the map's dev readout silenced, the hover's duplicate type line deleted, TRADE became CLASS on nine surfaces, the muster's stat line moved to a hover, a door that is not a decision lost its sub-line **and its empty `<i>`**, THE LONG FIRE's second part 151 words to 99, one height for every chip on the world bar, and **A Treasure Map** that puts a cache on the road ahead - ⚠ filtered out of the deal where there is nowhere left to bury anything, which `snarejoin` (the last node of the act) is every single time. | 8f.219 |
| **#196** *(08-19)* | **three asks, and two of them found something the build had been hiding**: ⛔ **the ✎ TEXT tool could not see a word of the hover layer** - `#gtTip` is one box for fifteen sources, `pointer-events:none`, hidden on the anchor's `mouseleave`, so it has no click to intercept, and 33 `title=` attributes are not even in the DOM. A **dwell** mode picks with no click and snapshots the tip where it stands; the editor offers TEXT / HOVER BOX / TOOLTIP as targets and the ledger records which. ⛔ **a hover-box edit gets no preview and must not fake one** (`tipOn` rebuilds from a function); a `title=` does. The panel went 370x430 to **460x456** with a **860** wide switch and a textarea that grows to what it holds - it was a three-row box for a 175-word card. ⛔ **and the races had two vocabularies**: ◆ ▪ ⬢ on the sheet, 🐀 👹 🧑 on the event doors since #154. One table is the source now and the doors read it. The class marks became pictograms and the WORD came off the LISTS only - it stays where one body is studied, and on the hover everywhere. ⚡ **it fixed a shipped clipping**: the 194px battle rail cut an ogre BATTLE-MAGE's line by 23px. | 8f.218 |
| **#195** *(08-19)* | **the hit odds come back, and the mirehare overlay turned out to be lying**: `SHOW_HIT_ODDS` -> true, one boolean because **#191's own rule is symmetric** - hiding a total means hiding its signed parts, so showing one means showing them too; his *"basically return, how it was before"* confirmed the whole family. • ⛔ **the hover overlay painted a WALK for two creatures that cannot walk**: the doe was drawn 24 hexes of which **18 are unreachable** (everything at distance 1-2) with **8 legal landings unpainted**, the buck 40 of which 22 were unreachable and **his six lanes not drawn at all**. `peekOn` called `reachMap` on bodies with `noWalk:true` and no MOVE act - #102's wrong-unit rule on the overlay, and doubly wrong for a leap, which **does not pay for the ground it crosses** while a flood fill can only. • ⚑ fixed with three builders (`CUBE_AXIS` · `chargeLanes` · `moveEnvelope`) and the brain refactored onto them, plus `drawMoveLines` so a lane reads as a LINE: **a wash says which hexes, only a line says straight**. • ⛔ **`peekThreat` was drawing both counters backwards** - a movement-only creature threatens from its LANDINGS, never from where it stands, so standing against either animal is safety and neither had ever been shown. • ⛔ **the file warned in writing about the bug I then shipped for an hour**: `render()` holds a second peek-rebuild site, and the honest overlay survived exactly one frame. Found by **making the live-charge branch fire on purpose**. • ⚑ **`MB.gap()` answers the wrong question and answers it backwards**, because `arrange()` fires once and both counters are per-turn; `MB.rules()` measures the counter instead: **CONTACT 20/20 · 0.00 down · 46 taken** against **NAIVE 14/20 · 2.50 down · 336 taken**, i.e. knowing the movement is worth 2.5 bodies and takes the fight 70% -> 100%. • regression 4x n=20 per build against `35227a4`: every delta inside each build's own spread. ⚠ built on desk `work/hit-odds-mirehare` at his word, main's prototype untouched. | 8f.217 |
| **#194** *(08-19)* | **four asks on the world map, and two of them were the map having quietly gone out of date**: ⛔ **the legend keyed a coloured diamond that 18 of 25 nodes stopped drawing** - #114/#115/#116 replaced the chip with a painting, it never covered `town` or `event` (the two commonest types), and #190's painted ground took the last of a 10px row's contrast; three entries rotted it and none of them had any reason to look here. • **a walked node was .45 against `far`'s .38**, a seven percent difference between *you fought here* and *you have never heard of this*, so `done` is opacity 1 and gives up glow, beat and colour instead; ⚠ the rule has to sit BELOW `.node.t-*`, which sets `border-color` at the same specificity, or it loses the cascade on five of seven types **silently**. ⚡ **the filter was measured, not chosen**: two candidates were indistinguishable on screen, and a canvas probe said the saturation half does all the work while the brightness half buys a 17% dim on art already at luma 36/255 - on the one state whose instruction was *leave them lit*. • **the clan wedding left `FLOATING` and became a node**: one of eighteen cards for seven slots, and the card `G.sins`, the kin reinforcement and `AFTER.wedding` are all written against. ⛔ **it is one stop later than he sketched and the reason is arithmetic**: camp1→hire is 167px against `MAP_MIN_GAP` 90, so nothing fits between them, and every solved position sits on the road down into the fen unless The Muster Field moves; he was given the three options with the numbers and chose the one where nothing else moves (+1 day on every run). • **the day plaque's hover is deleted from `WTIPS`, not merely unbound**, and `cursor:help` went with it. ⚠ **the preview server was another session's, serving the main repo, and reported my own edits missing** - caught by md5, not by reading. | 8f.216 |
| **#191** *(08-18)* | **six asks in one batch, and the finding is in the first one**: ⛔ **the terrain tooltips were `title` attributes, so a body standing on the hex ATE the hover** - the one moment you most need to know somebody is in the fire was the one moment nothing said so, and the two kinds with the most to say (`marsh`, and the ring around an open bloom) had no title at all. `groundNote` is one builder with three readers and the answer is per body. • the fight stopped printing % to hit anywhere (`SHOW_HIT_ODDS`, one const, reversible) and **it had to take the signed parts with the total**, or the player just adds the receipt back up. • three perks, none of which adds a system: Patient banks an action on the hand-over, Ground-Wise joins `wetproof()` (already the one door for that question), Elemental Weapon rolls FIRE/FROST/VENOM per fight through effects that already existed. • payday and the second objective now arrive centred. • the feedback popup lost SEND and NOT NOW: **the pick is the send**, and the reason writes onto the same row as it is typed so nothing is lost. • **the game is RabbleBound**; the island is still Grimtoll and the `gt_*` save keys never moved. | 8f.211 |
| **#190** *(08-18)* | **the world map's ground is a painting now, and the finding is that the ground was the only thing allowed to change**: the picture is drawn inside `drawMap()` directly before `EDGES`, so roads, node plates, sights, the token and every camera rule are untouched, and the 25 node positions are identical with the picture on, off and back on · ⛔ **the procedural terrain is NOT dead code** - it is the frame before the file lands and the map for ever if it never does, proved by blocking the asset at the network layer · ⚡ **the node layer was drawn for a dark ground, so the correction went on the GROUND rather than on the plates**: measured over the SAME 23 name plates in one run, the old procedural ground sits at median luminance 48 and the painting at 119, with 37% of it over 140, which is why the sights washed out - a flat wash at `WORLD_MAP_DIM=.45` lands it on 31/53/92 against the old map's 32/48/78, and that is where the number comes from · ⛔ **`THE GRAUSEN VALLEY` was redrawn correctly and still could not be seen**: it is authored as the dimmest of the three region names on purpose and a sunlit valley floor is the brightest part of the map, so it landed at luminance 85 on a ground of 75, a ratio of 1.1 - three stacked shadow passes take its ground to 58 and every authored colour survives · ⛔ **and it is EMBEDDED, never linked**: the published build is one file at the repo root where `../art/...` resolves above the repo, so it loads at the desk and 404s on the host, and because the fallback works that failure is silent - `build_assets.ps1` reads the wanted key out of the prototype so the two cannot drift, ships only that one file, and `deploy.ps1` now asserts the ground by name | 8f.210 |
| **#189** *(08-18)* | **his two notes, and the first one had a false premise that was worth more than the request**: ⛔ **the three typefaces `:root` has named since the first slice were never in the build** - measured, all three fell through to **Palatino Linotype, Georgia and Consolas**, so the fonts he called beautiful were the fallbacks, and every screenshot this project has ever taken is of the wrong faces. They are embedded now, base64 `@font-face` built by `art/fonts/build_fonts.py` and injected on the same marker pattern as the paintings, **never a `<link>`**: the build is one file and has to open with the network off · ⚑ **the face list was measured, not counted**: the stylesheet's `font-weight:` declarations argue for twelve faces and walking the ten screens says **eight triples**, which is 860KB down to 648 · ⚠ **the bill is metrics** - real Cinzel is 16% wider than Palatino and real JetBrains Mono 9% wider than Consolas, and exactly one box broke (a role line at 117.8px in a 117px column) · the five `c.font=` sites that hand-copied the stacks read the custom property now, so a canvas cannot disagree with a stylesheet about a typeface · **and the scrollbar he photographed was two faults wearing one bar**: ⛔ **#150's phantom, fixed for `#prologue` and left on every other `.pcard` for five months** - 8px of nothing, invisible on overlay scrollbars and a full-height thumb on classic Windows ones, which is exactly why it survived five months of measurement · plus a real one, because the longest aftermath in the game has 145px for a crew grid and `PARTYCAP` is 13: the column count is authored now, never more than two rows, and what geometry cannot pay comes out of the prose box, **which is the one surface his own note allows to scroll** · ⛔ **the first cut of that fitter deleted the story** - a flex item given `overflow:auto` has `min-height:auto` of zero, so the card shrank a 165px paragraph to nothing and said nothing about it | 8f.209 |
| **#188** *(08-18)* | **his seven notes, and note 2 is the one with a finding under it**: ⛔ **#98 hid three corner buttons behind a word because they asked for an opinion continuously, and two rounds later nobody was pressing the word either** - a corner glyph is not a question, it is furniture. The three `.rx` and the whole fold are deleted and the question is a **modal that arrives**: three faces, and only the unhappy one opens a five-word *WHAT WENT WRONG?*, because asking everybody for a reason is how a form gets closed. Once a run, after a fight, keyed on the run id · ⛔ **the fight's name is captured before the navigation**, or the reaction files against the map it opened over · ⛔ **the glyphs are deliberately outside `MOODSTATES`** - that ladder is the company's morale and one glyph may not mean two things; 👍 🤷 👎 is his second pick and ⛔ **"so-so" could not be filed as `like`**, because the report prints that word as *liked* and a tally that calls a shrug a like is a tally that lies - so `meh` is a fourth kind and ★ is retired from the popup rather than deleted, still rendering for every journal that already carries one · **the 8-second grace window went with the corner buttons**: it existed against mis-clicks, and a modal with a SEND cannot be mis-clicked · **the address and the mail button are back**, which 8f.105 predicted in writing when it pulled them on an unconfirmed hunch - one constant to pull again if the share ever refuses · ⛔ **his GitHub question is no**: the CSP blocks every outbound request, so the clipboard stays the road that always works and the mail is the convenience on top of it, **measured against a 1800-character encoded ceiling** because a mailto is a URL and clients cut one silently · four sentences deleted by name, Back stripped on all three screens that carry a sub-line, the copy button moved beside Back **with the address under it** · ⚙ is one dim glyph in the true corner and **one glyph in both states, which is geometry**: a wider ON would grow into the FEEDBACK chip | 8f.208 |
| **#187** *(08-18)* | **the after-battle screen re-dressed on the hybrid mockup he approved outside the repo, and the discipline of the entry is that NOTHING under it moved**: no payment, no number, no line of prose, no `AFTER` or `LOOT` row - #122's data model, #137's chips-beside-the-payment, #159's mercy branch and #174's ✦ all intact, and if a future pass finds itself editing `takeLoot` to make the screen look right it has left the brief · an **emblem, CSS rays** (three gradients, because an image would be a network fetch and the single-file build cannot pay that), **one meta line** built from figures already on the screen, and **four cards across** at `auto-fit` rather than a fixed four, so a company of three fills its row · ⚠ **the numbers span the card and the mockup's card was 226px wide where this one is 181**, which is the whole reason - four chips wanting 120px in a 111px column · ⛔ **the promotion ribbon lost to the overflow counter and the badge went back where #122 had it**: a corner ribbon only reads as one if the corner crops it, cropping IS layout overflow, `overflow:clip` does not dodge it, and a flush-sized one stops looking like a ribbon and covers the name - so `▲ N` sits in the top-left corner instead, and the level number now appears exactly once · **injury kept the old blood and gained the strip**, per his *"не заменять это одной тонкой красной обводкой"* · ⚠ **the gap came down to `--p3` because the header costs ~130px and the Snare's prose is the longest in the game**: without it the longest card in the build scrolls before its accordion is open · **verified against a `git show HEAD:` baseline in the same browser** - ten screens, 0 under the floor, no new clipped box, and the 8px phantom scroll measures 8 on HEAD too · ⚠ **still open and deliberately not here: none of the three typography tokens resolves** - Cinzel, Spectral and JetBrains Mono are named and not embedded, so the whole game is in fallbacks, and his brief says that hookup is *"отдельно и глобально"* | 8f.207 |
| **#186** *(08-18)* | **his six notes, and every one is a user-ordered addition, so the file says so**: ⚑ **the road remembers two choices and sends two bodies when you are down to two** - the Circle's ring and the wedding's race doors each earn a rescue that comes **half the time, once a run**, muttering *"One of us"*, and the log line opens on *"Luck."* because a rescue that came every time would be a garrison; measured, a lost 2v6 becomes 5/5 with them · **the dust ridge and the wood sometimes burn**: `ember` is a walkable ground that costs 2, lights whoever crosses or stands in it (6, then 4 a turn for two), and goes out in water - the shove into the fire is the whole reason it can be stood on, and both brains got the same six lines · **a gem buys a level** on the sheet, once a run, on the medicine chest's shape · **two wide and the third lands**, decided in `hitBreakdown` so the card, the roll and AUTO cannot disagree (+4.8 points on your side against HEAD) · the objective's payout is **nine words in a gold strip** instead of a paragraph across a phone · two more mutters, his own words, and LINT 11 lets a six-word line through at the width of a five-word one | 8f.206 |
| **#185** *(08-17)* | **his three voice notes, and the middle one deleted a whole second teacher**: ⛔ **the game had two teachers wearing two shapes and only one stopped the fight** - the first battle's spotlight took the lock, and everything after it was a parchment bubble hung off whichever hex the Captain stood on, running 2.2s while the enemy moved underneath. His *"random places"*, his *"strange"* and his *"it should stop other actions"* were **one defect**, so `.ccall` and `capBalloon()` are gone and `tutShow`'s body became `tutPaint()` - a lesson and a Captain's line are one code path now, and the blocking is #156's `B.tutLock`, already there. **The portrait is the ask and not one word of any step changed**, which is the point: the text was always second-person and simply had nobody's mouth on it · **the mutter**, race-voiced and never actionable, so it is never logged either - ⛔ **rarity turned out to be `BARK_GAP` and not `BARK_MAX`**: measured over 4000 stub fights, a cap of four with one-per-round spent all four inside rounds 1-4 in 99% of them and then went silent, which is a queue rather than randomness; three rounds of enforced silence gives **2.74 a fight, even across all nine** · ⛔ **and the offset the building session flagged and never checked was wrong** - `hexSpan()*0.9` printed through every ogre and most humans, because a head sits **1.09 × hexSpan** above the anchor on an ogre and **0.36** on a lurcher, so it asks the sprite now (#157's rule), with a margin sized by the keyframes because `mut` is born 6px lower than it rests · **`TRAITSHORT` deleted and derived**, because a table written in mood (*"Old hand. Bad knees."* for +4/+4/−1/−8) cannot tell a player what a person does; four of twenty-one had no downside at all and `LINT` 12 now fails what a comment could not, plus five new ones each wired through a hook that already existed | 8f.205 |
| **#176-#184** *(08-17)* | **the eleven-item batch, and the first item turned out to be a renderer**: a door's cost is **derived off `fx`** now instead of typed, because deleting the prose he boxed in red would have deleted the price off the toll-man, whose four labels *were* its receipt - and the derivation immediately caught the aqueduct promising `+1 salvage` on a door that pays 2. 63 mood-echo clauses gone with the shape that carried them · the second objective is one recruit and its row **closes itself** · the road speaks 29% less often and FOUND/LOST drop from 7 of 12 vignettes to 4 of 9 · a shop row gets a reserved art box and its real numbers (reversing the half of #157 that took the arithmetic off with the atmosphere) · the aftermath band set at its own shipped floor, 867 words to 702 · ⛔ **the fork sign was counting node TYPES and not one road on this map holds a fight you cannot refuse** - it asks the cards now, and the row became a Hades boon with the node's own painting on it · ⛔ **an empty larder started 4 of 4 of the line ROUTED**, measured against HEAD in a second tab, and the guard that stops it runs after the penalties and reads `nerveFrac` because the Circle is never written into `u.morale` · the first Hold patrol loses a crest, 9/15 → 14/15 at n=15 · the survey is three questions and the report stops showing the player a JSON blob | 8f.204 |
| **#175** *(08-17)* | **the shake comes back a quarter, and four captions get four lines**: `--hitp` 2.5 → 1.875, one character for all fifteen impact durations · `fx()` gets a queue per hex at the one door, and the first cut broke twice in ways only `getBoundingClientRect()` could see (a mixed-size ladder collapsing to 5px, and an 18px row against a 20px box) · `forwards` → `both`, because nothing was describing what a staggered animation looks like before it starts | 8f.203 |
| **#173** *(08-17)* | **the fight slows down enough to be watched, and the hex grid finally interlocks**: the lunge had never once been seen because `render()` deleted it two statements later (third report, first two fixes aimed at the wrong half) · sprites stop eating the hexes behind them, 49 stolen probe points to 0 · the zone of control becomes the front three hexes in the RULE and the picture together, balance-neutral over 200 arena fights · `HEXOFF` 27 → 19, the shear #105 measured and then painted around | 8f.201 |
| **#164** *(08-16)* | **the designers' checklist out of the Turn-Based Games Discord, and the three scales that answered four of it**: 29 font sizes to 9 tokens, 7 near-identical border hexes to 5, 72 paddings to 7, and **a 10px floor** that raised 153 declarations. ⛔ **Four checklist failures were one cause** - no tokens for size, spacing or edge - so this is a merge, not an addition. ⚑ **The floor broke five boxes and the spacing broke none**, which is the opposite of the prediction: `.act` 72x86 → 80x90 because GUARDS ALLIES was being eaten by `text-overflow:clip`. Rule: `.claude/rules/ui-scales.md` | 8f.192 |
| **#157** *(08-14)* | thirteen of his notes, and the first three were **one bug**: the token's furniture is nailed to the hex and the painted bodies stopped fitting it, so a health bar vanished under whoever stood in front and a status badge was drawn across the face. Both hang off the sprite now. ⊛ **SURROUNDED** on the head at three-or-more, adding no rule; the caster starts with **one school**; **nothing eats the provisions by itself** any more; the Thing in Armour cannot be left (**QA-7 cashed**); the muster's intro 100 words to 32; **a lost fight can be taken again**; and **nobody chases a broken dog into a corner** | 8f.185 |
| **#156** *(08-14)* | twelve of his notes: **the step numbers and the terrain glyphs off the hexes**, the enemy's reach finally shown where your own preview was hiding it (with a blow picked too), the acting body wearing a hexagon instead of a smudge, **a tutorial spotlight stopping the fight it was explaining**, two actions on every body including six enemy captains, the front door down to labels and a loud pillar, the contract down to the head and the plate to 305x53, **a second company skipping the brawl**, and **the road grew the battlefield's own three-stop camera** | 8f.184 |
| **#155** *(08-14)* | the opening pass: **THE GRAUSEN ROAD card and the tour's THE DAY step both deleted**, the prologue card stops stretching to its own ceiling, ⚔️ lands on the first fight door in the game, the naming screen trades ♥ MENDING for ⛊ HEADS, Blood on the Road halves and grows portraits, *"the company stands taller for it"* out of 11 places - and **the archer's cursor stops being a sword** | 8f.183 |
| **#154** *(08-14)* | **every road door wears its intent**: eight glyphs measured down from ten, plus a race mark derived from `needRace`; and the Fen-Mother drops to two doors, the quiet one paying in days | 8f.182 |
| **#151** *(08-14)* | eight of his notes: a fourth door on the toll-man that takes the whole robbery, the menu's two playtest rows merged, *"it sits well with them"* out of 16 cards, cost lines in the game's own resource glyphs, one loot receipt per fight, and **the fork sign spans a range instead of counting** | 8f.179 |
| **#150** *(08-13)* | eleven notes: the opening loses a title, a sub-label and a whole screen; the receipt chips learn which glyph means what; *"it sits badly with them"* out of 24 cards; **the road bar grows the three signs it never had** | 8f.178 |
| **#145-#149** *(08-13)* | the brawl's second wave splits · the aiming pass, every summed row carrying its working · two finished grounds reach the campaign · #36's blocked lane joins the one gate · AUTO leaves `setInterval` | 8f.173-177 |
| **#143** *(08-13)* | **the road's prose cut by a third**: 5,474 words to 3,652 across 34 cards, and the outcome numbers leave the prose for the aftermath's own chips | 8f.171 |
| **#141 · #142** *(08-13)* | the coin is a one-door pickup that wears its loot on the card · the race box was saying everything twice | 8f.169-170 |
| **#124-#135** *(08-13)* | the overnight batch: one menu button and Esc everywhere, START NEW GAME starts one, six morale rungs instead of five, archers that advance and give ground, the spear line screening them, stat names people already have, every bow reaching 5 | 8f.152-163 |
| **#133 · #136 · #140** *(08-13)* | 🎒 **the company sheet, three rounds**: 1241px and 564 words down to 678px and 242, no scroll, stats first and the kit on the body | 8f.161-168 |
| **#137 · #138** *(08-13)* | the seven-item pack, and **four cards between the brawl and the map become one**, which leaves no decision in the opening at all | 8f.165-166 |
| **#123** *(08-12)* | **nine road events lose eleven doors**, then five more go 4 to 3: 48% of the multi-choice road, and the choice economy becomes concept §5 | 8f.150-151 |
| **#122** *(08-12)* | 📜 **the aftermath on the Battle Brothers shape**, so half the fights are one screen now | 8f.149 |
| **#108-#111 · #118-#120** *(08-12)* | 🚪 the front door: an intro brawl that teaches by being played, then fight first and contract after | 8f.135-147 |
| **#103-#107 · #112-#117 · #121** *(08-11/12)* | 🗺 **the road screen, ten rounds**, and 32 painted sights standing on the nodes | 8f.130-148 |
| **#88 · #91 · #94-#105** *(08-10/11)* | ⚔ **the battle screen, nine passes** | 8f.117-132 |
| **#90 · #93 · #99** *(08-11)* | the board is **drawn** per battle from a terrain-owned pool: four arrangements where there had only ever been one | 8f.118-127 |
| **#92 · #139 · #144** *(08-11/13)* | the desk itself: collision guards, a branch and a worktree per session, and a spent number that gives itself back | 8f.119-172 |
| **#87** *(08-10)* | every skill gets its own animation from eight primitives keyed on what an act does: **subtraction of reading, not addition of system** | 8f.116 |

### What it taught, and this is the part that decides the work still left

- ⛔ **Measure the box before you argue about the font** *(#88)*. All three of his battle-panel
  complaints had one cause: a 133px column leaves a card 116px of inner width and the longest skill
  name in the game wants 128. The type could not be raised because there was nowhere to put it.
- ⛔ **Budget three passes on a surface, not one: the second is finish, the third is corrections**
  *(#94, #95)*. The battle screen took nine. **And by the eighth pass, a complaint that sounds like
  taste is usually a constant that is wrong** *(#105)*: three of round nine's eight points were
  faults that had been shipping for weeks.
- ⛔ **By the third ask on a surface, look for the readout printing the wrong unit** *(#102)*, not for
  another box to move. SPEED counted in duration, so pressing toward the bigger number made the
  board slower.
- ⛔ **A rule that was complete when it was written breaks the day the screen grows a second kind of
  object** *(#113, #117, #121)*. When that happens, grep every placement rule that enumerates the old
  ones, and add the new candidates as a second pass rather than another rung in the first.
- ⛔ **A minimum over n is not a measurement, it is the unluckiest sample** *(#145)*, and it gets
  worse every time you look. Two nerfs were drafted against a worst case that n=50 said did not
  exist. **Measure a tail as the fraction of runs under a threshold.**
- ⛔ **Build the literal ask first when it is cheap, then measure it, then refuse it if it does
  nothing** *(#146)*. The measurement is what earns the right to ship the better version instead of
  arguing about it.
- ⛔ **Re-measure a remainder before scheduling work against it** *(#147-#149)*. #133's *"the sheet
  still scrolls 65px"* had already been closed by #136 and nobody noticed.
- ⛔ **A report's premise is a claim, and a claim about what was deleted is one `git log -S` away
  from being checked** *(#151, and #114 and #136 before it)*. Three reports now have described a
  build that does not exist.
- ⚑ **The signal usually exists already, inside the room it is about** *(#150)*. What ships is it
  being visible from where the player decides whether to open that room.
- ⚑ **A sign is not a readout** *(#151)*. A forecast carries its uncertainty on its face, a receipt
  stays exact. It is rule 11 in [`README.md`](README.md) §4 now.

> **⚑ #152 SHIPPED 2026-08-14 (8f.180): wages fall due every FOURTH day now, not daily.** He asked
> the design question first (*"how often do you think i need payments for crew? I am thinking once
> in 4-5 days +-"*), the analysis answered **4** (daily was Battle Brothers' cadence without its
> hundreds-of-days timescale; a ~22-day run wants the Wartales shape, a dated bill you can see
> coming; 4 gives one payday per fork branch and a bill one good find can cover), and he ruled
> *"ececute this new system of padayin 4 day"*. The bill **accrues silently** and the chest opens
> **loudly** on the day - a gold flash on the chip and the purse, the coin sound, a road line,
> never a card - and the new **PAYDAY chip** counts down to it, which is the "add this to global
> map more clear" half. **Every crown price came off the roads in the same order** (map labels,
> fork rows, travel buttons: days only now), which cashed #150's last open remainder, and is safe
> because accrual keeps a longer road costing more. Two wages changed hands with it: **the Captain
> draws 1** and **the ogre is paid 2**, ending the one-crown joke. The missed-payday grievance
> (#5/#114: 10 mood a day, nerve on the field, the five-day walkout) is untouched except for when
> it starts: the first short day is a payday now. **Under the pass's own rule this is a MERGE**:
> ~22 silent daily charges became 5-6 dated events, and the only new pixel is the chip that
> replaced UPKEEP. Full record: [`SHIPPED.md`](SHIPPED.md) #152 and [`CHANGELOG.md`](CHANGELOG.md)
> 8f.180.

## The work, in order

| | | done when |
|---|---|---|
| ~~**1**~~ | ~~Measure the opening before touching it.~~ ✅ **CLOSED 2026-08-25 BY THE USER, AND NOT BY A MEASUREMENT.** *"close, it is manual tuning, I was returning it many times"*. ⛑ **The step asked for a number so that #86's cuts would have something to argue with, and the cuts happened anyway** - #108 put the brawl first, #112 added a node, #118-#120 reshaped the whole opening, #138 collapsed four cards into one, #141 made the second card LIGHT, #219 put both cards on the painted stage. **Six passes of hand tuning, each judged by playing it**, and then ten people played the result. ⚠ **A count taken now would be measuring a thing that is already settled**, which is the opposite of what step 1 was for. | closed by ruling |
| **2** | **[#86 - the first fifteen minutes](#86--the-first-fifteen-minutes)** | the numbers from step 1 are cut hard, and the teaching survives the cut |
| **3** | **The screen pass**, five surfaces, one at a time, before-and-after shots | ✅ 🎒 **company** *(#133 · #136 · #140)* · ✅ 🗺 **road** *(ten rounds, #103-#107 · #112-#117 · #121)* · ✅ 📜 **aftermath** *(#122)* · ✅ ⚔ **battle** *(nine passes, #88 · #91 · #94-#105)* · ⏳ 🚪 **front door** - rebuilt by #108, #111, #118-#120, #138 and #150, and what it still owes is step 1's measurement · ⏳ **the practice field**, untouched. ⚑ ~~The target for what is left is a number: 61 declarations below 10px~~ **cashed by #164 (2026-08-16): the 10px floor raised all 153 of them and `font-size` literals in the file are 0** - re-measured by #202 on every screen, nothing under the floor. Each round's rules are in the focus section above; the rows are in [`SHIPPED.md`](SHIPPED.md) |
| **4** | **[#39 - Meet the Rabble](archive/BACKLOG_ENTRY_SPECS.md)** | the crew is introduced without a fourth modal. **It must replace or enrich the arrival cards, never add to them** |
| **5** | **Re-measure**, and hand it to the ten friends | step 1's numbers, run again |

---

## 86 · The first fifteen minutes

> 🚪 **THE FRONT DOOR** and 🗺 **THE ROAD** - also ⚔ the first fight
> **SYSTEMS** the tavern prologue · the arrival cards · `COACH` · `whisper()` · `? RULES` ·
> #60's ten Captain lessons · the fork card · the first battle's action row
> **RELATED** #39 (the crew's introduction) · #60 (its open remainder IS the per-round cap) ·
> #14 (the three onboarding layers, shipped) · #43 (the speaking budget)
> **STATE** ⛔ specced only. **The gate is unpaid: no picture yet, and no measurement yet.**

**The entry is the feedback, and the number exists so the pass has somewhere to live.**

**What the opening currently runs, in order,** and the length of this list is the finding:

the tavern prologue cards → the joke and its verdict → the lord's brief (Skelbrook) → the map card →
the arrival whispers → the first fork → **Blood on the Road**, which is a four-or-five round fight
carrying **eleven Captain lessons**, allied NPCs you do not command, a side to pick, facing arcs,
engagement, the nerve ladder, cooldowns, two actions a turn, and a race skill.

**Three candidate cuts, and none of them is decided.** They are written down so the measurement in
step 1 has something to argue with:

1. **Cap the lesson rate.** #60's own open remainder already says this: *"eight to nine lessons
   landed inside a four or five round clash... if it reads as narration rather than teaching, the
   fix is the per-round cap, not the lesson count."* ⚠ **The thing this must not delete is the
   teaching.** A lesson that fires when its rule first bites is the cheapest teaching in the build
   and the most expensive to rebuild if it goes out with the prose.
2. **Delay a system rather than explain it.** The first fight does not have to contain every rule
   the game owns. Anything that can first appear at the Ruined Steading instead of at Blood on the
   Road is one fewer thing in the fifteen minutes.
3. **Fewer options on the opening cards.** The style rule already caps a card at four options
   ([`README.md`](README.md) §4). The opening is where that is worth being stricter than the rule.

**⚠ The trap in this entry: the opening is crowded, not empty.** Every instinct to "make the opening
clearer" adds a screen. Read the complaint again before writing anything.

> ✅ **#108 SHIPPED against this entry (2026-08-12, 8f.135): the Three Bells brawl**, the user's
> direct order. The run now opens with a scripted tavern 1v1 that grows in waves and teaches
> morale, armour, kick, the corner, the ratkin and a class skill with spotlight callouts on the
> real UI, while Blood on the Road's eleven balloon lessons stay muted there and unspent. **What
> this changes for #86: the cut of the first fight's lesson load is now SAFE to make**, because the
> teaching has somewhere else to live. The measurement in step 1 still comes first. Full record:
> [`SHIPPED.md`](SHIPPED.md) #108 and [`CHANGELOG.md`](CHANGELOG.md) 8f.135.

> ⚠ **AND #112 ADDED A NODE TO THE OPENING ON 2026-08-12** *(8f.139, the user's direct order, the
> same door #108 came through)*. **The Falling Star** sits between the tavern brawl and Blood on
> the Road, which had been two fights back to back; the road east is two days where it was one, and
> the wagon starts with 17 more crowns to pay for it (his number, in his sentence).
>
> **What this means for the measurement in step 1, and it has to be said plainly: the opening is
> one card and one day LONGER than it was when the complaint arrived.** That is not a violation of
> the pass by accident, it is the user overruling it deliberately in two consecutive orders, and
> both orders point the same way: he is not asking for more systems, he is asking for the two
> fights at the start to stop being adjacent. **The Falling Star adds no system, no screen, no hint
> layer and no resource** - one card, four options, and what it teaches is the pillar, which is
> the one thing the opening is supposed to teach.
>
> ⛔ **So step 1 must measure the opening AS IT NOW IS**, including the brawl and the star, and the
> candidate cuts above are to be re-argued against that number rather than against the one in the
> complaint. Full record: [`SHIPPED.md`](SHIPPED.md) #112 and
> [`CHANGELOG.md`](CHANGELOG.md) 8f.139.
>
> ⚑ **AND ON 2026-08-13 THE STAR LEFT THAT SLOT AGAIN, WHICH IS THE FIRST TIME THIS PASS HAS
> SUBTRACTED FROM THE OPENING RATHER THAN ARGUED ABOUT IT** *(#141, 8f.169, the user: "after tavern
> fight - next event instead of faling star (it could be a random event somwhere)")*. **The node
> stays. The beat stays. The two days stay. The CARD changed**, and it changed from a four-door wish
> that spends a permanent stat to a two-door pickup that costs nothing to understand. The star went
> into `FLOATING`, so it still happens, later, somewhere nobody scheduled.
>
> ⛔ **The number step 1 has to carry forward is therefore not "the opening is one card longer" any
> more, it is "the opening is one card longer and that card is now LIGHT."** #123's choice-weight
> rules (`01_GAME_CONCEPT.md` §5) are what make that a measurable difference rather than a taste:
> the second node used to ask for a decision the player would retell and now asks for one they will
> make in two seconds. **Whether the opening still needs the beat AT ALL is the question step 1 was
> always going to answer, and it is now the only thing left to argue about here.**

> ✅ **AND #118 + #119 + #120 RESHAPED THE OPENING ON 2026-08-12** *(8f.145-147, the user's
> five-task batch, built autonomously - the same door as #108 and #112)*. **The opening is the
> user's script now: the fight FIRST, the contract as its consequence.** One backstory card (a
> failed nobleman-venturer, alone; his own pint over his head; draw your sword) → the brawl →
> the man in the corner makes the Skelbrook offer with the old three replies intact → the
> company picks its own two-word name (7×7, deliberately ugly, signed along the bottom of the
> map) → a SHORT map card → **five spotlight steps on the #108 sheet** teaching the contract,
> the chest, the company, the day and the road, each beside the real element. The menu's "How
> any of this works" row became **The tutorial fight** (the brawl as a free practice fight;
> `? RULES` still owns the reference).
>
> **What this does to this entry's ledger:** the long first map card is CUT (four paragraphs →
> two), its teaching moved next to the furniture it describes, and the lord's pre-fight card is
> GONE as a separate beat - the opening now runs card → fight → cards, not cards → cards →
> fight. **Step 1's measurement must be taken against THIS opening.** Full record:
> [`SHIPPED.md`](SHIPPED.md) #118-#120 and [`CHANGELOG.md`](CHANGELOG.md) 8f.145-147.

## 137 · The seven-item pack, and 138 · the opening's post-brawl chain

> ✅ **BOTH SHIPPED 2026-08-13** (8f.165, 8f.166) and both are on the live link. One brief from the
> user, split across two numbers because five of its items were ordinary surface work and two of
> them were the front door.
>
> ⚑ **#138 got smaller after the mockup, and that is the entry's best moment.** The drawn card was
> built to carry the two existing doors settling in place. He looked at it and cut one of them
> outright - *"delete option 'put him on the floor' it is irrelevant"* - and turned the other into
> *"Finish your drink and leave"* straight to the map. **So the opening now contains no decision at
> all**, which is further than the spec dared go and is right: the card hands over a contract, a
> crew and a name, and the first real decision becomes the fork on the road, in a game about the
> road. **The mockup earned its keep by being cheap enough to be overruled.**
>
> **[#137](archive/BACKLOG_ENTRY_SPECS.md)** *(8f.165)* - the aftermath's haul line rebuilt off
> `haul.fx` with the top bar's own `RES_ICON` glyphs instead of 11.5px prose · **DISENGAGE's extra
> hex moves from NEXT turn to THIS turn** and takes a `cool:2` lockout, which is his *"so it makes
> sense and easier to escape"* · every event door carrying `battle:` renders red from one gate in
> the renderer (five fight doors ship un-marked today) · **four `learn`-tier Captain lessons
> deleted** because the #108 spotlight now teaches the same four rules ten minutes earlier · the
> map token wears the company's own name.
>
> **[#138](archive/BACKLOG_ENTRY_SPECS.md)** *(8f.166)* - **four full-screen cards between the
> brawl and the map become one.** Two of the four carry a single button that cannot be refused,
> which is the exact defect #118's own comment describes killing once already, on either side of
> the decision. The receipt joins the corner-table card, the pick settles in place, and **the
> naming strip appears only after the contract is accepted** - his second line, and the order the
> name card's own prose already assumed. ⛔ Nothing is deleted but the two dead buttons.
>
> ⚑ **This is #86's measurement arriving as a bug report before the measurement was taken.** Step
> 1 above still owes a number, and #138 changes the thing step 1 is supposed to count, so **the
> count must be taken against the post-#138 opening.**

---

# 🟡 NEXT - after the clarity pass, still before the friends play

*Fairness and finish. **Nothing here adds a system either.** These wait because the feedback did not
name them, not because they stopped mattering.*

| # | what it is | why it waits here | full spec |
|---|---|---|---|
| **204** | 📐 **STRENGTH and AGILITY become ladders, 0-5, and the rung says what it gives** - the user, 2026-08-19: *"возле уровня показать, что он дает. Базовый уровень - условно средний для человека и обозначен 1... каждый уровень давал изменение к стату... показатели не перемножаются, а добавляются"*, and *"на здоровье влияет размер... здоровье огра большое - потому-что он большой и много силы. И реткина маленькое... Вот это уже перемножающиеся результаты"*. **PROPOSED, not built, at ROUND THREE (nine signed rungs −4..+4, 0 = the ordinary human, the RACE table carries the body, damage is one bracket with no flat, negatives are real debuffs softer than the buffs, ±4 carries a capstone; the spec's round-three section supersedes the 0-5 numbers below)**: `HP = BASE_HP[size 42/56/70] × (1 + Σ STR hp rungs)`, damage `dice + 2 × (1 + morale + Σ STR dmg rungs)`, to-hit `68 + Σ AGI`, dodge `14 + Σ AGI`; STR 0..5 = −10%hp/−10%melee · base · +10%hp · +20%melee/+10%bow · +15%hp · +20%melee/+10%bow; AGI 0..5 = −5/−5 · base · +5 hit · +5 dodge · +5 hit · +5 dodge. Measured against today on the seven authored bodies: hitpoints, to-hit and dodge within 1-2 everywhere; the swing is −8% on the two human founders (the flat +3 becoming the field-wide +2), +20% on the ratkin knives, −12% on the bow (it moved to STR at his word). ⚑ one row closes both race deltas: **size also multiplies melee ×0.9/×1/×1.1** (recommended, his call). ⛔ STR and AGI only; INT and MOR are the next iteration. ⛔ prototype only. ⚠ the work is the MIGRATION (race lean, recruit roll, seven traits, three trinkets, five armour `agi`s, six injuries, the promotion click, the save map), all listed | the ruling on the five open numbers, then build + harness n=50 | [spec](archive/BACKLOG_ENTRY_SPECS.md) |
| ~~**193**~~ | ✅ **SHIPPED 2026-08-19 (8f.212), AND REACHABLE SINCE #245 (8f.269).** 🐇 **The mirehares, and THE RED LIGHTS.** The creatures, both movement verbs and the overlay were built on the day; ⛔ **and for six days nobody could meet them**, because `f2` was authored `ev:'mirehares'` AND listed in `SLOTS_ON_MAP`, so `dealEvents()` overwrote the key before the player took a step - **0 survivals in 20 deals**. #245 made it a fixed place and taught **LINT 8h** to ask whether a painted key can ever reach a node. ⚑ **The row stayed in NEXT for four days after it shipped**, which is the reason this file's own rule says *strike it from this file entirely* when an entry lands. | 8f.212 + 8f.269 |
| ~~**192**~~ | ✅ **BOTH HALVES SHIPPED: the art 2026-08-19 (8f.215), the ITEMS 2026-08-25 (8f.269).** 🎒 **Item pack 01.** All 56 paintings are wired through one table, `ITEM_ICON{}`, onto five surfaces plus the hover tip, ⛔ **the picture a COLUMN beside the text and never a character inside the name line** (inline it is a smudge at 24px and breaks the 67px slot pitch at 40). #245 then found the other half had never been pasted at all - specced, painted, embedded, **and no `GEAR` row anywhere** - and built four to this spec's own numbers: the draught, the pilum, the shuriken and the Time-Cube. ⏳ **What is left is TWO items and it is #245's remainder, not this row's**: the Thunder-fish Kris needs a BLEEDING status that does not exist, and the Stone Shield is unbuilt. Both already have paintings sitting in `ITEM_ICON` with no row to hang on. | 8f.215 + 8f.269 |
| ~~**174**~~ | ✅ **SHIPPED 2026-08-17 (8f.202).** ✦ **Experience: the level is earned, half by the company and half by the hand** - the user's order the same day the research landed, and the one entry in this bucket that DOES add a system, by his ruling. `p.xp` cumulative, `XP_TABLE` 100..3300 (a gentle triangle, cap 10), race need ×.8/1/1.2, INT the learning rate, a fight worth **1.5 × enemy hitpoints** (the dial set by the harness: 2 overshot to 4.7, 1.5 lands act 1 at ~4) paid half evenly to everyone who stood in it, half by damage + kills; the random promotion gone; nothing on the field; the crew card's `✦ +N`; the ring on the sheet and round every bust; perk tiers 2/4/6/8; one level-0 stranger per muster wall who picks a trade on the sheet; doors may pay `xp` | **Two things are still his**: whether the hand half counts damage TAKEN and the class verb (measured: the archer finishes ~a level ahead of the spearwoman over the act), and **acts 2-4**, which levels 5-10 are a promise about. Rows in [`SHIPPED.md`](SHIPPED.md) #174 and [`WHAT_TO_TEST.md`](WHAT_TO_TEST.md) | [spec](archive/BACKLOG_ENTRY_SPECS.md) §174 |
| ~~**101**~~ | ✅ **CASHED BY #126, 2026-08-13 (8f.155).** *"The archer that is not allowed to leave"* is closed from the enemy's side, and the two gates it named were exactly the two that were wrong: the kite's `!engaged(u)` (so it switched off at the moment it was for) and AUTO having no kite at all. `fallBack` charges the parting swing now, because `walkTo` is not `clickHex`. ⚠ **Its own measurement was NOT re-taken** - 26% of archer turns with a 20%-or-better shot spent walking, and 119 dry ENGAGED turns of 874 - so whether those numbers moved is still an open reading, and #99's instrument is still the harness for it. ⚠ AUTO's kite is emergent (it retreats because the band scores hexes that way) rather than an explicit rule, so a future band change moves it silently | see [`SHIPPED.md`](SHIPPED.md) #126 |
| **13** | **The balance harness earns its keep** | ⚑ **the instrument.** Without it every balance session below is somebody's opinion. `window.ARENA` exists and has gone stale around every combat change since | [spec](archive/BACKLOG_ENTRY_SPECS.md) |
| **89** | **The combat benchmark, and the three things it did not measure** - ✅ the document is written: [`COMBAT_BENCHMARK_2026-08-11.md`](COMBAT_BENCHMARK_2026-08-11.md) | ⚑ **the second instrument.** #13 measures *win rates*, this one measures **shape**: RabbleBound now sits in a table beside Battle Brothers, Wildermyth and Wartales at **8.45 rounds · 71.4 unit-turns · 115 actions · 1.61 actions per unit-turn · 4.6 skills per unit**, all measured. ⛔ **The remainder was three MEASUREMENTS, not builds** - the mop-up tail, a real stopwatch, and hit-rate distribution - so it does not argue with the clarity pass. ✅ **HIT-RATE DISTRIBUTION IS NOW TAKEN** (2026-08-13, during #146, ~1,200 swings a side across all eight fights, probed at `hitBreakdown` inside `runFight`): **before the pass, mean 60.3% · yours 58.7% · theirs 61.8% · 30.5% of every swing in the game was a coin flip or worse.** After: **65.6% · 66.3% · 65.0% · 19.4% under fifty.** ⚑ **AND THE MEASUREMENT FOUND SOMETHING NOBODY HAD ASKED ABOUT: BEFORE THIS PASS THE ENEMY WAS THE BETTER-AIMING SIDE**, by three points, in every fight in the act. It is not in any statblock - it falls out of the penalties only the player's side pays (shooting in a scrum, their own nerve, cautious) against the flanking bonus a side with more bodies collects - which is **THE FIGURE IN A STATBLOCK IS NOT THE FIGURE ON THE BOARD** arriving on a quantity nobody had thought to point it at. ⏳ **Four measurements still open**: the mop-up tail, a real stopwatch, and **two added 2026-08-18 by PART FOUR** - the forced-answer test and token legibility. Two readings are already live: **`brigand` runs 2.4x the opener at an even 6 v 6**, which is the exact shape of Wartales' *"longer, not harder"*, and the **enemy skill gap is 2:1** in the player's favour, which is what "one strategy beats everything" looks like from the inside. ⚑ **PART FOUR (2026-08-18, the user's GPT pass) ANSWERED THE SKILL-GAP TRAP AND IS THE PART TO READ**: *the unit of variety is not the race, it is the weapon* - Battle Brothers gets ten problems out of ten humans because **its enemies run on the same weapon rules the player does** (a spear is spearwall, a shield splits, a hook drags, a hammer breaks armour). ⛔ **So RabbleBound does not need new races or ten monsters. It needs the humans, ratkin and ogres it already has to force a different answer, visibly.** Its test routes the whole argument: *"what was I forced to do here that I did not do in the previous fight?"* - **same answer = MECHANICAL problem, different answers but same feel = PRESENTATION problem.** ⚠ **And it corrected itself on the way**: the research assumed most battle tokens were undrawn, and the check disproved it - **50 painted tokens are embedded, 41 of them units, nothing falls back to the silhouette** - which turns a vague worry into the measurable question in gap **e** (distinct as a FILE, but distinct as an OBJECT at 26x39 px?) | [spec](archive/BACKLOG_ENTRY_SPECS.md) |
| - | **The class pass** - pick up each of the seven and adjust | the human test the arena cannot run: **can the class be said in one sentence.** A class that needs a paragraph is a balance problem wearing a UI problem's clothes | [reasoning](archive/PLAN_REASONING.md) |
| - | **The race pass** - three ways to play, not three stat blocks | three banked questions: the poison stack is uncapped · a gilled body reaches 62% further in the swamp · is an ogre's 3 stride a tax or a shape | [reasoning](archive/PLAN_REASONING.md) |
| ~~**50**~~ | ✅ **DONE 2026-08-25, on the user's word**: *"that balance - done"*. The eight fights are survivable, readable and fair enough for strangers, which is all this entry ever claimed - it was always *"not the terminal pass"*. ⚑ **What actually paid it was not one session**: #216 priced both sides, #229 built the Gauntlet, #227 and #245 re-tuned the finale from a drifted 53% to 40%, #239 halved salvage and cut xp, and #242 gave the bench any foe and any perk. ⏳ **The TERMINAL pass is a different entry and is not claimed here** - #80's armour bands and #45's synergy rule are still its two named debts. | done by ruling |
| ~~**27**~~ | ~~Balance: the optional fight is harder than the finale~~ ⛑ **CARRIED BY #50's closure**, since this row's own note was *"inside #50's parking lot"*, and #245 measured and moved exactly this number: the enforcer finale had drifted to **53% against the honest one's 27%** and is **40%** now. ⚠ If the optional fight still reads as the harder one in play, it comes back as its own row with a win rate attached. | carried by #50 |
| **12** | Painted faces, pack 03 | mostly the user's generator work. A face is legibility, so it is arguably clarity work, but it is not a subtraction | [spec](archive/BACKLOG_ENTRY_SPECS.md) |
| **158** | ➹ **The shot that always hits** | ⛔ **specced only, and deliberately unbuilt** - the user's own framing, 2026-08-14: *"add a skill (for the future, into the perk branch which is under development)."* An archer PERK, 2 actions, cool 3, **55-60% of the bow**, and it ignores the roll and only the roll: ⛔ the lane still refuses it, or *"always hits"* deletes #82's cover and #36's lane in one perk. ⚑ Its reason is measured rather than felt - the average chance to hit is 65.6% and 19.4% of swings are under fifty, so what an archer lacks is not power but the ability to **promise** anything. **It waits on #50 or at least #13**, because its value is exactly as large as the current hit rate is bad, and this pass has already moved that number once | [spec](archive/BACKLOG_ENTRY_SPECS.md) |
| **47** | The spear becomes a zone | **a rework, not an addition**, which argues for earlier. It touches enemy AI in **both brains**, which is not what you destabilise the week before strangers arrive. **The trigger that pulls it forward:** the class pass finds the spearwoman unsayable in one sentence | [spec](archive/BACKLOG_ENTRY_SPECS.md) |

---

# ⚪ LATER - the depth pass, after the friends have played

*This is not a demotion. Most of it gets **cheaper** once the two foundations at the top exist.*

**Two foundations gate the rest, and the word gate means something else cannot start:**

| # | what it is | what is blocked behind it |
|---|---|---|
| **24** | **The provenance ledger** - `addFact` / `facts` / `latestFact` + `characterRef()`. The id is the reference; the snapshot is so a dead, dismissed or renamed person can still be talked about three hours later. **Double-write first, migrate never** | **#19 #22 #23 #25**, distant #26, #34's chain, and all of Act 2's memory. ⚑ **#38's typed bonds are its first real tenant** and currently live in their own store: absorb it rather than leaving two ledgers |
| **4+17** | **Body parts you can lose + mutations you can see.** One painter API, used in opposite directions: a lost part draws as outline-plus-cross, a mutated part is redrawn as a different shape | **#34 #35 #16** |

**Then, and the order inside is the tier order:**

| # | what it is | one line |
|---|---|---|
| **34** | ☠ **The mortality chain** | ✅ **SHIPPED as #225, 2026-08-21 (8f.248)**, on the user's own shape: **INJURED -> SCARRED -> gone**, where the middle rung is a temporary injury with a clock rather than #4's maiming. It therefore no longer waits on #4+#17. **Death is still the end of a visible chain, never a roll.** ⏳ Two halves of the original spec are still open and are the next work here: **AUTO may never spend somebody on their last rung**, and the road's `armourSac` door hands a body up the ladder without saying so |
| **32** | The rally rule | a routed body may not re-cross the rout line alone: it needs the Captain within 3 hexes or a steady ally adjacent |
| **2** | Personalities that command the AI | AUTO becomes a judgement about who you trust with which part of a fight |
| **35** | Grafts: rare authored consequences | of injuries **or of events on the road**. Not a doctor's menu. Start with one: Shield-Skull |
| **45** | ★ Reward the combination, not the repetition | the synergy rule. Deferred, and it returns as the first thing the terminal #50 builds |
| **78** | A good thing you carry takes something away | the gear side-effect contract. **The cost is paid on a different axis from the benefit.** ⚠ its quiver is blocked on a ruling that reopens #45 |
| **80** | The weapon gets its half of the armour bands back | ⛔ **hard order constraint: before the TERMINAL #50**, or the sweep gets done twice. #79 left a measured balance debt and this moves the number back |
| **16** | The rest of the mutations | 3 or 4 of the 14, never all. Each needs a real battle rule |
| **3** | The clash recruit is already changed | puts a mutation in front of the player in the first hour and gives the tutorial fight a cause |
| **22** | The road reads the company | waits on #24. *"Every Rabble is different"* should be visible in the prose, not only the stat screen |
| **19** | Desperation contracts at the Muster | waits on #24. ⚠ owes a rewrite: it promised a recruit *first pick of the loot* and there is no pick any more |
| **23** | The cost of command | waits on #24. The Captain's sheet only, and **facts with names, never a morality score** |
| **25** | Personalities that earn their evolution | waits on #24. Count qualified risky successes, never repeated actions |
| **59** | The woman from the cage comes back | spare Wynn, and in a **later run** she is waiting at the last quiet node and fights the Snare as an ally. She joins the fight, never the company |
| - | **Settlements & Legacy MVP** | the inheritance screen first. It is what makes a lost run mean something. [`09_SETTLEMENTS_AND_LEGACY.md`](09_SETTLEMENTS_AND_LEGACY.md) |
| **21** | What they did without you | strict thresholds and authored templates. If nothing exceptional happened, show nothing |

---

# ⏸ PARKED - with intent, never rejected

| # | what it is | why it waits |
|---|---|---|
| **134** | 🃏 **The joke at the man with paid swords** - the opening's cut third reply. *(Parked 2026-08-13 by the user: "make a joke option put from the opning to our ideas/things for future set.")* **"Agree with him. Cheerfully."** answered *"You scum"* with *"Scum, my lord, and cheap with it. You will not find better value in this county, and you have clearly been looking."* Then `Math.random()<0.5`: it lands and he pays **120** instead of 80, tells his swords this one can read, and leaves without shutting the door; or it does not, and the nearer sword puts a cudgel in your ribs for **50** and 16% of the Captain's hitpoints. | ⛔ **Why it was the right one of the three to cut**, and the note to argue with when it comes back: it put a **coin flip on the player's first real decision**. The other two are the pillar in one card (pride costs money, money costs pride) and a fifty-fifty beside them is not a third door, it is a lottery ticket nobody can reason about. **What it needs to return is a reason the player can read** - a stat gate, a trait, a personality, anything that makes the joke a judgement instead of a dice roll. ⚑ **Nothing was torn out:** `PRO_ART.joke_hit` still maps to EV00C (the rule file requires that mapping preserved and the painting is good), and the `verdict` banner machinery is intact. Full prose in [`CHANGELOG.md`](CHANGELOG.md) 8f.162, so this is a paste, not a rewrite |
| **49** | Make the AI explain itself | **deferred by the user.** The gate was paid: the rules are written and the picture was drawn, so it returns as a *build* session, not a design one. ⚠ **The picture itself is gone** - `shots/49_ai_explains.html` was deleted in the 2026-08-14 shots cleanup, and `shots/` was never in git. The written half survives in full at [`archive/BACKLOG_ENTRY_SPECS.md`](archive/BACKLOG_ENTRY_SPECS.md) §49, so redrawing it is an hour, not a session |
| **16** | 🧬 **MUTATIONS - parked for the whole first game, behind one flag** *(2026-08-18, the user's call after weighing it)* | ⛔ **NOT A CUT. `MUTATIONS_ON=false` is the entire change**, and nothing was torn out: `MUTS`, both mutation acts (SINK BELOW, ROOTING GRASP), the gilled sprite state and its badge, `wetproof`, the sheet chip, the doll line and the `needMut` gate are all still there and all unreachable. **The only two statements in the game that write `p.mut` are inside the ripening block the flag guards**, which is what makes one flag enough - proved structurally, not by reading. Flip it and all ~35 touch points are live again with no archaeology.<br><br>**His reasoning, verbatim:** *"уже сложно: навыки, способности, типы врагов, арты со своими особенностями, метательное, расходники. Мутации звучат круто, но это огромный объём работы... раса и черты уже дают разброс. Слишком много систем, слишком много экранов."*<br><br>**And the measurement that agreed with him:** the system was **2 mutations** against ~35 integration points, and **exactly one door in the entire content set** gated on it (`chapel[1]`, needMut=gills). ⚑ **The slot was already occupied by SCARS** - permanent, stacking, removable once a run - which is the same design job (*run history written on a body*) at a fraction of the surface. And #89 part four says the unit of variety is the **weapon**, not a third axis.<br><br>⚑ **WHAT SURVIVES ON PURPOSE: THE COUGH.** The Fen-Mother's drag still marks whoever she held under, because that chain is the payoff of the hardest fight in the act and it is already built. It is now a plain permanent mark - **no bonus, no penalty, no clock** - per his instruction *"просто кашель, который не даёт ни позитивных, ни негативных эффектов, просто остаётся на персонаже."* Its description and its news line were rewritten: both used to promise a becoming and a bone-setter cure, and **neither was ever implemented** - the cure was always prose.<br><br>⚠ **The Bloom's `nail` is off too**, and that is deliberate rather than collateral: a magenta fingernail whose whole job is to make the player watch something coming is a promise the game will never keep. The door keeps its 8 morale and its bloom-stem; only the dangling chain is gone.<br><br>⚠ **`EVENTS.chapel[1]` is now permanently invisible** (it needs gills and nobody can have gills). Left in place, because it comes back with the system.<br><br>⛔ **THE DECISION POINT IS THE GODOT PORT, NOT THE NEXT SESSION.** *"Когда будем передавать на Godot, на движок, тогда надо будет думать, хотим ли мы её делать или нет."* Until then this stays false and nobody re-opens the argument |
| **26** | The Smaller One | seven cards of content. Needs #24 first. **First candidate to graduate back** |
| **28** | The Act 1 stinger | demo-ending content, and the enemy still needs a real name |
| **29** | Zone of Control, the better version | cut once for making movement unaffordable. Needs #13 to measure a return |
| **30** | Action economy remainder: quiver, slow mage | its base rule was superseded by #45; **#78 is the new contract it waits for** |
| **37** | AUTO doctrines: PRESS · KEEP LINE · HOLD & SCREEN | needs a mature, trusted AUTO, so it sits on #2 |
| **42** | AUTO gets one flourish | after #37 |
| **43** | The narrator's budget | production-stage. **The audio ranking applies today**: action readability → barks → narrator → ambience |
| - | **ACT 2** | parked by the user 2026-08-01: *"Delete act 2 for now as in plan."* Nothing about it is rejected. **Deeper beats longer**, and the feedback above is an argument for that, not against it |
| - | The ScenarioSpec / dev-menu half of the Battle Lab | tools for the builder, not the game. Test mode and the linter cover the need |
| - | The Godot port | still the production target. Scope-lock after the systems stop moving |

---

# 👤 YOURS - not a build session

*Running in parallel, and none of it consumes a session here.*

| | what it is | state |
|---|---|---|
| **#77's round trip** | you edit [`content/events_book.html`](../content/events_book.html), then a session reads your edits back into the prototype | ⏳ **the doc is generated; nothing reads it back yet.** The three rules that make it possible are in the doc: keep the `[key]`, comment a deletion, leave the `{TOKENS}` |
| **The event art** | no event card falls back to a placeholder. By your own order this waits on the texts | ⏳ |
| **The unit sprites** | a body still reads at **x2.50**, the closest camera stop. **The size target is a number now, not a guess** | ⏳ |
| **Music and sound** | #8 shipped a sound for every verb and both rungs of the nerve ladder, and **the builder cannot hear it.** Step one is you listening and ruling | ⏳ |
| **#76's form** | the mailto send shipped in 8f.86; you verify it from the published page | ⏳ |
| **#57 event analytics** | ⛔ **do not pick this up here.** Your own task in a separate tab, the ANALYTICS tab of [`tools/dramaturge.html`](../tools/dramaturge.html), and **its spec lives inside the tool.** What this repo owes it: a stable journal blob, currently **v3**. If a row's *shape* ever changes, bump `v` and say so here | 🚧 running |

---

# ✅ The gate: ten friends - PAID, 2026-08-25

> **Ten people have played it** (*"10 people played, I slowly implement feedback"*). The gate is
> behind us and the feedback is now a QUEUE rather than a verdict, worked through a batch at a time:
> #243 eighteen asks, #244 sixteen, #245 eleven, #246 twelve, #247 fourteen. ⛑ **The section below
> is kept for the question list and for what gate 2 would be**, not as a thing still to reach.
>
> ⚑ **The one line worth carrying forward is what the batches keep proving**: in #245 and again in
> #247, **three asks in a row turned out to be features that had SHIPPED and could not be SEEN**.
> A player saying *"I have never seen X"* is the only instrument this project has for that class of
> fault, and no gate, linter or probe found any of them first. **That is what ten players bought.**

**What the gate was for:** *does it survive somebody who is not you?* Friends forgive rough edges, so
this gate tests **understanding**, not tolerance. That is exactly what the 2026-08-10 feedback is
about, which is why the focus above replaced the old thirteen-step list.

**The collection instrument already exists and needs no work.** #53 and #54 shipped the ★▲▼ bar on
every screen, reactions attached to the exact screen, a decision log, a per-run report, and a
copy-paste blob so a friend's journal travels home. #76 added the questionnaire and the send. So the
gate needs a build, a link, and a sentence asking them to paste the blob back.

**Questions to ask them, written before they play, because a question nobody wrote down is a run
wasted.** Each one came from a shipped entry that said some version of *"the harness structurally
cannot answer this"*:

| the question | from |
|---|---|
| is **"send somebody wide"** on the Thing in Armour worth the person it costs? | #18 |
| does the **bloom flower** actually force you to move? *(that was its whole reason)* | #64 |
| should the **Fen-Mother** get flowers after all? 11-29 rounds with, 8-23 without | #64 |
| is **24% wet** enough to READ as a swamp? the honest lever is the painting, not more water | #63 |
| is the **forest** interesting, or merely slow? | #56 |
| do the **sounds** actually sound right? | #8 |
| does the **first-fight teaching** read as teaching, or as narration? | #60 |
| what is the **haul worth** now that it cannot be optimised against the stash? | #55 |
| the **four camera questions**: distances, default stop, claustrophobia, hex text | #66 |
| is **×1.75** the right board speed? | #81 |

**Split them.** Feel-over-a-whole-run goes to the friends. A single screen or a single number goes
to a playthrough here, which is cheaper and repeatable. **Do not spend a human on anything the arena
could have measured.**

*(Gate 2, twenty to thirty strangers, is deliberately unwritten. What is worth recording now is what
changes between ten and thirty: **a friend asks you what a screen means, a stranger closes it.**)*

---

# Rulings still waiting on you

> ## ➡ **THEY ALL LIVE IN [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md) NOW** *(#248, 2026-08-25)*
>
> The user: *"what questions? can you please gather all questions and send to me in one go?"*
>
> ⛔ **The reason he had never seen them as a list is that there were FOUR lists and one of them was
> not a list at all.** Five rulings sat here, four design questions in [`README.md`](README.md) §8,
> **twenty marked `👤` inside `SHIPPED.md` rows** - a convention that was faithfully written for
> twenty entries and that nothing ever collected - and the rest as `⏳` remainders spread over ten
> entries. **A question filed in the row of the entry that raised it is filed where nobody will ever
> look for it.**
>
> ⛑ **So the rule is now the same one this project uses for everything else: ONE place, and the
> other places point at it.** A `👤` in a `SHIPPED.md` row is still the right way to RAISE one - it
> is written where the reasoning is - but it is not where it lives. **An entry that raises a ruling
> adds a row to `OPEN_QUESTIONS.md` in the same session**, the way it already adds a row to
> `WHAT_TO_TEST.md`.
>
> ⚠ **Ten of them came back ANSWERED by the sweep and are in that file's section F**, unstruck,
> because a question closed by somebody other than the person who asked it should be seen before it
> disappears.

**The one older item that is not a taste call**, from the QA day
([`QA_PLAYTEST_2026-08-02.md`](QA_PLAYTEST_2026-08-02.md)):

- **QA-27: the defeat epilogue opens with *"The bells are still ringing"*** wherever the company
  dies, including half a map from Grausen. Keep it placeless, or key one clause on where the run
  ended.

# ⛔ PARKED BY THE LANDING SWEEP OF 2026-08-21

*Six desks landed together on 2026-08-21 (#215 through #222). Everything below was found while
checking that nothing had been lost, and none of it was fixed in that session. It is here rather than
in six session reports nobody will reopen. ⚡ **None of it blocks anything and none of it is a
regression**: every item either shipped this way or was never written down at all.*

## The record has holes, and the sweep is how they were found

~~⛔ **NINE NUMBERS ARE CLAIMED AND APPEAR NOWHERE IN THE DOCS.**~~ ✅ **ANSWERED BY #248,
2026-08-25, and the answer is that no work was lost.** Re-grepped four days later: all nine still
appear nowhere but in this section. ⛑ **The section framed it as a two-way question** - *either the
work shipped and wants its row, or it was abandoned and wants `claim.ps1 release`* - **and there is a
third answer it did not have, which covers most of them:**

| the claim | what it actually is |
|---|---|
| `#204` + `8f.226` | **the entry number is correctly held.** #204 is a LIVE row in 🟡 NEXT above - the STR/AGI ladders, specced, deliberately unbuilt, waiting on your ruling. Nothing is stranded; a build number was taken beside it and never spent |
| `#158` + `8f.186` | **the same shape.** #158 is a LIVE row in 🟡 NEXT - *the shot that always hits*, ⛔ *specced only, and deliberately unbuilt*, in your own framing |
| `#207` + `8f.229` + `8f.230` | **shipped, as DOCUMENTS.** The event book and the hover book were delivered 2026-08-21. A deliverable that is two generated files has no build-log row to write, so the two `8f` numbers beside it were never going to be spent. ⚠ This section guessed it *"describes what shipped as #208"*; that is the claim TITLE being stale, not the work being missing |
| `#153` | **no trace, and nothing missing from the build.** *the doc cut and clean-out*, and the doc cut demonstrably happened |
| `8f.213` | **never spent.** The changelog runs 8f.212 → 8f.215, and #192 part two took 8f.215 |

⛑ **SO THE RULE THE SWEEP WAS REACHING FOR IS THIS: AN UNSPENT NUMBER IS NOT EVIDENCE OF LOST
WORK.** `claim.ps1` issues an entry number and a build number together, and the two are spent by
different things - an entry number by a row in this file, a build number by a row in the changelog.
**A specced-and-parked entry spends the first and never the second, on purpose**, and that is the
healthy majority of this table rather than the exception.

⚡ **AND THE NINE CLAIM FILES WERE ALREADY FREED**, into `.grimtoll/freed/20260821-163937-sweep/`,
because the table above CITES all nine and the pre-commit guard reads a citation and a spend
identically (#144's trap, which #217 taught it only for a stacked desk). ⚠ **Freeing them lost
nothing and burning them cost nothing**: the issuing floor comes from a scan of the REPO, so all nine
have been unissuable since the day that paragraph was committed, claim file or no claim file.

⛔ **AND TWENTY-TWO SHIPPED ENTRIES HAVE NO ROW IN THE REGISTRY.** ⚠ **This entry first said SIX,
because the sweep had only looked from #195 up.** Counted over the whole record it is **22**: sixteen
below #195 (45 · 71 · 154 · 165 · 169-172 · 187-194) and six above it (#196, #198, #199, #201, #208,
#209). Each has a `CHANGELOG.md` row and none has a `SHIPPED.md` row. **#209 is the painted event
stage**, i.e. the largest single change on the board, so this is not a tail of small ones. ⛑ The
reason to care is stated in `SHIPPED.md`'s own header: it is **the registry that names every open
remainder**, so an entry missing from it has its leftovers recorded nowhere.

⚡ **AND THIS EXACT FAILURE IS ALREADY WRITTEN DOWN IN THAT HEADER, WHICH IS WHY IT IS NOW COUNTED
RATHER THAN NOTICED.** `SHIPPED.md` states the contract - *"when an entry ships, four writes"* - and
records the day it broke: *"#117, #137 and #138 were added to this table on 2026-08-14 by #151's
audit ... so the four writes were three."* That audit was done by hand, once, and left nothing behind
to do it again. **`python tools/record.py check` is what was left behind this time**, and the six
above the floor are named in its `KNOWN_MISSING_SHIPPED` list so that a NEW gap fires while these
stay parked. Each leaves that list the day its row is written.

⚡ **THE THREE ROWS THAT WERE WRONG WERE REPAIRED RATHER THAN PARKED**, and they are worth knowing
because two of them came out of the same mechanism: `docs/CHANGELOG.md` and `docs/SHIPPED.md` are
`merge=union` in `.gitattributes`. #221's row had been written INTO THE MIDDLE of #220's and rendered
as one broken line; #215's row was in `SHIPPED.md` twice, once as authored and once as #217 edited it
to close its remainder; and #222 had written no row anywhere at all. ⛔ **A UNION MERGE NEVER
CONFLICTS, WHICH IS WHY IT NEEDS A COUNTER**: the duplicate and the splice both merged clean and
neither would have been seen by reading the diff.

## Open remainders the desks declared, and one that half closed itself

- ~~**#263: the enemy runs on the four stats.**~~ ✅ **BUILT AND MEASURED 2026-08-28 (8f.288).** The spec is still in [`archive/BACKLOG_ENTRY_SPECS.md`](archive/BACKLOG_ENTRY_SPECS.md) and the standing contract is `.claude/rules/enemy-stats.md`. ⚠ **What it did NOT do is the part to carry forward**: the Snare is still 93% for the starting four at n=30, so `OPEN_QUESTIONS.md` **O1 stands** - the finale is a ratkin deck and the ratkin lean is close to a wash. Filed as **P2**, with **P1** asking whether the Steading-Line at 20% and the broken men at 53% came down too far.

- **#216: the easy / even / hard multipliers are a first cut.** 1.3 / 1.0 / 0.75 against a fight's
  points, and they ignore the player's tactical edge entirely. ⚡ The scale itself validated against
  something it never read (the seasoned six price at 202 against the six-body Snare's 199, the fight
  #160 measured at 45% win), so it is the MULTIPLIERS that want refining against real win rates when
  the balancing pass starts, not the pricing under them.
- ~~**#222: the `2>$null` sweep was never run.**~~ ✅ **CASHED BY #226 the same day**, and this row
  outlived it by a session: *"#222's own declared sweep was run: three live `2>$null` sites, proved by
  making them fire, plus a guard `deploy.ps1` was missing - an unreadable branch silently PASSED the
  check that keeps a work branch off the live link."* ⛑ **The lesson is the one this whole section is
  about**: a remainder can be cashed by the NEXT entry without anybody going back to strike the row, so
  re-read a parked item before scheduling work against it (#133's own finding, twice now).
- **#214's art remainder is HALF closed and #248 measured the other half exactly.** Its row read *`18 of 40 ``EVENTS`` cards, plus every one of the 19 ``CAMPS`` incidents and all 10 ``VIGNETTES```*. Driven through `jStageFor` on 2026-08-25: **0 road cards without a wide master and 19 camps with none**, plus the 10 vignettes. So the road deck is done and **the remainder is exactly those 29 cards**, each a 1672x941 master plus one row in `JSTAGE`. A legitimate shipping state per `.claude/rules/static-event-art.md`, and now a number instead of a feeling.

- **#224: the items with no painting are FOUR, not two** *(counted by `probes/reach.js`, #248)*. THE SECOND SHAPE and the crossbow were the two this row named; `tmap` and `draught` arrived with #245's BAG and nobody added them here. All four fall back to `gGlyph`, which is a legitimate shipping state and still four items whose whole point is that you can see what they are. ⚑ **Two of the paintings already exist and the ITEMS do not**: `kris` and `stoneshield` are mapped in `ITEM_ICON` with no `GEAR` row (deliberate, per the art rule), and #245's remainder says the kris needs a BLEEDING status that does not exist. Each of the four is one 128x128 cut-out with alpha plus one row.

- ~~**#236: the opening fight stalled once, and thirty more runs could not reproduce it.**~~ ✅ **CLOSED BY #248, 2026-08-25, and it was never the game.** This row asked for *`a seeded loop over that one fight at n>=200 with ``B.order`` dumped on the failing run`* and it did not need one: **the reproduction is a SEQUENCE, not a seed.** A fresh page then `regress.js` is clean; a fresh page then `gates.js` then `smoke.js` then `regress.js` stalled **4 of 4**. Both #236 and #239 recorded that shape in passing and read it as noise. ⛑ **What it was**: the game raises a spotlight or Captain card mid-fight, `nextTurn` takes its `if(B.tutLock)` door, and the harness has nobody to click it - a player does, and the fight carries on. **The flag that silences it existed and said in its own comment that the harness sets it**; nothing set it. 0 of 6 after. ⚡ **The lesson worth keeping is the one #226 already wrote down**: a check a document says exists is a claim about the build. #239's row said *``runFight`` already returns ``guard``, so the instrument exists`* - and `HIT GUARD` says the loop gave up, never what it gave up ON. It dumps its state now.

- ~~**878 em dashes survive in the DOCS**, against a standing hard rule of none anywhere.~~ ✅ **SWEPT BY #248, 2026-08-25, and the reason it waited was an assumption rather than the work.** This row parked it because #199 *`needed a lexer to tell a player-facing string from a code sample, and the docs have the same problem worse`*. ⛑ **They do not.** That difficulty is a fact about one 3.4 MB HTML file; in markdown a fenced block opens and closes on three backticks and an inline span on one, so the whole lexer is four lines. `tools/dev/emdash.py` counts by WHERE the dash sits and `--fix` rewrites prose only: **1,197 prose, 0 left, 24 inside fenced code untouched**, 1,028 lines out and 1,028 back. ⛔ **`docs/playtests/` is deliberately excluded**: it is a record of what was on the screen in front of Dima, Sanya and Andrey on a build three days older than #199, and **a transcript that is corrected is not a transcript**. ⚑ **And it is a note in `record.py check` now**, proved by seeding one and watching it fire, or it drifts straight back.

⚠ **AND ONE ENVIRONMENT NOTE, NOT A DEFECT**: `claim.ps1`'s repo scan intermittently runs past two to
  three minutes on this machine because the repo lives on a Google Drive path. It looks exactly like a
  hang and it is not one. Same shape as `LINT()`'s documented ~25s.

---

# How this file works

**One list, one order, one truth.** ⛔ **There is no second priority system.** Tiers, date buckets,
screen groups and a thirteen-step gate plan used to sit on top of each other in this file, and four
overlapping orders is the same complaint the playtesters made about the game. They were collapsed
into the four headings above on 2026-08-10. The reasoning that produced them is kept in
[`archive/PLAN_REASONING.md`](archive/PLAN_REASONING.md).

**One entry per session.** Do not batch. Each touches combat or character systems that are easy to
disturb.

> ## ⛔ NOTHING HERE GETS BUILT STRAIGHT FROM THE ENTRY
>
> **1. Write out the actual rules** - costs, targets, cooldowns, limits, what it takes away.
> **2. Show a picture. Mandatory.** ⚑ **Make it IN THE GAME** whenever the thing has a screen
> already: stand the real board up in the practice field and `shotBoard()` it, annotated. Hand-draw
> only when there is nothing to photograph, and keep it to the **two or three panels that carry the
> decision**. **3. Then build it. 4. Write its section in
> [`WHAT_TO_TEST.md`](WHAT_TO_TEST.md)** the same session it ships.
>
> Set by the user 2026-07-31: *"That way we keep it clean and don't do extra job."* Every time it
> has been skipped, work has been thrown away.
>
> Applies to any new ability, resource, screen, panel, overlay or player-facing rule. Does not apply
> to bug fixes, wording, or balance numbers on something that already exists.

**When an entry ships, FIVE writes:** full text and a build-log row → [`CHANGELOG.md`](CHANGELOG.md)
· a one-line row → [`SHIPPED.md`](SHIPPED.md) · **strike it from this file entirely** · a section →
[`WHAT_TO_TEST.md`](WHAT_TO_TEST.md) · **and every ruling it raised → [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md)**.

> ## ⛔ THE FIFTH WRITE, AND IT IS THE ONE YOU ONLY OWE SOMETIMES *(#248, 2026-08-25)*
>
> The user: *"make a rule to put all questions in only 1 file"*. Full rule: [`.claude/rules/open-questions.md`](../.claude/rules/open-questions.md).
>
> ⛔ **When that was asked there were 39 open questions in FOUR places, and TWENTY of them were marked `👤` inside `SHIPPED.md` rows** - a real convention, used correctly, in twenty entries over five months, **that nothing ever collected**. Some had waited since #90. ⚑ **Ten came back answered by work that was not about them**, which is the cost: a question nobody can find does not wait, it rots, and it hides the twenty-nine beside it.
>
> ⛑ **RAISE IT WHERE THE REASONING IS, FILE IT WHERE HE READS.** A `👤` beside the measurement and the rejected alternative is still right and should keep being written - that is the raising. **The row in `OPEN_QUESTIONS.md` in the same session is the filing**, and the answer strikes that row in the session that acts on it.
>
> ⚠ **Unlike the other four this write is CONDITIONAL** - most entries raise nothing - **which is exactly why it is checked rather than remembered.** `record.py check` names a `👤` whose entry has no row there.

> ## ⛔ DO NOT PICK A NUMBER. ASK FOR ONE.
>
> ```
> powershell -NoProfile -ExecutionPolicy Bypass -File tools\claim.ps1 number
> ```
>
> **Run it before you write anything.** It scans the docs, the prototype and `shots/`, hands you a
> `#NN` and an `8f.NNN`, and reserves them in a file that cannot be created twice, so a session
> running beside you cannot be given the same ones. `claim.ps1 status` says who holds what.
> **`git commit` now refuses a change that spends a number somebody else is holding.**
>
> **This replaced a sentence in this document, and the sentence was the bug.** It said *next free
> #NN*, it was maintained by hand, and it was written **last**: the code and `shots/` get the number
> first. So at the moment the other session grepped, this line was already wrong. That cost **five
> collisions** and nearly two more on 2026-08-11, when both docs said *next free #89* while two
> sessions were both building #89. ⚑ **Neither tell was in a document.** `ls -t shots/` caught both,
> which is why the script reads `shots/` too.
>
> Full rules, and what else two sessions collide on: [`PARALLEL_SESSIONS.md`](PARALLEL_SESSIONS.md).
> **If you are going to change the prototype, take it first:** `claim.ps1 lock -Title "..."`.

**Which model builds it.** 🧠 **STRONG** for anything touching the two AI brains, morale, the save,
combat rules or the game's voice: every expensive bug here was subtle-systemic, and the tone is as
easy to get almost-right as the code. 🔧 **SONNET-OK** for mechanical, render-only or
schema-following work with an exact spec and a verifiable result. Either way **the strong model owns
the session** and may hand fenced mechanical sub-chunks down; the reverse is forbidden, because a
cheap model cannot escalate judgment it does not know it is missing.

---

# Where everything else lives

| | |
|---|---|
| [`archive/BACKLOG_ENTRY_SPECS.md`](archive/BACKLOG_ENTRY_SPECS.md) | **the full text of every unbuilt entry.** Come here when you pick one up |
| [`archive/PLAN_REASONING.md`](archive/PLAN_REASONING.md) | why an entry waits, what was rejected and on what grounds. History, not instructions |
| [`README.md`](README.md) | what the game is, the pillar, the traps that keep biting |
| [`SHIPPED.md`](SHIPPED.md) | one line per shipped entry, and **every open remainder named** |
| [`CHANGELOG.md`](CHANGELOG.md) | the build log with the reasoning, and every shipped entry in full |
| [`WHAT_TO_TEST.md`](WHAT_TO_TEST.md) | **the user's file.** How to reach each new thing in three steps and what it should do |
| [`OPEN_QUESTIONS.md`](OPEN_QUESTIONS.md) | **the user's other file.** Every ruling waiting on him and nothing else. A new one lands here the session it is raised; [`archive/QUESTIONS_ANSWERED.md`](archive/QUESTIONS_ANSWERED.md) takes them once they are closed, parked or overtaken |
| the running build | **what is actually true. It wins over every document.** |
